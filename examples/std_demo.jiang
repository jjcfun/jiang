import std;

UInt8[] msg = std.string.identity("hello from std");
std.io.println(msg);
std.assert.require(std.path.is_absolute("/tmp/demo"));

{
    _ file = std.file.File.open(1);
    std.assert.require(file.is_valid());
    file.close();
}
