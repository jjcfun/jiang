import std;

UInt8[] msg = "hello from std";
std.io.println(msg);
std.debug.assert(std.fs.is_absolute("/tmp/demo"));

{
    _ file = std.fs.File.open(1);
    std.debug.assert(file.is_valid());
    file.close();
}
