public struct Box {
    Int value;
}

enum TokenKind {
    ident,
    eof,
}

UInt8[] bytes = "ok\n";
Int[3] nums = {1, 2, 3};
Bool enabled = 1 != 0;
Int* ptr = new value;

if (value >= 1) {
    print("ge\n");
} else {
    print("lt\n");
}

while (value <= 9) {
    value = value + 1;
    break;
}

for Int i in 0..3 {
    sudo {
        print("range\n");
    }
    continue;
}

switch ($ptr.tag) {
else: {
    print("done\n");
}
}
