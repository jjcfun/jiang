// 验证 release 安装包中的编译器可以构建并运行最小 hosted 程序。

import io = "../../src/compiler/system/io.jiang";

Int main() {
    if (!io.stdout().write_all("Hello from Jiang\n")) {
        return 1;
    }
    return 0;
}
