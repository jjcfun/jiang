// check-fail
// expected: positional_after_named_arg
// 命名参数后不能再出现位置参数。

Int value(Int first, Int second = 2, Int third = 3) {
    first + second + third
}

Int main() {
    return value(third: 5, 1);
}
