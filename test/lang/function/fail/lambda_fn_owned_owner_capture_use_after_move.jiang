// check-fail
// expected: use_after_move

struct Resource: Movable {
    Int value;

    deinit(self) {}
}

Int main() {
    Resource resource!;
    Fn<Int>^ reader = new { [resource] => resource.value };
    Int value = resource.value;
    value + reader()
}
