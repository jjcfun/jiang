// check-fail
// expected: capture_initializer_removed
// capture list 只选择已有局部变量及其捕获方式，不声明新变量或执行表达式。

Int invalid_capture_initializer() {
    Int value = 1;
    Fn<Int> read = { [saved = value] => saved };
    read()
}
