// expected: function_body_type_mismatch

struct Meter {
    Int value;

    Int add(self, Int extra) {
        self.value + extra
    }

    Fn<Int, Int> bad(self) {
        self.add
    }
}

Int main() {
    Meter meter = Meter(value: 1);
    meter.bad()(2)
}
