// check-fail
// expected-error@11: async intrinsic call is not allowed inside await expression

async Int load(Int value) {
    value
}

async Int main() {
    Future<Int> value = load$(1).async();
    await {
        _ nested = load$(2).async();
        value + nested
    }
}
