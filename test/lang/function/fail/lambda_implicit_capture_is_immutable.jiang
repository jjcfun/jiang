// check-fail
// expected: immutable_assignment_target
// 隐式捕获只建立共享借用，即使源 binding 可变也不能通过闭包写入。

Int invalid_implicit_write() {
    Int value! = 0;
    Fn<Int> increment = { =>
        value = value + 1;
        value
    };
    increment()
}
