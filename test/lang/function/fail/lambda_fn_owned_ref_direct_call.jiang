// check-fail
// expected: owned_closure_ref_call_requires_ref

Fn<Int>^? make_answer() {
    Fn<Int>^ answer = new { => 42 };
    return answer;
}

Int main() {
    Fn<Int>^? maybe = make_answer();
    guard maybe is .some(ref _ answer_ref) else {
        return 1;
    }
    answer_ref()
}
