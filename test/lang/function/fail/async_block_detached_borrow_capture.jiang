// check-fail
// expected: borrowed_value_escape

struct PageDomain: Domain<kind = .serial> {}

const PageDomain page = PageDomain();

async [page] Int detached_capture() {
    Int bias = 3;
    async [page] {
        bias + 1
    }
    0
}
