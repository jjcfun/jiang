// check-fail
// expected: generic_arg_arity_mismatch

struct PageDomain: Domain<kind = .serial> {}

const PageDomain page = PageDomain();

async Int check_future_domain_arg() {
    Future<Int, page> future;
    0
}
