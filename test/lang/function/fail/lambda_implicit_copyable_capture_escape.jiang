// check-fail
// expected: implicit_lifetime_contract_requires_annotation
// Copyable local 的隐式捕获仍是共享借用，不能随 owned closure 逃逸。

Fn<Int>^ invalid_implicit_escape() {
    Int value = 1;
    return new { => value };
}
