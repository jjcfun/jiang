// check-fail
// expected: mutable_borrow_conflict

struct Box {
    Int value!;
}

Int main() {
    Box box! = Box(value: 7);
    Int& first = box.value$.ref();
    Fn<Int> read = { [ref! box] => box.value };
    first$.get() + read()
}
