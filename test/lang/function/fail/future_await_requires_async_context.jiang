// expected: future_await_requires_async_context

struct PageDomain: Domain<kind = .serial> {}

const PageDomain page = PageDomain();

async [page] Int load() {
    1
}

Int main() {
    Future<Int> value = load$().async();
    value.await()
}
