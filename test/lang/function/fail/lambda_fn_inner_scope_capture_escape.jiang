// stack closure 不能活过隐式引用捕获的来源。
// check-fail
// expected: borrowed_value_escape

Int main() {
    Fn<Int> callback! = { => 0 };
    {
        Int value = 41;
        callback = { => value + 1 };
    }
    callback()
}
