// owned closure 的 heap environment 不能让捕获引用活过来源。
// check-fail
// expected: borrowed_value_escape

Int main() {
    Fn<Int>^ callback! = new { => 0 };
    {
        Int value = 41;
        callback = new { => value + 1 };
    }
    callback$.ref()()
}
