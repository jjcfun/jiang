// check-fail
// expected: immutable_borrow_source

struct Box {
    Int value!;
}

Int read_box(Box box) {
    Fn<Int> read = { [ref! box] => box.value };
    read()
}

Int main() {
    Box box! = Box(value: 3);
    read_box(box) - 3
}
