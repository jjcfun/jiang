// check-fail
// expected: callee_not_function

struct Counter {
    Int value;

    Int value(self, Int offset) {
        self.value + offset
    }
}

Int main() {
    Counter counter = Counter(value: 4);
    counter.value(1)(2)
}
