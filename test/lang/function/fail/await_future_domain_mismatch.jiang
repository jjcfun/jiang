// check-fail
// expected: await_future_domain_mismatch

struct PageDomain: SerialDomain {}

struct OtherDomain: SerialDomain {}

const PageDomain page = PageDomain();

const OtherDomain other = OtherDomain();

async [other] Int load_other() {
    1
}

async [page] Int main() {
    Future<Int> value = load_other$().async();
    await value
}
