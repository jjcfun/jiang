// check-fail
// expected: unique_requirement_not_declared

struct Box {
    Int value!;
}

Int read_box(Box& box) {
    Fn<Int> read = () [unique ref _ borrowed = box.value] => borrowed$.get();
    read()
}

Int main() {
    Box box! = Box(value: 3);
    read_box(box$.ref()) - 3
}
