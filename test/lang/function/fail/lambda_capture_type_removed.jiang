// check-fail
// expected: capture_declaration_removed
// capture list 不声明局部变量类型，只选择已有的同名 local。

Int invalid_typed_capture() {
    Int value = 1;
    Fn<Int> read = { [Int value] => value };
    read()
}
