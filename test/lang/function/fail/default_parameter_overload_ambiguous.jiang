// check-fail
// expected: ambiguous_default_parameter_overload
// `value(1)` 同时匹配两个重载，因此定义阶段直接拒绝。

Int value(Int number) {
    number
}

Int value(Int number, Int fallback = 12) {
    number + fallback
}

Int main() {
    return value(1);
}
