// run-pass
// expected-exit: 8

async Int leaf(Int value) {
    value + 1
}

async Int parent(Int value) {
    Int nested = leaf(value);
    nested + 2
}

Int main() {
    Future<Int> value = parent$(5).async();
    await value
}
