import std;

Int main() {
    std.String text = "hello";
    Fn<Int> read = { => text.byte_length() };
    if (read() != 5) {
        return 1;
    }
    if (text.byte_length() != 5) {
        return 2;
    }
    return 0;
}
