// run-pass
// expected-exit: 0

struct Resource: Movable {
    Int value;
    Int*! drops;

    public init(self, Int value, Int&! drops) {
        self.value = value;
        self.drops = unsafe { drops$.mut_ptr() };
    }

    deinit(self) {
        unsafe {
            self.drops$.set(self.drops$.get() + self.value);
        }
    }
}

Int main() {
    Int drops! = 0;
    {
        Resource resource! = Resource(7, drops$.mut_ref());
        Fn<Int> reader = { [resource] => resource.value };
        if (reader() != 7) {
            return 100;
        }
    }
    if (drops != 7) {
        return drops;
    }
    return 0;
}
