// run-pass
// expected-exit: 0

Int main() {
    Int count! = 0;
    Fn<Int> inc = { [ref! count] =>
        count$.set(count$.get() + 1);
        count$.get()
    };
    return inc() * 10 + inc() - 12;
}
