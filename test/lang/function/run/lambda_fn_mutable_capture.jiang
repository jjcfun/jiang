Int main() {
    Int count! = 1;
    Fn<Int> inc = { [ref! count] =>
        count$.set(count$.get() + 1);
        count$.get()
    };
    if (inc() != 2) {
        return 1;
    }
    if (inc() != 3) {
        return 2;
    }
    return count - 3;
}
