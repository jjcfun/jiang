// run-pass
// expected-exit: 10
// 命名参数可以乱序；type check 会在 side table 中恢复声明顺序。

Int value(Int first, Int second = 2, Int third = 3) {
    first + second + third
}

Int main() {
    return value(third: 5, first: 3);
}
