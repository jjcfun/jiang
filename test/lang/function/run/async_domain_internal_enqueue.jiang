// run-pass
// expected-exit: 6

struct WorkerDomain: Domain<kind = .concurrent> {}

async [WorkerDomain] Int load() {
    5
}

async [WorkerDomain] Int main() {
    Future<Int> value = async [WorkerDomain] load();
    value.await() + 1
}
