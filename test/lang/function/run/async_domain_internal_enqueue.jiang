// run-pass
// expected-exit: 6

struct WorkerDomain: Domain<kind = .concurrent> {}

async [WorkerDomain] Int load() {
    5
}

Int main() {
    Future<Int> value = load$().async();
    await value + 1
}
