// owned closure 可以在捕获来源存活期间持有并移动共享借用。
// run-pass
// expected-exit: 0

Int main() {
    Int value = 41;
    Fn<Int>^ callback = new { => value + 1 };
    Fn<Int>^ moved = callback;
    return moved$.ref()() - 42;
}
