enum Direction {
    left,
    right,
}

Int main() {
    Direction direction = .right;
    Fn<Bool> is_right = () => direction == .right;
    if (is_right()) {
        return 0;
    }
    return 1;
}
