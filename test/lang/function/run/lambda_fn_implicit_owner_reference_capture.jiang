// run-pass
// expected-exit: 0
// owner local 的隐式捕获只借用 binding，不转移 owner。

Int main() {
    Int^ box = new 7;
    Fn<Int> read = { => box$.get() };
    Int captured = read();
    return box$.get() - captured;
}
