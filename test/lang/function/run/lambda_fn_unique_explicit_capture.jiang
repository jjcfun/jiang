// run-pass
// expected-exit: 0

struct Box {
    Int value!;
}

Int main() {
    Box box! = Box(value: 9);
    Fn<Int> read = () [unique ref _ borrowed = box.value] => borrowed$.get();
    return read() - 9;
}
