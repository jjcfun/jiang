Int main() {
    Int base = 10;
    Fn<Int, Int> implicit_add = (value) => value + base;
    Fn<Int, Int> explicit_add = (value) [snapshot = base] => value + snapshot;
    return implicit_add(5) + explicit_add(7) - 32;
}
