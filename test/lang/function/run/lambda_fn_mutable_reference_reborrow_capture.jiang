// run-pass
// expected-exit: 0
// 已有 T&! 的 ref! capture 是幂等 reborrow，不形成 T&!&!。

Int main() {
    Int value! = 7;
    Int&! borrowed = value$.mut_ref();
    Fn<Int> update = { [ref! borrowed] =>
        borrowed$.set(13);
        return borrowed$.get();
    };
    return update() + value - 26;
}
