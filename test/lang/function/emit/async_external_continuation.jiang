extern async Int delayed_add(Int value);

async Int compute_external(Int value) {
    Future<Int> pending = delayed_add$(value).async();
    await pending + 2
}

Int main() {
    Future<Int> value = compute_external$(5).async();
    await value
}
