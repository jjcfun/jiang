// 验证安全引用参数的已证明事实能够进入 LLVM function declaration。

@link_symbol("__jiang_attribute_probe")
public Int attribute_probe(Int&! value) {
    return value$.get();
}

public Int main() {
    Int value! = 7;
    return attribute_probe(value$.mut_ref()) - 7;
}
