// check-pass
// init 参数和普通函数一样支持命名参数、默认参数和重排。

struct Point {
    Int x;
    Int y;

    init(Int x, Int y = 2) {
        self.x = x;
        self.y = y;
    }
}

Point make_point() {
    Point(y: 4, x: 1)
}

Point make_default_y() {
    Point(x: 3)
}

Int main() {
    0
}
