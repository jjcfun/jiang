// check-pass
// 覆盖函数声明、block tail expr、成员访问、下标和普通比较表达式。

Bool less(Int lhs, Int rhs) {
    lhs < rhs
}

Int len(UInt8[] text) {
    text.length
}

UInt8 at(UInt8[] text, Int index) {
    text[index]
}

UInt8 at_return(UInt8[] text, Int index) {
    return text[index];
}
