struct Box {
    Int value!;
}

Int read_box(unique Box& box) {
    Fn<Int> read = () [unique ref _ borrowed = box.value] => borrowed$.get();
    read()
}

Int main() {
    Box box! = Box(value: 3);
    read_box(box$.ref()) - 3
}
