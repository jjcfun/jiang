// check-pass

struct ImmediateDomain: Domain<Kind = .serial>, DomainExecutor {
    () enqueue(UInt8* context, RawFn<(), UInt8*> resume) {
        resume(context);
    }
}

async [ImmediateDomain] Int load_immediate() {
    1
}

Int main() {
    0
}
