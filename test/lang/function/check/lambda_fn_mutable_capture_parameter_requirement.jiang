struct Box {
    Int value!;
}

Int read_box(Box&! box) {
    Fn<Int> read = { [ref! _ borrowed = box.value] => borrowed$.get() };
    read()
}

Int main() {
    Box box! = Box(value: 3);
    read_box(box$.mut_ref()) - 3
}
