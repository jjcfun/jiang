// run-pass
// expected-exit: 42

Int main() {
    Mutex<Int> value = Mutex<Int>(0);
    Int updated = value.with_lock({ current =>
        current$.set(42);
        current$.get()
    });
    Int current = value.with_lock({ locked => locked$.get() });
    current + updated - 42
}
