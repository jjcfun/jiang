// check-fail
// test-package
// expected: cross_package_file_import

#doc(module) 原生包必须拒绝通过相对路径导入包边界外的源码。

import util = "../../check/source_dependency_util/util.jiang";

Int read_dependency() {
    util.answer()
}
