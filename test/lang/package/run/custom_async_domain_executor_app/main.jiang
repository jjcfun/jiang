// run-pass
// expected-exit: 0
// 跨 package 的自定义 Domain 接口可以恢复 Executor descriptor 并执行任务。

import domain_lib;

Int main() {
    Int value = sync [domain_lib.library_domain] {
        domain_lib.load_from_library_executor()
    };
    value - 33
}
