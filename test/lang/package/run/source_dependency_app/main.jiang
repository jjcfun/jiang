// expected-exit: 52
// test-package

#doc(module) 通过原生包入口加载多层依赖，运行时仅选择宿主包的 main。

import util;

Int main() {
    // dependency package 也可以有自己的 language main；runtime entry
    // 只能选择 root package root module 中的 main。
    util.answer() + util.exported_value() + util.extra.value() + util.ExportedBox(value: 3).value + util.base_answer()
}
