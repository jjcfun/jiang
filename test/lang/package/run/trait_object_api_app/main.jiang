// expected-exit: 0

import lib;

Int main() {
    lib.Value.Any any = lib.make_any();
    lib.Value.VTable vtable = lib.Value$.vtable(lib.Box);
    lib.Box box = lib.Box(data: 8);
    lib.Value.Receiver receiver = lib.Value$.receiver(box);
    any.value() + vtable.value(receiver) - 15
}
