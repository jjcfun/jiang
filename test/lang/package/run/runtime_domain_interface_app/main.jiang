// run-pass
// expected-exit: 0

import domain_lib;

Int main() {
    domain_lib.InterfaceDomain domain = domain_lib.make_runtime_domain(7);
    Int value = coroutine.sync(domain$.ref()) { 42 };
    if (domain.marker != 7) { return 1; }
    if (value != 42) { return 2; }
    0
}
