// run-pass
// expected-exit: 0
// 验证依赖 package 的 payload enum schema 可用于构造、pattern 和泛型实例化。

import dep;

Int main() {
    dep.Value<Int> local = .one(40);
    if (dep.read_value(local) != 40) { return 1; }
    dep.Value<Int> loaded = dep.make_value(41);
    if (dep.read_value(loaded) != 42) { return 2; }
    return 0;
}
