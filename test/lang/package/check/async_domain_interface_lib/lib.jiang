public struct PageDomain: Domain {
    associated Kind = .serial;
}

public struct OtherDomain: Domain {
    associated Kind = .serial;
}

public const PageDomain page = PageDomain();

public const OtherDomain other = OtherDomain();

public async [page] Int load_page() {
    11
}

public async [other] Int load_other() {
    22
}

public RawFn<async [page] Int> page_loader() {
    load_page
}

public () accept_page_loader(RawFn<async [page] Int> loader) {
}
