// check-pass

import util;

Int read_dependency() {
    util.answer() + util.exported_value() + util.extra.value() + util.base_answer() + util.cstring_length("hello")
}

Int read_exported_type() {
    util.ExportedBox(value: 3).value
}
