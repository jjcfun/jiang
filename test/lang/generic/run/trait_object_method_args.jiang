// run-pass

trait Add {
    Int add(Int value);
}

struct Offset: Add {
    Int base;

    Int add(Int value) {
        self.base + value
    }
}

Int main() {
    Offset offset = Offset(base: 30);
    Add.Any& any = Add$.ref(offset);
    any.add(12) - 42
}
