// run-pass

trait Add {
    Int add(self, Int value);
}

struct Offset: Add {
    Int base;

    Int add(self, Int value) {
        self.base + value
    }
}

Int main() {
    Offset offset = Offset(base: 30);
    Add.Any& any = Add$.ref(offset);
    any.add(12) - 42
}
