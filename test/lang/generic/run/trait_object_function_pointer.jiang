trait Value {
    Int value();
}

struct Box: Value {
    Int data;

    Int value() {
        self.data + 1
    }
}

Int main() {
    Box box = Box(data: 40);
    Value.Any& any = Value$.ref(box);
    Fn<Int, Value.Receiver> value = any.value;
    return value(any.receiver) - 41;
}
