// run-pass

trait Value {
    Int value();
}

struct Box: Value {
    Int data;

    Int value(self) {
        self.data
    }
}

Int main() {
    Box box = Box(data: 41);
    Value.Any& any = Value$.ref(box);
    any.value() - 41
}
