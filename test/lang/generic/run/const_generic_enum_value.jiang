// run-pass

enum LocalKind {
    first,
    second,
}

Int value<K: const LocalKind>() {
    if (K == .second) { return 2; }
    return 1;
}

Int main() {
    if (value<LocalKind.first>() != 1) { return 1; }
    if (value<LocalKind.second>() != 2) { return 2; }
    return 0;
}
