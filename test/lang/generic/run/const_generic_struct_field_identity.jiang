// run-pass
// expected-exit: 0

struct Fixed<T, const Int N> {
    T[N] values;
}

Int main() {
    Fixed<Int, 3> fixed = Fixed<Int, 3>(values: [3, 30, 300]);
    if (fixed.values[0] != 3) {
        return 1;
    }
    return 0;
}
