// expected-exit: 0

trait Value {
    Int value(self);
}

struct Box: Value {
    Int data;

    Int value(self) {
        self.data
    }
}

Int read(Value.Any& any) {
    any.value()
}

Int main() {
    Box box = Box(data: 41);
    Value.Any& any = Value$.ref(box);
    read(any) - 41
}
