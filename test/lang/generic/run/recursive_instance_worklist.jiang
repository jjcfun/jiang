// 递归泛型调用由 concrete 引用闭包收口，instance query 不递归求值自身。
// expected-exit: 0

Int countdown<T>(T value, Int remaining) {
    if (remaining == 0) {
        return 0;
    }
    return countdown<T>(value, remaining - 1);
}

Int main() {
    return countdown<Int>(0, 32);
}
