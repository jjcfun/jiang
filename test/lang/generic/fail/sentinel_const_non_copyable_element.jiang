// check-fail
// expected: generic_constraint_not_satisfied
// Sentinel 元素必须 Copyable；默认 Movable 的 nominal 并不满足该约束。

struct Resource {
    Int value;
}

Int use_non_copyable_sentinel<R: const Resource>(Resource[:R]& values) {
    0
}
