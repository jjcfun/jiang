// check-fail
// expected: generic_constraint_not_satisfied
#doc(module)
    宿主管理的语法列表不接受任意用户数据类型，避免跨 ABI 复制拥有资源的值。
#end
import std;
Void invalid(std.jiang.syntax.Parser<std.jiang.TokenKind>& parser) {
    _ values = parser.list<Int>();
}
