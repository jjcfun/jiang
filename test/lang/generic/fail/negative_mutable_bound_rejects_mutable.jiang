// expected: generic_constraint_not_satisfied

@where(T: !Mutable)
T identity<T>(T value) {
    value
}

Int main() {
    Int value! = 1;
    Int*! pointer = unsafe { value$.mut_ptr() };
    Int*! result = identity<Int*!>(pointer);
    0
}
