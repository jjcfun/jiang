#doc(module)
    条件 extension 的 Copyable 约束不得使原 nominal 的无约束参数变为可复制。
#end
// check-fail
// expected: projection_move_not_allowed

struct Holder<T> {
    T value;
    T get(self) { self.value }
}

extend <T: Copyable> Holder<T> {
    T copy_value(self) { self.value }
}
