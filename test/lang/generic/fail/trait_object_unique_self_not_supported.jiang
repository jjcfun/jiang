// check-fail
// expected: trait_object_restricted_receiver_not_supported

trait Reader {
    Int read(unique self);
}

struct Box {
    Int value;
}

extend Box: Reader {
    Int read(unique self) {
        self.value
    }
}

Int main() {
    Box box = Box(value: 1);
    Reader.Any& any = Reader$.ref(box);
    0
}
