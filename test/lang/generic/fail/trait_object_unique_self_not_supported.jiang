// check-fail
// expected: trait_object_restricted_receiver_not_supported

trait Reader {
    @unique(self)
    Int read();
}

struct Box {
    Int value;
}

extend Box: Reader {
    @unique(self)
    Int read() {
        self.value
    }
}

Int main() {
    Box box = Box(value: 1);
    Reader.Any& any = Reader$.ref(box);
    0
}
