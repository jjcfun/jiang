// fail
// expected: trait_object_restricted_receiver_not_supported

trait Consume {
    Int consume(Self self);
}

struct Box: Consume, Movable {
    Int data;

    Int consume(Self self) {
        self.data
    }
}

Int main() {
    Box box = Box(data: 1);
    Consume.Any& any = Consume$.ref(box);
    0
}
