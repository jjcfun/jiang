// fail
// expected: trait_object_move_receiver_not_supported

trait Consume {
    @self(move)
    Int consume();
}

struct Box: Consume, Movable {
    Int data;

    @self(move)
    Int consume() {
        self.data
    }
}

Int main() {
    Consume.Any any = Consume$.any(Box(data: 1));
    0
}
