// check-fail
// expected: generic_constraint_not_satisfied

enum LocalKind {
    serial,
    concurrent,
}

@where(K == .serial)
struct SerialOnly<K: const LocalKind> {}

Int use(SerialOnly<LocalKind.concurrent> value) {
    0
}
