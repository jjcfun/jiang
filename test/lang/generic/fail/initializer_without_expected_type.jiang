// 验证省略泛型实参的 initializer 必须具有 expected type。
// check-fail
// expected: generic_initializer_requires_context

struct Box<T> {
    T value;

    public init(self, T value) {
        self.value = value;
    }
}

Int main() {
    _ value = Box(1);
    value.value
}
