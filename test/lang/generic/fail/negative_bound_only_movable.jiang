// check-fail
// expected: unsupported_negative_trait_bound
// 当前负 trait bound 只允许用于 Movable，避免泛型约束表达过早泛化。

trait Printable;

@where(T: !Printable)
Int reject_negative_non_movable<T>(T value) {
    0
}
