#doc(module)
    extension 参数的负 Movable 约束同样参与方法体的所有权检查。
#end
// check-fail
// expected: non_movable_value_transfer

struct Holder<T> { T value; }

extend <T: !Movable> Holder<T> {
    T transfer(self, T value) { value }
}
