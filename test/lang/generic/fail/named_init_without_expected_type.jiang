// check-fail
// expected: generic_initializer_requires_context

struct Box<T> {
    T value;

    public init make(self, T value) {
        self.value = value;
    }
}

Int main() {
    _ value = Box.make(1);
    value.value
}
