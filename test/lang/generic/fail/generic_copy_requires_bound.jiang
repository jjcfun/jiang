// check-fail
// expected: use_after_move
// 无 Copyable 约束的泛型值会被隐式移动，源值随后不可再用。

T copy_value<T>(T value) {
    T copy = value;
    value
}

Int main() {
    Fn<Int> value = { => 1 };
    Fn<Int> moved = copy_value<Fn<Int>>(value);
    moved()
}
