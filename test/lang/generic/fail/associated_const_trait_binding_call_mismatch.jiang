// check-fail
// expected: generic_constraint_not_satisfied

enum LocalDomainKind {
    serial,
    concurrent,
}

trait LocalDomain {
    associated kind: const LocalDomainKind;
}

struct WorkerDomain: LocalDomain<kind = .concurrent> {}

@where(T: LocalDomain<kind = .serial>)
Int accepts_serial<T>(T value) {
    1
}

Int main() {
    accepts_serial(WorkerDomain())
}
