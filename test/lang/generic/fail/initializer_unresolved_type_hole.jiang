// 验证 initializer 未提供信息时不会猜测泛型 inference hole。
// check-fail
// expected: partial_generic_inference_failed

struct Phantom<T> {
    Int value;

    public init(self, Int value) {
        self.value = value;
    }
}

Int main() {
    Phantom<_> value = Phantom(1);
    value.value
}
