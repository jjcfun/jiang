// check-pass
// 覆盖显式泛型调用中的嵌套 `>>` 关闭，以及普通 shift-right 操作符路径。

@region(a)
struct Box<T> {
    @life(a)
    T value;
}

Int accept_type<T>() {
    0
}

Int pass_nested() {
    accept_type<Box<Box<Int>>>()
}

Int shift_right(Int value) {
    value >> 1
}
