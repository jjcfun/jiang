// check-pass

Int named_len<T: Copyable, S: const T>(T[:S]& text) {
    text.length
}

Int layered_len<T: Copyable, S: const T>(T[:S]& text) {
    text.length
}

Int main() {
    UInt8[:0]& text = "hi";
    named_len<UInt8, 0>(text) + layered_len<UInt8, 0>(text)
}
