// check-pass

enum Mode {
    stop,
    go,
}

struct Point {
    Int x;
    Int y;
}

const Mode stop_mode = Mode.stop;
Int bool_sentinel(Reference<SentinelSlice<Bool, true>> values) {
    values.length
}

Int char_sentinel(Reference<SentinelSlice<Char, '\n'>> values) {
    values.length
}

Int enum_sentinel(Reference<SentinelSlice<Mode, stop_mode>> values) {
    values.length
}

Int struct_sentinel<const Point P>(Reference<SentinelSlice<Point, P>> values) {
    values.length
}

Int sentinel_pointer(SentinelPointer<Char, '\0'> pointer) {
    0
}

Int sentinel_array(SentinelArray<Bool, 2, false> values) {
    2
}
