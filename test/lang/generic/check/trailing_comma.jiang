// check-pass
// 覆盖 generic params、type args 和函数参数列表允许 trailing comma。

struct Box<T,> {
    T value;
}

Int value(Int first, Int second,) {
    first + second
}

Int call() {
    value(1, 2,)
}

Int use_type(Box<Int,> box) {
    box.value
}
