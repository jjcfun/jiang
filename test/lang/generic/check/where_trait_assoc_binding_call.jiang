// check-pass

trait Iterable {
    associated Item;
}

struct IntBox {}

extend IntBox: Iterable {
    associated Item = Int;
}

@where(T: Iterable<Item = Int>)
Int read<T>(T item) {
    1
}

Int main() {
    read(IntBox())
}
