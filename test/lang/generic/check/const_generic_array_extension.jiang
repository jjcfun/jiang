// check-pass

extend Array<T, N> {
    public static Int array_marker() {
        3
    }
}

extend SentinelArray<T, N, S> {
    public static Int sentinel_array_marker() {
        4
    }
}

Int main() {
    UInt8[3].array_marker() + UInt8[3:0].sentinel_array_marker() - 7
}
