// check

trait Reader {
    Int read(Self&! self);
}

struct Box {
    Int value;
}

extend Box: Reader {
    Int read(Self&! self) {
        self.value
    }
}

Int main() {
    Box box! = Box(value: 1);
    Reader.Any&! any = Reader$.mut_ref(box);
    any.read()
}
