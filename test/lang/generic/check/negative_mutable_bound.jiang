// check-pass

@where(T: !Mutable)
Int accept<T>(T value) {
    1
}

@where(T: !Mutable)
struct Boxed<T> {
    T value;
}

Int main() {
    Boxed<Int> boxed = Boxed<Int>(value: 1);
    accept(boxed.value)
}
