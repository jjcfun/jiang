// check-pass

enum LocalKind {
    serial,
    concurrent,
}

@where(K == .serial)
struct SerialOnly<K: const LocalKind> {}

@where(K == LocalKind.concurrent)
struct ConcurrentOnly<K: const LocalKind> {}

Int main() {
    0
}
