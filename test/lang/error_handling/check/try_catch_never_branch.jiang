// check-pass
// catch body 可以直接 return；Never 分支应和 success value 合并。

Int@Int parse(Bool fail) {
    if fail {
        throw 1;
    } else {
        2
    }
}

Int recover(Bool fail) {
    try parse(fail) catch Int err {
        return err;
    }
}
