// check-fail
// expected: unresolved_member_for_type
// Intrinsic operation declarations only serve `$`; ordinary member lookup must not see them.

Int^ move_as_member(Int^ owner) {
    owner.move()
}
