// check-fail
// expected: unsupported_implicit_operation
// $.forget() 不能移动 !Movable receiver。

struct Pinned: !Movable {
    Int value;
}

Void forget_pinned(Pinned value) {
    unsafe {
        value$.forget();
    }
}
