// check-fail
// expected: unsupported_implicit_operation
// $.dealloc() 只由内部 intrinsic 表中的 pointer receiver 提供。

() dealloc_plain_value(Int value) {
    do [unsafe] {
        value$.dealloc();
    }
}
