// check-pass

@where(T == UInt8[])
extend Box<T> {
    public static Int plain_marker() {
        3
    }
}

@where(T == UInt8[:0])
extend Box<T> {
    public static Int sentinel_marker() {
        4
    }
}

Int main() {
    UInt8[]^.plain_marker() + UInt8[:0]^.sentinel_marker() - 7
}
