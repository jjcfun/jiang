// check-pass
// Void is the preferred type spelling for unit. The unit value remains ().

Void complete() {
    return ();
}

Void* erase(UInt8* pointer) {
    unsafe {
        pointer$.as(Void*)
    }
}

Int main() {
    complete();
    0
}
