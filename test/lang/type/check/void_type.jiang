// check-pass
// Void is the type spelling. Its only value is ().

Void complete() {
    return ();
}

Void* erase(UInt8* pointer) {
    unsafe {
        pointer$.as(Void*)
    }
}

Int main() {
    complete();
    0
}
