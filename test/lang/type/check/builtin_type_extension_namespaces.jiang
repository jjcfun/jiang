// check-pass

extend Bool {
    public static Int marker() { 1 }
}

extend Option<T> {
    public static Int marker() { 2 }
}

extend Result<T, E> {
    public static Int marker() { 3 }
}

extend Reference<T> {
    public static Int marker() { 4 }
}

extend Box<T> {
    public static Int marker() { 5 }
}

extend RawPointer<T> {
    public static Int marker() { 6 }
}

extend ManyPointer<T> {
    public static Int marker() { 7 }
}

extend SentinelPointer<T, S> {
    public static Int marker() { 8 }
}

extend Slice<T> {
    public static Int marker() { 9 }
}

extend SentinelSlice<T, S> {
    public static Int marker() { 10 }
}

extend Array<T, N> {
    public static Int marker() { 11 }
}

extend SentinelArray<T, N, S> {
    public static Int marker() { 12 }
}

Int main() {
    Bool.marker()
        + Option<Int>.marker()
        + Result<Int, Int>.marker()
        + Reference<Int>.marker()
        + Box<Int>.marker()
        + RawPointer<Int>.marker()
        + ManyPointer<Int>.marker()
        + SentinelPointer<UInt8, 0>.marker()
        + Slice<UInt8>.marker()
        + SentinelSlice<UInt8, 0>.marker()
        + Array<UInt8, 3>.marker()
        + SentinelArray<UInt8, 3, 0>.marker()
        - 78
}
