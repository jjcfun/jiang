// check-pass
// 裸指针转换需要 unsafe；显式 get/set 在语法与类型层允许。

Int read_raw(Int!* ptr) {
    ptr$.set(7);
    ptr$.get()
}

Int!* int_ptr(Int address) {
    do [unsafe] {
        address$.as(Int!*)
    }
}

Int main() {
    0
}
