// check-pass
// 覆盖 slice expression 的三种边界形式；具体代码生成另由 MIR/backend 测试覆盖。

UInt from_start(UInt8[] text) {
    UInt8[] part = text[1..];
    part.length
}

UInt to_end(UInt8[] text) {
    UInt8[] part = text[..2];
    part.length
}

UInt range(UInt8[] text) {
    UInt8[] part = text[1..2];
    part.length
}
