// check-pass

unsafe Int unsafe_value() {
    1
}

async Int async_value() {
    2
}

Int call_unsafe(Fn<unsafe Int> callback) {
    unsafe {
        callback()
    }
}

async Int call_async(Fn<async Int> callback) {
    callback()
}

() accept_unsafe_callback_list(Fn<unsafe Bool>[]& callbacks) {
}

() accept_async_callback_list(Fn<async Bool>[]& callbacks) {
}

Int main() {
    unsafe {
        call_unsafe(unsafe_value)
    }
}
