// check-pass

unsafe Int unsafe_value() {
    1
}

async Int async_value() {
    2
}

Int call_unsafe(RawFn<unsafe Int> callback) {
    do [unsafe] {
        callback()
    }
}

async Int call_async(RawFn<async Int> callback) {
    callback()
}

() accept_unsafe_callback_list(RawFn<unsafe Bool>[]& callbacks) {
}

() accept_async_callback_list(RawFn<async Bool>[]& callbacks) {
}

Int main() {
    do [unsafe] {
        call_unsafe(unsafe_value)
    }
}
