// check-pass

extern {
    Int strlen(UInt8[:0]& text);
}

Int length(UInt8[:0]& text) {
    do [unsafe] {
        strlen(text)
    }
}

Int main() {
    UInt8[:0]& borrowed = "hello";
    return length(borrowed);
}
