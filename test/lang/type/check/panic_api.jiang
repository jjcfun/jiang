// check-pass

import std;

RawFn<Void, UInt8[]&> global_panic() {
    return panic;
}

RawFn<Void, UInt8[]&> qualified_panic() {
    return std.panic;
}

Int main() {
    RawFn<Void, UInt8[]&> first = global_panic();
    RawFn<Void, UInt8[]&> second = qualified_panic();
    return 0;
}
