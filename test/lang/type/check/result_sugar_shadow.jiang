// check-pass

struct Result<T, E> {
    T value;
    E error;
}

Int@Int parse(Bool fail) {
    if fail {
        throw 7;
    } else {
        3
    }
}

Int main() {
    Result<Int, Int> local = Result<Int, Int>(value: 2, error: 0);
    (try parse(false) catch (Int err) => err) + local.value
}
