// check-pass
// SentinelPointer<T, S> 和普通 ManyPointer 一样支持低层 `$` intrinsic。

() free_sentinel_pointer(UInt8[*:0] ptr) {
    ptr$.free();
}

() free_optional_sentinel_pointer(UInt8[*:0]? ptr) {
    ptr?$.free();
}

Int main() {
    0
}
