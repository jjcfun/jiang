// expected: ok

Int byte_len(String& value) {
    value.byte_length()
}

Int vector_len() {
    Vector<Int>! values = Vector<Int>();
    values.append(1);
    values.append(2);
    values.length()
}

Int main() {
    String text = String("hello");
    byte_len(text$.ref()) + vector_len()
}
