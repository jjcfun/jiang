// check-pass

@region(a)
struct Text {
    @life(a)
    UInt8[]& bytes;
}

Int length(UInt8[]& text) {
    text.length
}

Int main() {
    UInt8[]& text = "hello";
    Text value = Text(bytes: text);
    length(value.bytes)
}
