// check-pass

extern {
    Int strlen(CString& text);
}

Int length(CString& text) {
    strlen(text)
}

Int main() {
    CString& borrowed = "hello";
    CString* raw = null;
    CString^ owned = null;
    return length(borrowed);
}
