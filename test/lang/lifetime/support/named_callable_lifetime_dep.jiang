@life(callback.result: callback.value, return: value)
public Int& apply_first(
    Fn<Int& result, Int& value, Int&> callback,
    Int& value,
    Int& fallback
) {
    return callback(value, fallback);
}
