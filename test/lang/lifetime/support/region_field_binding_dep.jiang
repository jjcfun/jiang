@region(left, right)
public struct Pair {
    @life(left)
    public Int& first;

    @life(right)
    public Int& second;
}

@region(a, b, b: a)
public struct Wrapper {
    @life(left: a, right: b)
    public Pair pair;
}
