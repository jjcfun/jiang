// expected-exit: 0

@life(value > return)
Int& identity(Int& value) {
    return value;
}

Int main() {
    RawFn<Int&, Int&> raw = identity;
    Fn<Int&, Int&> callable = Fn(raw);
    Int value = 42;
    Int& borrowed = value$.ref();
    return callable(borrowed)$.get() - 42;
}
