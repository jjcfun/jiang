// expected-exit: 0

Int main() {
    (Int, (Int, Int)) values! = (1, (2, 3));
    values[0] = 4;
    values[1][0] = 5;

    Int left = 7;
    Int right = 9;
    (Int&, Int&) borrowed = (left$.ref(), right$.ref());
    Int sum = values[0] + values[1][0] + values[1][1];
    return sum + borrowed[0]$.get() + borrowed[1]$.get() - 28;
}
