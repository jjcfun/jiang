// check-fail
// expected: borrowed_value_escape

import dep = "../support/field_path_contract_dep.jiang";

Int main() {
    Int stable = 42;
    dep.OrderedPairRef pair;
    {
        Int local = 7;
        pair = dep.OrderedPairRef(left: local$.ref(), right: stable$.ref());
    }
    return pair.right$.get();
}
