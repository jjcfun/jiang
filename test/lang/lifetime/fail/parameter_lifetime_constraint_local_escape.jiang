// check-fail
// expected: borrowed_value_escape

@life(left: right, return: left)
Int& select(Int& left, Int& right) {
    return right;
}

Int& escape(Int& outer) {
    Int local = 42;
    return select(outer, local$.ref());
}
