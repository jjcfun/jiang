// check-fail
// expected: return_lifetime_source_not_allowed

struct State: Movable {
    Int value;
}

State& borrow_state(Mutex<State>& state) {
    return state.with_lock({ value => value$.ref() });
}
