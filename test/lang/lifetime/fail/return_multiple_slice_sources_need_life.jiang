// check-fail
// expected: return_lifetime_needs_annotation
// 返回 slice 可能来自多个输入来源时，必须显式声明所有允许的 return lifetime。

UInt8[]& pick(UInt8[]& left, UInt8[]& right, Bool use_left) {
    if use_left {
        left[0..left.length]
    } else {
        right[0..right.length]
    }
}
