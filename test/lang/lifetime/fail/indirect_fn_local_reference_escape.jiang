// expected: borrowed_value_escape
// Fn 间接调用不能让指向局部变量的引用从当前函数逃逸。

@life(action.result: action.value)
Int& escape_local(Fn<Int& result, Int& value> action) {
    Int value = 1;
    return action(value$.ref());
}
