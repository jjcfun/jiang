// check-fail
// expected: stack_fn_escape

@region(a)
struct CallableField {
    @life(x: a)
    Fn<Int, Int& x> callback;
}

@life()
CallableField escape_capture() {
    Int local = 1;
    Fn<Int, Int& x> callback =
        (ignored) [ref _ captured = local] => captured$.get();
    return CallableField(callback: callback);
}
