// check-fail
// expected: lifetime_constraint_not_satisfied

@life(callback.left: callback.right)
() invoke_without_constraint(
    Fn<() result, Int& left, Int& right> callback,
    Int& left,
    Int& right
) {
    callback(left, right);
}
