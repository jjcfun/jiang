// check-fail
// expected: borrowed_value_escape

@life(return: (left, right))
(Int& x, Int& y) make_tuple(Int& left, Int& right) {
    return (left, right);
}

Int main() {
    Int stable = 1;
    (Int&, Int&) escaped;
    {
        Int local = 2;
        escaped = make_tuple(stable$.ref(), local$.ref());
    }
    return escaped[1]$.get();
}
