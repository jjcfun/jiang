// check-fail
// expected: return_lifetime_needs_annotation
// 含引用字段的第二参数经过局部聚合转发后，仍受默认 arg0 lifetime 契约限制。

struct BorrowedOptions {
    Int& value;
}

struct ParseLikeResult {
    BorrowedOptions options;
    Bool ok;
}

ParseLikeResult forward(Int& allowed, BorrowedOptions options) {
    BorrowedOptions forwarded = options;
    ParseLikeResult(options: forwarded, ok: true)
}
