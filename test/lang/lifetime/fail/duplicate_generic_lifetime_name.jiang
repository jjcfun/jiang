// check-fail
// expected: duplicate_generic_lifetime_name

struct Duo<A, B> {
    A first;
    B second;
}

@life(return: input.a)
Int& duplicate(Duo<Int& a, Int& a> input) {
    return input.first;
}
