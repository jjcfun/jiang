// expected: return_lifetime_source_not_allowed
// Fn 间接调用返回的引用必须继续携带实参的借用来源，不能绕过 @life 限制。

@life(action.value > action.result, allowed > return)
Int& select(Fn<Int& result, Int& value> action, Int& allowed, Int& hidden) {
    return action(hidden);
}
