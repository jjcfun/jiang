// expected: borrowed_value_escape

import std;

@life(return: source)
public (Int value)&? first_from_local(Int& source) {
    std.Vector<Int> values! = std.Vector<Int>();
    values.append(source$.get());
    return values.first();
}
