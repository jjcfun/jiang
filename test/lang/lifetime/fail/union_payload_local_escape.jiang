// check-fail
// expected: borrowed_value_escape

union Borrowed {
    Int& value;
    (Int&, Int&) pair;
    () none;
}

Borrowed escape_single() {
    Int local = 1;
    return Borrowed.value(local$.ref());
}
