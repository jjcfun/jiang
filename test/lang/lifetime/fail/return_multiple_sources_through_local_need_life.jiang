// check-fail
// expected: return_lifetime_needs_annotation
// 局部引用在不同分支来自多个输入来源时，返回前不能只保留第一个来源。

Int& pick(Int& left, Int& right, Bool use_left) {
    Int& selected;
    if use_left {
        selected = left;
    } else {
        selected = right;
    }
    selected
}
