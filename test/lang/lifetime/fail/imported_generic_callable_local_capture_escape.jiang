// check-fail
// expected: borrowed_value_escape

import dep = "../support/generic_callable_carried_dep.jiang";

@life()
Int& borrow_local() {
    Int local = 1;
    Int value! = 2;
    return dep.apply({ [ref local] ignored => local }, value$.mut_ref());
}
