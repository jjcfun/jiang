// check-fail
// expected: borrowed_value_escape
// 返回引用可能来自 self 或参数时，必须显式声明两个来源。

struct Cell {
    Int value;

    Int& choose_or(Bool use_self, Int& fallback) {
        if use_self {
            self.value$.ref()
        } else {
            fallback
        }
    }
}

