// check-fail
// expected: return_lifetime_source_not_allowed
// reference receiver 优先形成 self contract，函数体不能改为返回其他参数。

struct Cell {
    Int value;

    Int& choose_or(self, Bool use_self, Int& fallback) {
        if use_self {
            self.value$.ref()
        } else {
            fallback
        }
    }
}
