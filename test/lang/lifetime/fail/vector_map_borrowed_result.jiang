// expected: return_lifetime_source_not_allowed

import std;

Int main() {
    std.Vector<Int> values! = std.Vector<Int>();
    values.append(1);
    std.Vector<Int&> refs! = values.map<Int&>({ value => value });
    return refs.length();
}
