// check-fail
// expected: borrowed_value_escape

import dep = "../support/implicit_lifetime_dep.jiang";

@life()
Int& escape_imported_identity() {
    Int local = 1;
    return dep.identity(local$.ref());
}
