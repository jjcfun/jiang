// check-fail
// expected: borrowed_value_escape

struct Cell<T> {
    T value;
}

@life(return: input.a)
Int& borrow_value(Cell<Int& a> input) {
    input.value
}

@life()
Int& invalid() {
    Int local = 1;
    Cell<Int&> input = Cell<Int&>(value: local$.ref());
    return borrow_value(input);
}
