// check-fail
// expected: invalid_callable_lifetime_constraint

@life(left.value > right.result)
Int& apply(
    Fn<Int& result, Int& value> left,
    Fn<Int& result, Int& value> right,
    Int& value
) {
    return left(value);
}
