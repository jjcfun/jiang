// check-fail
// expected: borrowed_value_escape

@region(a, b, b: a)
struct PairRef {
    @life(a)
    Int& left;

    @life(b)
    Int& right;
}

Int main() {
    Int stable = 42;
    PairRef pair;
    {
        Int local = 7;
        pair = PairRef(left: local$.ref(), right: stable$.ref());
    }
    return pair.right$.get();
}
