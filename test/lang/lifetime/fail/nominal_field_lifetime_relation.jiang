// check-fail
// expected: borrowed_value_escape

@life(left > right)
struct PairRef {
    Int& left;
    Int& right;
}

Int main() {
    Int stable = 42;
    PairRef pair;
    {
        Int local = 7;
        pair = PairRef(left: local$.ref(), right: stable$.ref());
    }
    return pair.right$.get();
}
