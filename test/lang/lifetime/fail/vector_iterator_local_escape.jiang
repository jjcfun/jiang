// expected: borrowed_value_escape

import std;

@life(return.owner: source)
std.collection.SliceIterator<Int> escape_local_vector(Int& source) {
    std.Vector<Int> values! = std.Vector<Int>();
    values.append(1);
    return values.make_iterator();
}
