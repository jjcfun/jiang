// check-fail
// expected: implicit_lifetime_contract_requires_annotation

@region(a, b)
struct Pair {
    @life(a)
    Int& first;

    @life(b)
    Int& second;
}

struct Store {
    Pair get(self);
}
