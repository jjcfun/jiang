// check-fail
// expected: borrowed_value_escape
// 返回值的引用字段不能保存当前函数局部变量的引用。

@region(a)
struct Holder {
    @life(a)
    Int& value;
}

Holder make_holder_from_local() {
    Int value = 1;
    Holder(
        value: value$.ref(),
    )
}
