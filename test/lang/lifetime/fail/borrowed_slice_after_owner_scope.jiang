// check-fail
// expected: borrowed_value_escape
// 从 owner 借出的 slice 不能在 owner 离开作用域后继续使用。

import std;

Int main() {
    UInt8[]& bytes;
    {
        std.String text = "hello";
        bytes = text.bytes();
    }
    return Int(bytes[0]);
}
