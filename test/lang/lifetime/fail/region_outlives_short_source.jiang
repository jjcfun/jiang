// check-fail
// expected: borrowed_value_escape

@region(a, b: a)
struct Pair {
    @life(a)
    Int& first;

    @life(b)
    Int& second;
}

Int use_shorter_region() {
    Int stable = 42;
    Pair pair;
    {
        Int local = 7;
        pair = Pair(first: local$.ref(), second: stable$.ref());
    }
    return pair.second$.get();
}
