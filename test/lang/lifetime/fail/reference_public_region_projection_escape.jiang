// check-fail
// expected: borrowed_value_escape

@region(a, b)
struct Pair {
    @life(a)
    Int& left;

    @life(b)
    Int& right;
}

@life(return: input.a)
Int& select_left(Pair& input) {
    return input.left;
}

Int reference_public_region_projection_escape() {
    Int stable = 42;
    Int& escaped;
    {
        Pair input = Pair(left: stable$.ref(), right: stable$.ref());
        escaped = select_left(input$.ref());
    }
    return escaped$.get();
}
