// check-fail
// expected: borrowed_value_escape

@region(a, b)
struct Pair {
    @life(a)
    Int& left;

    @life(b)
    Int& right;
}

@life()
Pair& reference_public_region_projection_escape() {
    Int stable = 42;
    Pair input = Pair(left: stable$.ref(), right: stable$.ref());
    return input$.ref();
}
