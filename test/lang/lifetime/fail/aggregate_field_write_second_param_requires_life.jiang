// check-fail
// expected: implicit_lifetime_contract_requires_annotation
// 将第二参数写入聚合字段后返回，不能丢失该参数的 lifetime 来源。

@region(a)
struct BorrowedConfig {
    @life(a)
    Int& value;
}

BorrowedConfig replace_value(BorrowedConfig config, Int& value) {
    BorrowedConfig result! = config;
    result.value = value;
    result
}
