// check-fail
// expected: borrowed_value_escape

union Borrowed {
    Int& value;
    (Int&, Int&) pair;
}

Borrowed escape_pair(Int& stable) {
    Int local = 1;
    return Borrowed.pair(stable, local$.ref());
}
