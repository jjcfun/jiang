// check-fail
// expected: trait_method_signature_mismatch

trait Select {
    @life(return: value)
    Int& select(self, Int& value);
}

struct Stored: Select {
    Int value;

    @life(return: self)
    Int& select(self, Int& value) {
        return self.value$.ref();
    }
}
