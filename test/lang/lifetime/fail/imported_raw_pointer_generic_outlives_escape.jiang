// check-fail
// expected: borrowed_value_escape

import dep = "../support/structural_generic_dep.jiang";

@life()
unsafe dep.RawStorage<dep.Pair> invalid(Int& stable) {
    Int local = 1;
    return dep.store(stable, local$.ref());
}
