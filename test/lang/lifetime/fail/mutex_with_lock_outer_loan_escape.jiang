// check-fail
// expected: return_lifetime_source_not_allowed

struct State: Movable {
    Int value;
}

Int& borrow_value(Mutex<State>& state) {
    return state.with_lock({ value => value.value$.ref() });
}
