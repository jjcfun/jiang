// check-fail
// expected: borrowed_value_escape

import dep = "../support/structural_generic_dep.jiang";

@life()
dep.Pair invalid(Int& stable) {
    Int local = 1;
    dep.Pair pair = dep.Pair(left: stable, right: local$.ref());
    dep.Box<dep.Pair> input = dep.Box<dep.Pair>(value: pair);
    return dep.unwrap(input);
}
