// check-fail
// expected: borrowed_value_escape

@life(left > right)
struct Pair<T> {
    T left;
    T right;
}

Int main() {
    Int stable = 42;
    Pair<Int&> pair;
    {
        Int local = 7;
        pair = Pair<Int&>(left: local$.ref(), right: stable$.ref());
    }
    return pair.right$.get();
}
