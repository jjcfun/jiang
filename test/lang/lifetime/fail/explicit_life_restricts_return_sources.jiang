// check-fail
// expected: return_lifetime_source_not_allowed
// 显式 @life 出现后，返回引用只能来自标注允许的来源。

@life(left > return)
Int& pick(Int& left, Int& right, Bool use_left) {
    if use_left {
        left
    } else {
        right
    }
}
