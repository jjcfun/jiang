#doc(module)
    enum 构造中的 array 到 slice 转换不能使局部数组借用逃逸。
#end
// expected: borrowed_value_escape

@region(a)
enum Bytes {
    @life(a)
    data(UInt8[]&),
    empty,
}

@life()
Bytes escape() {
    UInt8[2] local = [3, 4];
    return Bytes.data(local);
}
