// check-fail: return_lifetime_source_not_allowed

struct State: Movable {
    Int value;

    @life(return: self)
    Int& borrow(self) {
        return self.value$.ref();
    }
}

Int& escape(Mutex<State>& mutex) {
    return mutex.with_lock({ value => value.borrow() });
}
