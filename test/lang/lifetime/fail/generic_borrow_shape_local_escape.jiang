// check-fail
// expected: borrowed_value_escape
// Box<Int&> 携带字段中的 loan，不能把局部值的引用带出函数。

struct Box<T> {
    T value;
}

Int& escape() {
    Int local = 1;
    Box<Int&> box = Box<Int&>(value: local$.ref());
    box.value
}
