// check-fail
// expected: lifetime_constraint_not_satisfied

import dep = "../support/parameter_constraint_dep.jiang";

() source_does_not_outlive_target(Int& outer) {
    Int local = 1;
    dep.relate(outer, local$.ref());
}
