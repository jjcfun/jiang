// check-fail
// expected: borrowed_value_escape

@life(return: left & right)
Int& choose(Bool first, Int& left, Int& right) {
    if (first) { return left; }
    return right;
}

Int& escape(Int& stable) {
    Int local = 1;
    return choose(true, stable, local$.ref());
}
