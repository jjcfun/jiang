// check-fail
// expected: implicit_lifetime_contract_requires_annotation
// Self self 与另一个非空 root 并存时不能享受 receiver 优先规则。

@region(a)
struct Holder {
    @life(a)
    Int& value;

    Holder choose(Self self, Holder other) {
        self
    }
}
