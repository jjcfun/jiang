// check-fail
// expected: generic_lifetime_name_requires_type_argument

struct Fixed<T, N: const Int> {
    T value;
}

@life(return: input.n)
Int& invalid(Fixed<Int&, 1 n> input) {
    return input.value;
}
