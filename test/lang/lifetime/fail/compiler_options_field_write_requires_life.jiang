// check-fail
// expected: return_lifetime_needs_annotation
// 写入 CompileOptions 的 slice 字段后返回，不能丢失第二参数来源。

import options = "../../../../src/driver/options.jiang";

options.CompileOptions configure(UInt8[]& allowed, UInt8[]& version) {
    options.CompileOptions result! = options.CompileOptions();
    result.compiler_version = version;
    result
}
