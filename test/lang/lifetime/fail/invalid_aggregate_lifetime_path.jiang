// check-fail
// expected: invalid_lifetime_name

struct PairRef {
    Int& left;
    Int& right;
}

@life(left > return.missing)
PairRef make_pair(Int& left, Int& right) {
    return PairRef(left: left, right: right);
}
