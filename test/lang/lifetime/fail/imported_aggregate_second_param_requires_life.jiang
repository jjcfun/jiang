// check-fail
// expected: return_lifetime_needs_annotation
// imported nominal 的引用字段也必须参与 borrow shape 和返回来源传播。

import dep = "../support/aggregate_borrow_dep.jiang";

dep.ImportedConfig forward(Int& allowed, dep.ImportedConfig config) {
    dep.ImportedConfig forwarded = config;
    forwarded
}
