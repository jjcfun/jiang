// check-fail
// expected: return_lifetime_source_not_allowed
// @life 约束的是参数携带的借用，不能延长按值参数自身栈槽的生命周期。

struct Plain {
    Int value;
}

@life()
Int& borrow_value_slot(Plain input) {
    input.value$.ref()
}
