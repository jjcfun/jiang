// check-fail
// expected: borrowed_value_escape
// 显式 @life 出现后，返回 slice 只能来自标注允许的来源。

@life(left > return)
UInt8[] pick(UInt8[] left, UInt8[] right, Bool use_left) {
    if use_left {
        left[0..left.length]
    } else {
        right[0..right.length]
    }
}
