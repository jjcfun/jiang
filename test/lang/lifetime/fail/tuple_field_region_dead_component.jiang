// check-fail
// expected: borrowed_value_escape

@region(a, b, b: a)
struct FlowTuple {
    @life(y: b, x: a)
    (Int, Int& x, Int& y) values;
}

Int dead_tuple_component() {
    Int stable = 42;
    FlowTuple value;
    {
        Int local = 7;
        value = FlowTuple(values: (0, stable$.ref(), local$.ref()));
    }
    return value.values[2]$.get();
}
