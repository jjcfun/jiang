// check
// Imported declarations recompute the same signature-based implicit contract.

import dep = "../support/implicit_lifetime_dep.jiang";

Int& imported_identity(Int& value) {
    return dep.identity(value);
}
