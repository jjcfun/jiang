// check-pass
// nominal instance method 可以使用 self lifetime，让返回引用受 self 约束。

struct Cell {
    Int value;

    @life(self > return)
    Int& borrow_value() {
        self.value$.ref()
    }
}

@life(cell > return)
Int& borrow_cell_value(Cell& cell) {
    cell.borrow_value()
}
