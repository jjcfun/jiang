@region(a, b, b: a)
struct PairRef {
    @life(a)
    Int& left;

    @life(b)
    Int& right;
}

Int main() {
    Int stable = 42;
    PairRef pair;
    {
        Int local = 7;
        pair = PairRef(left: stable$.ref(), right: local$.ref());
    }
    return pair.left$.get() - 42;
}
