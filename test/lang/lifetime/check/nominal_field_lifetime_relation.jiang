@life(left > right)
struct PairRef {
    Int& left;
    Int& right;
}

Int main() {
    Int stable = 42;
    PairRef pair;
    {
        Int local = 7;
        pair = PairRef(left: stable$.ref(), right: local$.ref());
    }
    return pair.left$.get() - 42;
}
