// check-pass
// 多个引用字段各自绑定公开 region，构造后可以安全返回包含这些引用的值。

@region(a, b)
struct PairRef {
    @life(a)
    Int& left;

    @life(b)
    Int& right;
}

@life(return: (left, right))
PairRef make_pair_ref(Int& left, Int& right) {
    PairRef(
        left: left,
        right: right,
    )
}
