// check-pass
// 字段引用是只读 view，不承诺字段值不变；写入同一个 live 字段后仍可继续读取。

struct Box {
    Int value!;
}

Int main() {
    Box box! = Box(value: 1);
    Int& borrowed = box.value$.ref();
    box.value = 2;
    return borrowed$.get();
}
