// check

struct Cell<T> {
    T value;
}

@life(return: input.a)
Int& borrow_value(Cell<Int& a> input) {
    input.value
}

Int read(Int& value) {
    Cell<Int&> input = Cell<Int&>(value: value);
    return borrow_value(input)$.get();
}
