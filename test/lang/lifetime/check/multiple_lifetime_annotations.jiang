// check-pass
// 覆盖同一个 callable 上多个 lifetime 约束，以及字段 region binding。

@region(a)
struct Holder {
    @life(a)
    Int& item;
}

@life(return: primary & fallback)
Int& choose_ref(Int& primary, Int& fallback);
