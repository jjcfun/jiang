// check-pass
// 覆盖同一个 callable 上多个 lifetime 约束，以及字段 self lifetime 约束。

struct Holder {
    @life(item > self)
    Int& item;
}

@life(primary > return, fallback > return)
Int& choose_ref(Int& primary, Int& fallback);
