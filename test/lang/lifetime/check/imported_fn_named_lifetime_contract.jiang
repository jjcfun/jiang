import dep = "../support/named_callable_lifetime_dep.jiang";

@life(value > return)
Int& call_imported(Int& value, Int& fallback) {
    Fn<Int& result, Int& value, Int&> callback = (item, other) => item;
    return dep.apply_first(callback, value, fallback);
}
