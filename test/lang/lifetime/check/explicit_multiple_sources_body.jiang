// check-pass
// 返回引用可能来自多个输入来源时，显式 @life 列出的来源都可以返回。

@life(left > return, right > return)
Int& pick(Int& left, Int& right, Bool use_left) {
    if use_left {
        left
    } else {
        right
    }
}

