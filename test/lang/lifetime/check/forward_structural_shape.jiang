@region(a)
struct View: Copyable {
    @life(a)
    Later[]& values;
}

struct Later: Copyable {
    Token token;
}

enum Token: Copyable {
    empty,
    value(Int),
}

Token token_for(Bool has_value) {
    if (has_value) { return .value(1); }
    return .empty();
}
