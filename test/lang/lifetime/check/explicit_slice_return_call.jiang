// check-pass
// 显式 @life 让函数调用返回的 slice 继续携带参数来源。

@life(input > return)
UInt8[]& tail(UInt8[]& input) {
    return input[1..input.length];
}

UInt8[]& through_call(UInt8[]& input) {
    return tail(input);
}

