// check-pass
// 方法返回值可能来自 self 或参数时，显式列出两个来源后可以通过。

struct Cell {
    Int value;

    @life(self > return, fallback > return)
    Int& choose_or(self, Bool use_self, Int& fallback) {
        if use_self {
            self.value$.ref()
        } else {
            fallback
        }
    }
}

