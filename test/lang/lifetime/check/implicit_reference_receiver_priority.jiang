// check-pass
// reference receiver 比其他非空输入 root 优先。

struct Cell {
    Int value;

    Int& read(self, Int& fallback) {
        self.value$.ref()
    }

    Int& read_mut(Self&! self, Int& fallback) {
        self.value$.ref()
    }
}
