// check-pass
// 外层声明的 slice local 只要实际使用点仍在 owner live range 内，就不应被误杀。

Int main() {
    UInt8[]& bytes;
    {
        String text = "hello";
        bytes = text.bytes();
        if (bytes[0] != UInt8(104)) {
            return 1;
        }
    }
    return 0;
}
