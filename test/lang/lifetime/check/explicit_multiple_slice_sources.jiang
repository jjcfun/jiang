// check-pass
// 多个 slice 参数都可能作为返回来源时，显式 @life 列出的来源都可以返回。

@life(left > return, right > return)
UInt8[]& pick(UInt8[]& left, UInt8[]& right, Bool use_left) {
    if use_left {
        left[0..left.length]
    } else {
        right[0..right.length]
    }
}

