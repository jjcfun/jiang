// check-pass
// Payload enum variant 与 union variant 共用逐槽 @life 和 nominal @region schema。

@region(a, b)
enum Borrowed {
    @life(a)
    single(Int&),

    @life(a, b)
    pair(Int&, Int&),

    @life(y: b, x: a)
    named_pair(Int& x, Int& y),

    none,
}
