// 显式 region schema 在 generic 实例上仍是固定的公开 positional shape。

@region(k, v)
struct Inner<K, V> {
    @life(k)
    K key;

    @life(v)
    V value;
}

@region(k, v)
struct Outer<K, V> {
    @life(k, v)
    Inner<K, V> inner;
}
