// check-pass
// 局部引用在不同分支来自多个输入来源时，显式 @life 列出的来源都可以返回。

@life(left > return, right > return)
Int& pick(Int& left, Int& right, Bool use_left) {
    Int& selected;
    if use_left {
        selected = left;
    } else {
        selected = right;
    }
    selected
}
