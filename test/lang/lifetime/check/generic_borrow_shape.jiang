// check-pass
// 泛型实例按实际类型参数计算 borrow shape；Box<Int> 不携带借用。

struct Box<T> {
    T value;
}

Box<Int> make_box(Int input) {
    Box<Int>(value: input)
}

@life(input > return)
Int& unwrap(Box<Int&> input) {
    input.value
}
