// check-pass
// 覆盖 @life 标注、字段 lifetime 约束和多个 annotation 的顺序。

@life(input > return)
Int& borrow_input(Int& input);

struct Buffer {
    @life(data > self)
    UInt8[]& data;
}

trait Reader;

@where(T: Reader)
@life(input > return)
Int read<T>(T input) {
    0
}
