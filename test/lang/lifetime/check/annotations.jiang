// check-pass
// 覆盖 @life 标注与字段 region binding。

@life(return: input)
Int& borrow_input(Int& input);

@region(a)
struct Buffer {
    @life(a)
    UInt8[]& data;
}

trait Reader;

@where(T: Reader)
Int read<T>(T input) {
    0
}
