// check

@region(a, b)
struct Pair {
    @life(a)
    Int& left;

    @life(b)
    Int& right;
}

struct Box<T> {
    T value;
}

struct Outer<T> {
    T value;
}

@life(return: input.inner.a)
Pair unwrap_nested(Outer<Box<Pair a> inner> input) {
    return input.value.value;
}
