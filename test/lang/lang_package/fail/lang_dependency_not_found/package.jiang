#doc(module) 语言依赖错误用例的原生宿主，保持普通依赖与 Lang 依赖同时登记。
#package {
    name = "lang_package_fail";
    root = "main.jiang";
    dependencies {
        util = "../../non_lang_dep";
        sql = "../../sql_lang_dep";
    }
}
