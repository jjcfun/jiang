// run-pass
// expected-exit: 2
// UInt8[:0]& 默认参数可以接收 NUL 结尾字符串字面量。

extern {
    Int strlen(UInt8[:0]& text);
}

Int length(UInt8[:0]& text = "hi") {
    unsafe {
        strlen(text)
    }
}

Int main() {
    return length();
}
