// 非零 allocation 返回 null 时必须立即 trap，不能把 null 当成可写 storage。
// run-pass
// expected-exit: trap

Int main() {
    unsafe {
        UInt8*! storage = UInt8$.alloc(9223372036854775807);
        storage$.dealloc();
    }
    return 0;
}
