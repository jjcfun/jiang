// run-pass
// expected-exit: 0

import std;

Int main() {
    std.Vector<Int> values! = std.Vector<Int>();
    Int total! = 0;

    for item in values {
        total = total + item$.get();
    }

    values.append(1);
    values.append(2);
    for item in values {
        total = total + item$.get();
    }

    return total - 3;
}
