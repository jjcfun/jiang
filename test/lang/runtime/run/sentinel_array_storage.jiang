// run-pass
// expected-exit: 0

Int main() {
    UInt8[5:0] text = [104, 101, 108, 108, 111];
    UInt8[:0]& slice = text;
    UInt8[*:0] ptr = slice$.ptr();
    UInt8[*] raw = ptr$.as(UInt8[*]);
    if (slice.length != 5) {
        return 1;
    }
    if (raw[0] != 104) {
        return 2;
    }
    if (raw[4] != 111) {
        return 3;
    }
    if (raw[5] != 0) {
        return 4;
    }
    return 0;
}
