// run-pass
// expected-exit: 5
// UInt8[:0]& 字符串字面量在语义上自带末尾 NUL，可以直接传给 C ABI。

extern {
    Int strlen(UInt8[:0]& text);
}

Int main() {
    UInt8[:0]& text = "hello";
    return do [unsafe] {
        strlen(text)
    };
}
