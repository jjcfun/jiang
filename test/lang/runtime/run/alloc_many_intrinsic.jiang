// run-pass
// expected-exit: 0

Int main() {
    UInt8![*] bytes = do [unsafe] {
        UInt8!$.alloc_many(3)
    };
    bytes[0] = UInt8(4);
    bytes[1] = UInt8(5);
    bytes[2] = UInt8(6);

    Int result = Int(bytes[0]) + Int(bytes[1]) + Int(bytes[2]) - 15;
    do [unsafe] {
        bytes$.dealloc();
    }
    return result;
}
