// StringBuilder 拒绝负数浮点 precision。
// run-pass
// expected-exit: trap

import std;

Int main() {
    StringBuilder builder! = StringBuilder();
    builder.append(Float64(1.0), precision: -1);
    return 0;
}
