// run-pass
// expected-exit: 0

Int main() {
    UInt8[*]! buffer! = unsafe {
        UInt8!$.alloc_many(4)
    };
    buffer[0] = 104;
    buffer[1] = 105;
    buffer[2] = 0;
    Int result! = 0;
    unsafe {
        UInt8[*]! ptr = buffer[0..2]$.ptr();
        if (ptr[0] != 104 || ptr[1] != 105) {
            result = 1;
        }
        buffer$.dealloc();
    }
    return result;
}
