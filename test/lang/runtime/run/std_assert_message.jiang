// assert reports source location and an optional message in every build mode.
// run-pass
// expected-exit: trap
// expected-output-contains: assertion failed at test/lang/runtime/run/std_assert_message.jiang:
// expected-output-contains: explicit assert message

Int main() {
    assert(false, "explicit assert message");
    return 0;
}
