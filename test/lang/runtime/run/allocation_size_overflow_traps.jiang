// allocation byte size 溢出时必须在调用 allocator 前 trap，不能少分配 storage。
// run-pass
// expected-exit: trap

Int main() {
    unsafe {
        UInt64*! storage = UInt64$.alloc(2305843009213693952);
        storage$.dealloc();
    }
    return 0;
}
