// run-pass
// expected-exit: 0

Int main() {
    UInt8[]^ text = new "123";
    UInt8[]^ bytes = new [1, 2, 3];
    UInt8[3]^ fixed = new [1, 2, 3];

    if (text.length != 3) {
        return 1;
    }
    if (text[0] != UInt8(49) || text[2] != UInt8(51)) {
        return 2;
    }
    if (bytes.length != 3) {
        return 3;
    }
    if (bytes[0] != UInt8(1) || bytes[2] != UInt8(3)) {
        return 4;
    }
    UInt8[3]& fixed_ref = fixed$.ref();
    return 0;
}
