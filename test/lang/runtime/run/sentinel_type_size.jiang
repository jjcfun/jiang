// run-pass
// expected-exit: 0

Int main() {
    if (UInt8[5]$.size() != 5) {
        return 1;
    }
    if (UInt8[5:0]$.size() != 6) {
        return 2;
    }
    if (UInt8[]&$.size() != UInt8[:0]&$.size()) {
        return 3;
    }
    if (UInt8*$.size() != UInt8*$.size()) {
        return 4;
    }
    return 0;
}
