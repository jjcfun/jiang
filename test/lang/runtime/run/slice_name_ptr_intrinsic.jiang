// run-pass
// expected-exit: 0

Int main() {
    UInt8[]& text = "hello";
    do [unsafe] {
        UInt8[*] ptr = text$.ptr();
        if (ptr[0] != 104 || ptr[4] != 111) {
            return 1;
        }
    }
    return 0;
}
