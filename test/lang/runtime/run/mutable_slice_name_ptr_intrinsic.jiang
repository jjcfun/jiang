// run-pass
// expected-exit: 0

Int main() {
    UInt8[3] data! = [104, 105, 0];
    UInt8[]&! text = data[0..2];
    unsafe {
        UInt8*! ptr = text$.mut_ptr();
        if (ptr[0] != 104 || ptr[1] != 105) {
            return 1;
        }
    }
    return 0;
}
