// String.split rejects a separator that could break the String UTF-8 invariant.
// run-pass
// expected-exit: 134

import std;

Int main() {
    String text = "safe";
    UInt8[1] invalid = [255];
    text.split(invalid[..]);
    return 0;
}
