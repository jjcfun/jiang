// assert without a message still reports an exact source location.
// run-pass
// expected-exit: trap
// expected-output-contains: assertion failed at test/lang/runtime/run/std_assert_location.jiang:7:5

Int main() {
    assert(false);
    return 0;
}
