// run-pass
// expected-exit: 0

Int main() {
    UInt8[:0]& sentinel = "hello";
    UInt8[]& plain = unsafe {
        sentinel$.as(UInt8[]&)
    };
    if (plain.length != 5) {
        return 1;
    }
    if (plain[0] != 104) {
        return 2;
    }
    return 0;
}
