// check-fail
// expected: initializer_type_mismatch
// 字符串字面量可以按 expected type 变成 UInt8[*:0]，保留 NUL 结尾保证；
// 普通 UInt8[*] 不能直接接收字符串字面量，避免隐式丢失 sentinel guarantee。

Int main() {
    UInt8[*] text = "hello";
    return 0;
}
