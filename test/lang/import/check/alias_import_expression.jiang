#doc(module) 导入表达式由 alias 绑定；未选中的文件不要求存在。
alias direct = import "alias_import_leaf.jiang";
alias selected = if (true) {
    import "alias_import_leaf.jiang"
} else {
    import "does_not_exist.jiang"
};
const Int first = direct.answer;
const Int second = selected.answer;
