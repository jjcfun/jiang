#doc(module) alias 的显式 comptime 初始化使用普通局部作用域和块结果。
alias selected = comptime {
    Bool enabled! = false;
    enabled = true;
    if (enabled) {
        import "alias_import_leaf.jiang"
    } else {
        import "does_not_exist.jiang"
    }
};
const Int answer = selected.answer;
