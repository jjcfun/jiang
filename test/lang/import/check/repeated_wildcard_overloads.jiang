// check-pass
#doc(module) 相同模块及菱形路径重复导入不能把同一重载组判为重复定义。
public alias * = import "repeated_wildcard_overloads_leaf.jiang";
public alias * = import "repeated_wildcard_overloads_leaf.jiang";
public alias * = import "repeated_wildcard_overloads_mid.jiang";
Int number() { return select(1); }
Bool boolean() { return select(true); }
