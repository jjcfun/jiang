// check-pass

import dep = "public_const_dep.jiang";

Int page_size() {
    dep.page_size
}

Int16 small_page_count() {
    dep.small_page_count
}

UInt64 large_mask() {
    dep.large_mask
}

Bool enabled() {
    dep.enabled
}

Float ratio() {
    dep.ratio
}

Double scale() {
    dep.scale
}

Double separated_scale() {
    dep.separated_scale
}

const dep.Mode imported_mode = dep.mode;
const (Int, Bool) imported_pair = dep.pair;
const Int[3] imported_numbers = dep.numbers;
