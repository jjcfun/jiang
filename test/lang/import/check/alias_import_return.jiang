#doc(module) 显式 return 将模块值带出 alias 的编译期初始化块。
alias selected = comptime {
    if (false) {
        return import "does_not_exist.jiang";
    }
    return import "alias_import_leaf.jiang";
};
const Int answer = selected.answer;
