// check-pass
// root file 可以用 public alias 明确重新导出指定符号。

import leaf = "public_alias_reexport_leaf.jiang";

public alias ExportedBox = leaf.Box;
public alias exported_value = leaf.value;
public alias select = leaf.select;
public alias public_only = leaf.public_only;
public alias identity = leaf.identity;
alias private_value = leaf.value;
