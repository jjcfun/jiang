#doc(module)
    为跨模块及缓存恢复提供具有同名公开 alias 的独立类型 namespace。
#end
public struct First {
    public alias selected = import "alias_import_leaf.jiang";
}
public struct Second {
    public alias selected = import "alias_star_leaf.jiang";
}
