// check-pass
// public named init 跨 module 保留名称、签名和 constructor identity。

import dep = "visibility_dep.jiang";

dep.NamedInitBox make_value() {
    dep.NamedInitBox.make(7)
}

dep.NamedInitBox^ make_owned() {
    new dep.NamedInitBox.make(8)
}
