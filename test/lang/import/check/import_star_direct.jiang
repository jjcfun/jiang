#doc(module)
    验证 wildcard alias 直接暴露公开类型和函数。
#end
alias * = import "import_star_leaf.jiang";

Int use_import_star_direct() {
    StarExported item = StarExported(value: 1);
    return star_value() + item.value;
}
