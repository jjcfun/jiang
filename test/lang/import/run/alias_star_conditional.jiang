#doc(module)
    wildcard alias 展开编译期条件选中的模块，不加载未选中的缺失路径。
#end
alias * = if (true) {
    import "../check/alias_star_leaf.jiang"
} else {
    import "missing_wildcard_branch.jiang"
};
Int main() { alias_star_value() - 41 }
