#doc(module)
    extension 中的模块 alias 作为目标类型成员按需解析，不改变导入目标的 namespace。
#end
struct Container {}
extend Container {
    public alias selected = import "../check/alias_import_leaf.jiang";
}
Int main() { Container.selected.answer - 42 }
