#doc(module)
    条件 wildcard 使用同文件前向 const，条件经统一编译期求值后只加载选中模块。
#end
alias * = if (enabled) {
    import "../check/alias_star_leaf.jiang"
} else {
    import "missing_forward_wildcard.jiang"
};
const Bool enabled = true;
Int main() { alias_star_value() - 41 }
