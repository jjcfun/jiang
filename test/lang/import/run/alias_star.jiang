#doc(module)
    新旧 wildcard 导入共享同一目标时不重复发布声明。
#end
alias * = import "../check/alias_star_mid.jiang";
import * = "../check/alias_star_leaf.jiang";
Int main() {
    AliasStarValue value = AliasStarValue(value: alias_star_value());
    assert(value.value == 41);
    return 0;
}
