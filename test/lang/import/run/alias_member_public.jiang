#doc(module)
    跨模块查询同名成员 alias 时保留实际 owner，不混淆各自的导入目标。
#end
alias dependency = import "../check/alias_member_public_leaf.jiang";
Int main() {
    assert(dependency.First.selected.answer == 42);
    assert(dependency.Second.selected.alias_star_value() == 41);
    return 0;
}
