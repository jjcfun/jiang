#doc(module) false 分支导入在运行期代码中可正常使用，未选中的文件不要求存在。
alias selected = if (false) {
    import "does_not_exist.jiang"
} else {
    import "../check/alias_import_leaf.jiang"
};
Int main() {
    assert(selected.answer == 42);
    0
}
