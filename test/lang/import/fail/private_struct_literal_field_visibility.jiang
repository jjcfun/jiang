// check-fail
// expected: private_access
// 其他模块不能通过 struct literal 初始化 private field。

import dep = "../check/visibility_dep.jiang";

dep.PublicBox make_external_box() {
    dep.PublicBox {
        hidden: 1,
        shown: 2,
    }
}

