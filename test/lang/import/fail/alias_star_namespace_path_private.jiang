#doc(module)
    namespace 值的限定路径查询不能绕过私有 alias 的访问检查。
#end
// expected: private_access
alias dependency = import "../check/alias_star_namespace_path_mid.jiang";
alias * = if (true) { dependency.hidden } else { import "missing_private_branch.jiang" };
Int main() { 0 }
