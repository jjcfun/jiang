// check-fail
// expected: private_access
// private field is not readable or writable from other modules.

import dep = "../check/visibility_dep.jiang";

() assign_private_field() {
    dep.PublicBox! box = dep.make_box();
    box.observed = 4;
}
