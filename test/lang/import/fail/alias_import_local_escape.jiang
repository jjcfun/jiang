#doc(module) comptime 初始化器的局部绑定不发布到外部 namespace。
// expected: unresolved_value
alias selected = comptime {
    const Bool enabled = true;
    if (enabled) { import "../check/alias_import_leaf.jiang" }
    else { import "does_not_exist.jiang" }
};
const Bool escaped = enabled;
