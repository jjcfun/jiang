#doc(module)
    wildcard 导入仍限制在 module namespace，不因表达式化进入类型成员空间。
#end
// expected: import_requires_module_namespace
struct Container {
    alias * = import "../check/alias_star_leaf.jiang";
}
Int main() { 0 }
