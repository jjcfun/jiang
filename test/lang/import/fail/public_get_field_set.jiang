// check-fail
// expected: private_set
// public [get] field is readable from other modules but writable only by its module.

import dep = "../check/visibility_dep.jiang";

() assign_public_get_field() {
    dep.PublicBox! box = dep.make_box();
    box.observed = 4;
}
