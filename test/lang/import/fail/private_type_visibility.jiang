// check-fail
// expected: private_access
// 跨模块不能在类型位置引用 private top-level type。

import dep = "../check/visibility_dep.jiang";

dep.PrivateBox make_private() {
    dep.PrivateBox {
        value: 1,
    }
}

