#doc(module) 独立导入声明不能出现在 alias 初始化的局部块中。
// expected: import_requires_module_namespace
alias selected = if (true) {
    import "../check/alias_import_leaf.jiang";
} else {
    import "../check/alias_import_leaf.jiang"
};
