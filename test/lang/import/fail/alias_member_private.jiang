#doc(module)
    外部模块不能通过类型 namespace 访问私有 alias。
#end
// expected: private_access
alias dependency = import "../check/alias_member_private_leaf.jiang";
Int main() { dependency.Container.hidden.answer }
