// check-fail
// expected: private_access
// public type 的 private instance method 不能被其他模块调用。

import dep = "../check/visibility_dep.jiang";

Int call_private_method() {
    dep.make_box().secret()
}
