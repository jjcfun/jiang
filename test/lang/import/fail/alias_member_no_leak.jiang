#doc(module)
    成员 namespace alias 不会在所属模块的顶层发布同名绑定。
#end
// expected: unresolved_namespace
struct Container {
    public alias member_only = import "../check/alias_import_leaf.jiang";
}
Int main() { member_only.answer }
