// check-pass
// 字段默认值会成为默认构造的参数默认值，调用时仍必须使用字段标签。

struct Options {
    Int width;
    Int height = 16;
    Bool visible = true;
}

Options make_options() {
    Options(width: 10)
}

Options make_options_shorthand() {
    .(width: 20, visible: false)
}
