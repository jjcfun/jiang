struct Outer {
    struct Inner {
        Int value;
    }
}

Int read_ref(Outer.Inner& value) {
    value.value
}

Outer.Inner^ pass_owner(Outer.Inner^ value) {
    value
}

Int main() {
    Outer.Inner value = Outer.Inner(value: 7);
    read_ref(value$.ref()) - 7
}
