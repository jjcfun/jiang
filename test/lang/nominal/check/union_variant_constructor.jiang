// check-pass
// union variant constructor 在 type check 阶段产出 owner union type。
// LLVM backend 对 overlapping payload 的写入还在 MIR/backend TODO 中。

union MaybeInt {
    none;
    value(Int);
}

MaybeInt make() {
    MaybeInt.value(1)
}

Int main() {
    0
}
