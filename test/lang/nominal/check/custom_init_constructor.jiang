// `Type(...)` 通过隐藏 init_def 构造 nominal value。

struct Point {
    Int x;
    Int y;

    public init(Int x, Int y) {
        self.x = x;
        self.y = y;
    }
}

Int main() {
    Point point = Point(1, 2);
    point.x + point.y
}
