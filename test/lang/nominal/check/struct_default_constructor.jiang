// check-pass
// 覆盖 struct 默认构造与 expected type 简写构造的正向路径。

struct Pair {
    Int left;
    Bool right;
}

Pair make_pair() {
    Pair(
        left: 1,
        right: true,
    )
}

Pair make_pair_shorthand() {
    .(
        left: 2,
        right: false,
    )
}

