// check-pass
// init(self, ...) keeps the hidden init receiver and treats later parameters normally.

struct Box {
    Int value;

    init(self, Int value) {
        self.value = value;
    }
}

Int main() {
    Box box = Box(13);
    box.value
}
