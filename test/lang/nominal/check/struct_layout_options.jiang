// check-pass

struct [packed, align: 8] Packet {
    UInt8 tag;
    UInt32 value;
}

struct [align: 8, packed] PacketReordered {
    UInt8 tag;
    UInt32 value;
}

Packet packet() {
    Packet(tag: 1, value: 2)
}

PacketReordered packet_reordered() {
    PacketReordered(tag: 1, value: 2)
}
