// check-pass
// 覆盖定长数组字面量、数组长度推断、struct literal、字段初始化和 new 表达式语法。

struct Pair {
    Int left;
    Int right;
}

Pair pair = Pair {
    left: 1,
    right: 2,
};

Int[3] values = [1, 2, 3];
Int[_] inferred_values = [1, 2, 3];

Int inferred_array_sum() {
    Int[_] local_values = [1, 2, 3];
    local_values[0] + local_values[2]
}
