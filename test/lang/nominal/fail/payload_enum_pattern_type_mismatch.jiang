// check-fail
// expected: pattern_binding_type_mismatch

enum MaybeInt {
    none,
    value(Int),
}

Int unwrap(MaybeInt input) {
    switch input {
        .value(Bool item) => 1,
        .none => 0,
    }
}
