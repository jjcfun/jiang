// check-fail
// expected: or_pattern_binding_mismatch

enum State {
    count(Int),
    flag(Bool),
}

Int value(State state) {
    switch state {
        .count(value), .flag(other) => value,
    }
}
