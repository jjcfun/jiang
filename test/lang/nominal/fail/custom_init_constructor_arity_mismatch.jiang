// expected: type_constructor_arity_mismatch

struct Point {
    Int x;
    Int y;

    public init(self, Int x, Int y) {
        self.x = x;
        self.y = y;
    }
}

Int main() {
    Point point = Point(1);
    point.x
}
