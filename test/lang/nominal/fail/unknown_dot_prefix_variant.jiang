// check-fail
// expected: unresolved_variant_for_type

enum Direction {
    north,
    south,
}

Int value(Direction direction) {
    switch direction {
        .east => 1,
        else => 0,
    }
}
