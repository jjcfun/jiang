// check-fail
// expected: pattern_binding_type_mismatch

union MaybeInt {
    () none;
    Int value;
}

Int unwrap(MaybeInt input) {
    switch input {
        .value(Bool item) => 1,
        .none => 0,
    }
}
