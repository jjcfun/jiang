// check-pass
// Builtin 文档支持单行、块体、显式 builtin 名称和 declaration/module target。

#doc(module) Jiang 文档测试模块

#doc 第一段
#jiang.doc
    第二段包含 **Markdown**、"引号中的 }" 和普通大括号：{value}。
    ```jiang
    struct Example { Int value; }
    ```
#end
public Int documented(Int value) {
    value
}

public struct Box {
    #doc 成员文档
    Int get(Self& self) {
        1
    }
}
