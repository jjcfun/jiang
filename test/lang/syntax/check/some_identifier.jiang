// check-pass
// `some` is an ordinary identifier, not a keyword.

Int some(Int value) {
    value + 1
}

union Maybe {
    Int some;
    () none;
}

Int main() {
    Int some_value = 1;
    Maybe value = .some(2);
    some_value + some(3)
}
