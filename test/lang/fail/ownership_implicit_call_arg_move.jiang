// check-fail
// expected: explicit_move_required
// 传参同样是写入 callee 参数位置，不能隐式转移 owning pointer。

Int^ consume(Int^ owner) {
    owner$.move()
}

Int^ implicit_call_arg_move(Int^ owner) {
    consume(owner)
}
