// check-fail
// expected: runtime_domain_requires_reference

struct SceneDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}

Int check_runtime_domain_target() {
    SceneDomain domain = SceneDomain();
    Task<Int> task = Task(domain: domain) { 1 };
    coroutine.sync(main_domain) { task.await() }
}
