// fail
// expected: task_binding_reassignment_not_supported

async Int load(Int value) {
    value
}

async Int reassign_task() {
    Task<Int> task! = Task { load(1) };
    task = Task { load(2) };
    task.await()
}
