// check-fail
// expected: sendable_field_requires_sendable
// 验证用户声明的 Sendable aggregate 不能包含非 Sendable 字段。

struct LocalValue {
    Int value;
}

struct InvalidPayload: Sendable {
    LocalValue value;
}

Int main() {
    0
}
