// check-fail
// expected: associated_type_bound_not_satisfied
// Domain.ExecutorType 必须实现 Executor。

struct NotExecutor {}

struct InvalidExecutorDomain: Domain<kind = .serial> {
    associated ExecutorType = NotExecutor;

    NotExecutor make_executor(Self& self) {
        NotExecutor()
    }
}
