// check-fail
// expected: generic_constraint_not_satisfied
// 验证没有 Domain 契约的同步 owned closure 不自动满足 Sendable。

@where(T: Sendable)
Int consume_sendable_type<T>() {
    0
}

Int main() {
    return consume_sendable_type<Fn<Int>^>();
}
