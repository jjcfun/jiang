// check-fail
// expected: non_movable_value_transfer
// 验证 Sendable 不会绕过直接值传递所需的 Movable 约束。

struct FixedValue: !Movable, Sendable {
    Int value;
}

Int consume(FixedValue value) {
    value.value
}

Int main() {
    FixedValue value = FixedValue(value: 7);
    return consume(value);
}
