// check-fail
// expected: missing_associated_type_impl
// associated ExecutorType 不从 make_executor 的返回类型反向推导。

struct ExplicitExecutor: Executor {
    () enqueue(Self& self, ExecutorJob job) {
        job.run();
    }
}

struct MissingExecutorTypeDomain: Domain<kind = .serial> {
    ExplicitExecutor make_executor(Self& self) {
        ExplicitExecutor()
    }
}
