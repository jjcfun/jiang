// expected: task_result_already_consumed

struct OwnerDoubleAwaitDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}
const OwnerDoubleAwaitDomain OwnerDoubleAwaitDomainValue = OwnerDoubleAwaitDomain();

async [OwnerDoubleAwaitDomainValue] Int value() {
    1
}

Int main() {
    sync [OwnerDoubleAwaitDomainValue] {
        Task<Int>^ task = new Task { value() };
        Int first = task.await();
        first + task.await()
    }
}
