// check-fail
// expected: use_after_move

struct AsyncFnOnceUseDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}

const AsyncFnOnceUseDomain async_fnonce_use_domain = AsyncFnOnceUseDomain();

async [async_fnonce_use_domain] Int use_twice() {
    FnOnce<async [async_fnonce_use_domain] Int> callback = { => 1 };
    Int first = callback();
    first + callback()
}
