// check-fail
// expected: cross_domain_mutable_borrow

struct PageDomain: Domain<kind = .serial> {}

struct WorkerDomain: Domain<kind = .serial> {}

const PageDomain page = PageDomain();

const WorkerDomain worker = WorkerDomain();

async [page] Int update(Int!& value) {
    async [worker] {
        value$.set(value$.get() + 1);
    }
    0
}
