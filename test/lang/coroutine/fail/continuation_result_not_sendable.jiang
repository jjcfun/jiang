// check-fail
// expected: generic_constraint_not_satisfied
// Continuation 不能替非 Sendable result 隐藏跨 Domain 转移风险。

struct LocalValue {
    Int value;
}

@where(T: Sendable)
Int require_sendable<T>() {
    0
}

Int main() {
    return require_sendable<coroutine.Continuation<LocalValue>>();
}
