// check-fail
// expected: async_task_syntax_removed

struct RemovedDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}
const RemovedDomain RemovedDomainValue = RemovedDomain();

Int main() {
    Task<Int>^ task = new async [RemovedDomainValue] { 1 };
    task.await()
}
