// check-fail
// expected: stack_fn_escape

struct AsyncHolderDomain: Domain<kind = .serial> {}
const AsyncHolderDomain AsyncHolderDomainValue = AsyncHolderDomain();

@region(a)
struct AsyncCallbackHolder: Movable {
    @life(a)
    Fn<async [AsyncHolderDomainValue] Int> callback;
}

@life()
async [AsyncHolderDomainValue] AsyncCallbackHolder make_bad_async_holder() {
    Int value = 11;
    Fn<async [AsyncHolderDomainValue] Int> callback = () => value;
    return AsyncCallbackHolder(callback: callback);
}

Int main() {
    0
}
