// check-fail
// expected: generic_constraint_not_satisfied
// 验证 Mutex 不会替非 Sendable payload 隐藏跨 Domain 风险。

struct LocalValue {
    Int value;
}

@where(T: Sendable)
Int consume_sendable_type<T>() {
    0
}

Int main() {
    return consume_sendable_type<Mutex<LocalValue>>();
}
