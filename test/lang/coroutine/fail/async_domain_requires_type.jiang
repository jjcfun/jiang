// check-fail
// expected: effect_domain_requires_type

struct PageDomain: Domain<kind = .serial> {}

const PageDomain page = PageDomain();

async Int load() {
    1
}

Int main() {
    async [page] {
        load()
    }
}
