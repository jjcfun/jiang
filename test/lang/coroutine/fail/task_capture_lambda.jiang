// check-fail
// expected: task_capture_not_allowed

struct PageDomain: Domain<kind = .serial> {}

async [PageDomain] Int load() {
    1
}

async [PageDomain] Int call(Fn<async [PageDomain] Int> callback) {
    callback()
}

async [PageDomain] Int capture_in_lambda() {
    Task<Int> task = async load();
    call(() => task.await())
}
