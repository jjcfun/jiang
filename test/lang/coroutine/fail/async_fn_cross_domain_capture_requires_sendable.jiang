// check-fail
// expected: cross_domain_requires_sendable

struct CaptureTargetDomain: Domain<kind = .serial> {}
const CaptureTargetDomain CaptureTargetDomainValue = CaptureTargetDomain();
struct CaptureCallerDomain: Domain<kind = .serial> {}
const CaptureCallerDomain CaptureCallerDomainValue = CaptureCallerDomain();

struct LocalPayload {
    Int value;
}

async [CaptureCallerDomainValue] Int reject_non_sendable_capture(LocalPayload payload) {
    Fn<async [CaptureTargetDomainValue] Int> callback = () => payload.value;
    callback()
}
