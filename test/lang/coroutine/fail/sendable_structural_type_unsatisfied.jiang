// check-fail
// expected: generic_constraint_not_satisfied
// 验证结构类型会递归拒绝深层的非 Sendable 元素。

struct LocalValue {
    Int value;
}

@where(T: Sendable)
Int require_sendable<T>() {
    0
}

Int main() {
    return require_sendable<(Int, LocalValue?)[2]>();
}
