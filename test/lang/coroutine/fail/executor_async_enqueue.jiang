// check-fail
// expected: trait_method_signature_mismatch
// Executor.enqueue 必须是同步方法，不能把排队动作本身变成 async。

struct AsyncExecutor: Executor {
    async () enqueue(Self& self, ExecutorJob job) {
        job.run();
    }
}
