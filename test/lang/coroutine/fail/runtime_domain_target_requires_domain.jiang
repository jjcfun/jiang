// check-fail
// expected: runtime_domain_requires_domain

Int check_runtime_domain_target() {
    Int value = 1;
    Task<Int> task = Task(domain: value$.ref()) { 1 };
    coroutine.sync(main_domain) { task.await() }
}
