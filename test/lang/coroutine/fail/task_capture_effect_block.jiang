// check-fail
// expected: task_capture_not_allowed

struct PageDomain: Domain<kind = .serial> {}

struct WorkerDomain: Domain<kind = .serial> {}

async [PageDomain] Int load() {
    1
}

async [PageDomain] Int capture_in_async_block() {
    Task<Int> task = async load();
    async [PageDomain] {
        task.await()
    }
    0
}

async [PageDomain] Int capture_in_sync_block() {
    Task<Int> task = async load();
    sync [WorkerDomain] {
        task.await()
    }
}
