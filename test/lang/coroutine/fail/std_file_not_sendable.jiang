// check-fail
// expected: generic_constraint_not_satisfied
// File 的共享 offset/close contract 尚未支持跨 Domain，不能只因 handle 是整数就自动 Sendable。

import std;

@where(T: Sendable)
Int require_sendable<T>() {
    0
}

Int main() {
    return require_sendable<std.fs.File>();
}
