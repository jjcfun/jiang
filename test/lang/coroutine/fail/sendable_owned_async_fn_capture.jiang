// check-fail
// expected: cross_domain_requires_sendable
// 验证 owned domain-bound closure 在构造点检查非 Sendable capture。

struct LocalPayload {
    Int value;
}

async [global_domain] Int build_callback(LocalPayload payload) {
    Fn<async [global_domain] Int>^ callback = new { [payload] => payload.value };
    return callback();
}

Int main() {
    0
}
