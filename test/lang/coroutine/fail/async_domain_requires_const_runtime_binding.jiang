// check-fail
// expected: effect_domain_requires_const_binding

struct PageDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}

PageDomain page = PageDomain();

async [page] Int load() {
    1
}
