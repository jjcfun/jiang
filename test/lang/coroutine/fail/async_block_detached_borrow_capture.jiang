// check-fail
// expected: borrowed_value_escape

struct PageDomain: Domain<kind = .serial> {}


async [PageDomain] Int detached_capture() {
    Int bias = 3;
    async [PageDomain] {
        bias + 1
    }
    0
}
