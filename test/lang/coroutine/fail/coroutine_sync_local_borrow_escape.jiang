// check-fail
// expected: return_lifetime_source_not_allowed
// coroutine.sync 的 callback 不能返回指向 callback 局部 storage 的引用。

struct SyncLocalBorrowDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}

const SyncLocalBorrowDomain sync_local_borrow_domain = SyncLocalBorrowDomain();

@life()
Int& reject_sync_local_borrow() {
    coroutine.sync(sync_local_borrow_domain) {
        Int local = 11;
        local$.ref()
    }
}
