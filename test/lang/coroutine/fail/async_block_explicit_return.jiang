// check-fail
// expected: async_block_explicit_return_not_supported

struct ReturnDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}
const ReturnDomain ReturnDomainValue = ReturnDomain();

Int main() {
    sync [ReturnDomainValue] {
        return 1;
    }
}
