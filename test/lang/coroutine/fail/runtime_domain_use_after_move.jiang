// check-fail
// expected: use_after_move

struct MovedDomain: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;

    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}

Int use_moved_runtime_domain() {
    MovedDomain source = MovedDomain();
    MovedDomain destination = source;
    Task<Int> task = Task(domain: source$.ref()) { 42 };
    coroutine.sync(main_domain) { task.await() }
}
