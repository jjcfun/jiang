// check-fail
// expected: task_capture_not_allowed

struct WorkerDomain: Domain<kind = .serial> {}
const WorkerDomain WorkerDomainValue = WorkerDomain();

struct Holder {
    Task<Int> task;
}

async Int reject_capture() {
    Holder holder = Holder(task: async [WorkerDomainValue] { 42 });
    async { holder.task.await() }
    0
}
