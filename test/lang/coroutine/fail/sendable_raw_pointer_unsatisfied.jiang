// check-fail
// expected: generic_constraint_not_satisfied
// 验证 raw pointer 不会自动获得 Sendable，跨线程操作仍需显式 unsafe 边界。

@where(T: Sendable)
Int require_sendable<T>() {
    0
}

Int main() {
    return require_sendable<Int*>();
}
