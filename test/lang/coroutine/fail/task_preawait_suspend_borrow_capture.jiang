// check-fail
// expected: borrowed_value_escape

async Int reject_preawait_suspend_borrow_capture() {
    Int value = 7;
    Task<Int> child = Task {
        value
    };
    Int ignored = coroutine.suspend({ continuation =>
        continuation.resume(1);
    });
    child.await()
}

Int main() {
    0
}
