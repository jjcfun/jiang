// check-pass

struct PageDomain: Domain<kind = .serial> {}


async [PageDomain] () accept_page(Fn<async [PageDomain] ()> callback) {
    callback();
}

async [PageDomain] () root(Int!& value) {
    accept_page(() => {
        value$.set(value$.get() + 1);
    });
}
