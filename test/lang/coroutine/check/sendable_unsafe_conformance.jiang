// check-pass
// 显式 unsafe Sendable conformance 可以封装经过同步审计的底层地址。

struct UnsafeHandle {
    Int* pointer;
}

unsafe extend UnsafeHandle: Sendable;

@where(T: Sendable)
Int require_sendable<T>() {
    0
}

Int main() {
    require_sendable<UnsafeHandle>()
}
