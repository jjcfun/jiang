// check-pass

() inspect_continuation(coroutine.Continuation<Int>& continuation) {
}

async Int suspend_immediately() {
    coroutine.suspend((continuation) => {
        continuation.on_cancel(() [_ marker = 1] => {
            Int value = marker;
        });
        continuation.resume(7);
    })
}

Int main() {
    Fn<(), Int> callback = (_) => {};
    callback(1);
    0
}
