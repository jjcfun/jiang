// `new Task` 直接初始化堆上 Task，返回可移动的 owner pointer。

struct WorkerDomain: Domain<kind = .serial> {}

const WorkerDomain WorkerDomainValue = WorkerDomain();

Task<Int>^ make_task() {
    new Task(domain: WorkerDomainValue) { 42 }
}
