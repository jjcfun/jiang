// check-pass
// 验证 owner handle 可以转移地址固定且 Sendable 的 pointee。

struct FixedValue: !Movable, Sendable {
    Int value;
}

@where(T: Sendable)
Int consume_owner<T>(T^ value) {
    0
}

Int main() {
    FixedValue^ value = new FixedValue(value: 7);
    return consume_owner(value);
}
