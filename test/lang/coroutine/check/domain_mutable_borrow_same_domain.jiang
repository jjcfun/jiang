// check-pass

struct PageDomain: Domain<kind = .serial> {}

const PageDomain page = PageDomain();

async [page] () bump(Int!& value) {
    value$.set(value$.get() + 1);
}

async [page] () call_direct(Int!& value) {
    bump(value);
}

async [page] () call_raw(RawFn<async [page] (), Int!&> bump_fn, Int!& value) {
    bump_fn(value);
}

async [page] () root(Int!& value) {
    call_direct(value);
    call_raw(bump, value);
}
