// check-pass
// 验证 Mutex 本体保持 pinned，跨 Domain 能力由 Sendable owner handle 表达。

@where(T: Sendable)
Int consume_sendable_type<T>() {
    0
}

Int main() {
    return consume_sendable_type<Mutex<Int>>()
        + consume_sendable_type<Mutex<Int>^>();
}
