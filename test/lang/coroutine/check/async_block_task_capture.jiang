// check-pass

struct PageDomain: Domain<kind = .serial> {}

const PageDomain page = PageDomain();

async [page] Int load(Int value) {
    value
}

async [page] Int check_task_capture() {
    Int! bias = 3;
    Task<Int> value = async [page] {
        bias = bias + 1;
        load(4) + bias
    };
    value.await() + bias
}
