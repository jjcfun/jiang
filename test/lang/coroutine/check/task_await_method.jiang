// check-pass

struct PageDomain: Domain<kind = .serial> {}


async [PageDomain] Int load(Int value) {
    value
}

async [PageDomain] Int combine() {
    Task<Int> left = async [PageDomain] { load(3) };
    Task<Int> right = async [PageDomain] { load(4) };
    left.await() + right.await()
}
