// run-pass
// expected-exit: 6

struct WorkerDomain: Domain<kind = .concurrent> {}

async [WorkerDomain] Int load() {
    5
}

Int! result = 0;

Int main() {
    async [WorkerDomain] {
        Task<Int> value = async [WorkerDomain] load();
        result = value.await() + 1;
    }
    result
}
