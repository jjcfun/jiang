// run-pass
// expected-exit: 19

struct InterruptWorker: Domain<kind = .concurrent> {}

const InterruptWorker InterruptWorkerValue = InterruptWorker();

extern unsafe () task_blocking_wait_install_interrupt();
extern unsafe Int task_blocking_wait_interrupt_then_complete();

Int main() {
    unsafe { task_blocking_wait_install_interrupt(); }
    sync [InterruptWorkerValue] {
        unsafe { task_blocking_wait_interrupt_then_complete() }
    }
}
