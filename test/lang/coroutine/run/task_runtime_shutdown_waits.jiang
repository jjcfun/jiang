// run-pass
// expected-exit: 0

struct ShutdownWorker: Domain<kind = .concurrent> {
    associated ExecutorType = ConcurrentExecutor;

    ConcurrentExecutor make_executor(Self& self) { ConcurrentExecutor() }
}

const ShutdownWorker ShutdownWorkerValue = ShutdownWorker();

extern unsafe Void task_runtime_shutdown_prepare();
extern unsafe Void task_runtime_shutdown_complete();

Int main() {
    unsafe { task_runtime_shutdown_prepare(); }
    Task(domain: ShutdownWorkerValue) {
        unsafe { task_runtime_shutdown_complete(); }
    };
    0
}
