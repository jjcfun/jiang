// run-pass
// expected-exit: 3

struct PageDomain: Domain<kind = .serial> {}

async [PageDomain] Int explicit_sync() {
    sync [PageDomain] {
        1
    }
}

async Int dynamic_current_sync() {
    sync [PageDomain] {
        2
    }
}

Int main() {
    sync [PageDomain] {
        explicit_sync() + dynamic_current_sync()
    }
}
