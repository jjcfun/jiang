// fail
// expected: reflection_index_out_of_bounds
#doc(module) 契约与路径查询验证索引边界和编译器句柄不可物化。
struct Ops {
    Int& identity(Int& value) { return value; }
    @life(return: input.x) Int& projected((Int& x, Int& y) input) { return input[0]; }
}
Void verify() {
    guard reflect.type_of<Ops>().members().get(0) is .function(function) else { return; }
    _ invalid = function.lifetime_contract().get(0).source().projections().get(0);
}
comptime { verify(); }
