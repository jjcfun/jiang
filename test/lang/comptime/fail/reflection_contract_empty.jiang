// fail
// expected: reflection_index_out_of_bounds
#doc(module) 裸函数类型的空契约不能索引关系。
Void verify() {
    guard reflect.type_of<RawFn<Void>>().shape() is .function(function) else { return; }
    _ invalid = function.lifetime_contract().get(0);
}
comptime { verify(); }
