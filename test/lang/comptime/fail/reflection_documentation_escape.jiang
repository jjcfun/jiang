// fail
// expected: comptime_value_not_materializable
#doc(module) 文档视图持有模块句柄，不能物化为运行期常量。
struct Record { Int field; }
reflect.DocumentedTargets view() {
    _ module = reflect.type_of<Record>().members().get(0).module();
    return reflect.with_doc(module);
}
const reflect.DocumentedTargets escaped = view();
