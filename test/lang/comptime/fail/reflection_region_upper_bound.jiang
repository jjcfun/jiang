// fail
// expected: reflection_index_out_of_bounds
#doc(module) region 视图必须检查负数、上界和空来源集合。
@region(r) struct View { @life(r) Int& item; }
Void check() {
    guard reflect.type_of<View>().shape() is .nominal(type) else { return; }
    _ invalid = type.regions().get(1);
}
comptime { check(); }
