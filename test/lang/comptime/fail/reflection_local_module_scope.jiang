// fail
// expected: reflection_context_required
#doc(module)
    已知类型的局部查询不授予普通 comptime 枚举整个模块声明的权限。
#end
struct Record { Int field; }
comptime {
    _ module = reflect.type_of<Record>().members().get(0).module();
    _ declarations = module.declarations();
}
