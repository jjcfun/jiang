// fail
// expected: comptime_value_not_materializable
#doc(module) 公开导出视图持有本轮模块身份，不能保存为运行期常量。
struct Record { Int field; }
const reflect.Exports escaped = reflect.exports(reflect.type_of<Record>().members().get(0).module());
