// fail
// expected: comptime_value_not_materializable
#doc(module)
    普通成员查询返回的编译期句柄不能作为运行期常量发布。
#end
struct Record { Int field; }
const reflect.Decl escaped = reflect.type_of<Record>().members().get(0);
