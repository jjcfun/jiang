// fail
// expected: comptime_value_not_materializable
#doc(module) region schema 的编译器句柄不能被物化为运行期常量。
@region(r) struct View { @life(r) Int& item; }
const reflect.Region? escaped = {
    switch reflect.type_of<View>().shape() {
        .nominal(type) => type.regions().get(0), else => null,
    }
};
