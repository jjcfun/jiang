// fail
// expected: comptime_value_not_materializable
#doc(module) 结构化签名仍包含本轮类型与参数句柄，不能保存为运行期常量。
struct Sample { Int read(self, Int input) { input } }
const reflect.FunctionSignature? escaped = {
    switch reflect.type_of<Sample>().members().get(0) {
        .function(function) => function.signature(),
        else => null,
    }
};
