// fail
// expected: comptime_value_not_materializable
#doc(module) const 实参句柄不能作为运行期常量保存，读取出的普通数据不受此限制。
struct Marker<N: const Int> {}
const reflect.GenericArgument escaped = {
    switch reflect.type_of<Marker<3>>().shape() {
        .nominal(type) => type.arguments().get(0),
        else => .type(reflect.type_of<Int>()),
    }
};
