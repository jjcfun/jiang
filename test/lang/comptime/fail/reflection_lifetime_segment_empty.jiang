// fail
// expected: reflection_index_out_of_bounds
#doc(module) 生命周期路径与视图验证访问边界及编译器句柄不可逃逸。
@region(a) struct View {
    @life(a) Int& item;
    @life(return: self.a) Int& borrow(self) { return self.item; }
}
Void verify() {
    _ invalid = reflect.type_of<View>().members().get(1).lifetime_bindings().get(0).target()$.some().segments().get(0);
}
comptime { verify(); }
