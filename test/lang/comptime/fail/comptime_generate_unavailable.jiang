// fail
// expected: comptime_generate_not_supported
#doc(module)
    未支持的 generate 入口必须诊断，其内容不能误走 eval 发布或加载 import。
#end

comptime [generate] {
    import missing = "generate_must_not_select_import.jiang";
    Int invalid() { "must not enter eval" }
}
