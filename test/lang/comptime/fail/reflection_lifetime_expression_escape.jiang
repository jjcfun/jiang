// fail
// expected: comptime_value_not_materializable
#doc(module) 生命周期查询拒绝越界访问及编译器句柄逃逸。
@region(a) struct View { @life(a) Int& item; }
const reflect.LifetimeExpression escaped = reflect.type_of<View>().members().get(0).lifetime_bindings().get(0).source();
