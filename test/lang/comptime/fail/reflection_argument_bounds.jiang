// fail
// expected: reflection_index_out_of_bounds
#doc(module) 混合泛型实参视图拒绝负数索引。
struct Marker<T, N: const Int> { T[N] values; }
comptime {
    guard reflect.type_of<Marker<Bool, 3>>().shape() is .nominal(type) else { assert(false); return; }
    _ invalid = type.arguments().get(-1);
}
