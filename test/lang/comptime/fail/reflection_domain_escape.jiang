// fail
// expected: comptime_value_not_materializable
#doc(module) 域身份句柄不能作为运行期常量保存。
struct Serial: Domain<kind = .serial> {
    associated ExecutorType = SerialExecutor;
    SerialExecutor make_executor(Self& self) { SerialExecutor() }
}
const Serial first = Serial();
const Serial second = Serial();
alias same = first;
const reflect.Domain? escaped = {
    switch reflect.type_of<RawFn<async [first] Void>>().shape() {
        .function(function) => function.domain(),
        else => null,
    }
};
