// fail
// expected: comptime_value_not_materializable
#doc(module) 包句柄只在本轮求值中有效，不能保存为运行期常量。
struct Record { Int field; }
const reflect.Package escaped = reflect.type_of<Record>().members().get(0).module().package();
