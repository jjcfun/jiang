// fail
// expected: reflection_index_out_of_bounds
#doc(module) 元组类型的组成视图检查上界。
comptime {
    guard reflect.type_of<(Int, Bool)>().shape() is .tuple(types) else { assert(false); return; }
    _ invalid = types.get(2);
}
