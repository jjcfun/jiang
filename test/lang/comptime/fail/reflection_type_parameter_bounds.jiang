// fail
// expected: reflection_index_out_of_bounds
#doc(module) 函数参数类型视图拒绝负数索引，不把负数当作返回类型查询。
comptime {
    guard reflect.type_of<RawFn<Int, Bool>>().shape() is .function(fn) else { assert(false); return; }
    _ invalid = fn.parameters().get(-1);
}
