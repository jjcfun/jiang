// fail
// expected: reflection_index_out_of_bounds
#doc(module) 生命周期查询拒绝越界访问及编译器句柄逃逸。
@region(a) struct View { @life(a) Int& item; }
comptime { _ value = reflect.type_of<View>().members().get(0).lifetime_bindings().get(1); }
