// fail
// expected: generate_context_required
#doc(module) 普通 comptime 即使位于 unsafe 函数内也不能写生成产物。
unsafe Void write_file() { generate.write("forbidden.txt", "bytes"); }
comptime { unsafe { write_file(); } }
