// fail
// expected: comptime_value_not_materializable
#doc(module) 契约与路径查询验证索引边界和编译器句柄不可物化。
struct Ops {
    Int& identity(Int& value) { return value; }
    @life(return: input.x) Int& projected((Int& x, Int& y) input) { return input[0]; }
}
const reflect.BorrowPath? escaped = {
    switch reflect.type_of<Ops>().members().get(0) {
        .function(function) => function.lifetime_contract().get(0).source(), else => null,
    }
};
