#doc(module)
    未选中分支不执行局部赋值，不加载导入表达式对应的模块。
#end

alias selected = if (false) {
    import "does-not-exist.jiang"
} else {
    import "../fixture/value.jiang"
};
comptime {
    Int value! = 42;
    if (false) { value = 0; }
    assert(value == 42);
}

Int main() { selected.number() - 9 }
