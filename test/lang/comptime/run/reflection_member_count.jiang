#doc(module)
    运行期只使用普通反射得到的整数常量，编译期查询调用不进入目标执行路径。
#end
const Int count = member_count<Record>();
struct Record { Int first; Bool second; }
Int member_count<T>() { return reflect.type_of<T>().members().len(); }
Int main() { return count - 2; }
