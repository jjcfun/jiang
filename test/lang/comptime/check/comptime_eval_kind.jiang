// check-pass
#doc(module)
    显式 eval 复用默认块的声明选择；kind 名字不是保留关键字。
#end

comptime [eval] {
    if (true) {
        const Int eval = 17;
    } else {
        import missing = "not_selected.jiang";
        Int bad() { "not checked" }
    }
}

comptime {
    if (true) {
        Int generate() { eval }
    }
}

Int use_selected() { generate() }
