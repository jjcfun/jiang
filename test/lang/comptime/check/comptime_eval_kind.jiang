// check-pass
#doc(module)
    显式 eval 与默认块执行一致；kind 名字不是保留关键字。
#end

comptime [eval] {
    const Int eval = 17;
    assert(eval == 17);
}

comptime {
    const Int generate = 17;
    assert(generate == 17);
}
