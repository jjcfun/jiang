// check-pass
#doc(module)
    前向 const 与函数选择导入，未选中的模块不加载。
#end
alias selected = if (chosen != 7) {
    import "missing_first.jiang"
} else if (enabled()) {
    import "../fixture/value.jiang"
} else {
    import "missing_last.jiang"
};
const Int chosen = calculate();
Int calculate() { 7 }
Bool enabled() { chosen == 7 }
const Int result = selected.number();
comptime { assert(result == 9); }
