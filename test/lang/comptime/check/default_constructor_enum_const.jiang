// check-pass

enum Kind {
    a;
    b;
}

struct Info {
    public Kind kind;
    public Bool flag;
}

const Info info = Info(kind: .a, flag: true);

Bool const_default_constructor_keeps_enum_field() {
    info.kind == .a && info.flag
}
