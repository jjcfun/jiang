// check-pass

import build;

comptime {
    if ((build.target.os == .host || build.target.os == .macos) && build.target.link_libc) {
        public Int selected_compile_value() {
            31
        }
    } else {
        public Int selected_compile_value() {
            "dead branch must not type check"
        }
    }
}

Int use_selected_compile_value() {
    selected_compile_value()
}
