// check-pass
// `T&?` 的 `.some(x)` 绑定得到的是 `T&`，使用时按引用访问。

Int read_optional_ref(Int&? input) {
    guard input is .some(value) else {
        return 0;
    }
    return value$.get();
}

Int main() {
    Int value = 7;
    return read_optional_ref(value$.ref());
}
