// check-pass
// for-in 的 item binding 可以声明为可变；它只在单次循环 block 内生效。

Int sum_mutable_item() {
    Int! total = 0;
    for Int! item in 0..3 {
        item = item + 1;
        total = total + item;
    }
    total
}

Int main() {
    0
}
