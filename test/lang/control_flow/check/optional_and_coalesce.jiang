// check-pass
// 覆盖 optional pattern 和强制 some 解包的 expected type 传播。

Int unwrap_with_if(Int? value) {
    if value is some item {
        item
    } else {
        0
    }
}

Int unwrap_with_some(Int? value) {
    value$.some()
}

Int main() {
    0
}
