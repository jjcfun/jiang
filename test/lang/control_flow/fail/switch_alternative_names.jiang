// expected: or_pattern_binding_mismatch
#doc(module) 同一 switch body 不能从只在一个替代模式中绑定的名字取值。
enum Choice: Copyable { one(Int), two(Int) }
Int score(Choice value) { switch value { .one(a), .two(b) => a } }
