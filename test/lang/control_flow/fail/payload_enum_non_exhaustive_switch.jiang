// expected: non_exhaustive_enum_switch
// 验证 payload enum switch 缺少 case 且没有 else 时给出穷尽性诊断。

enum Value {
    none,
    one(Int),
    pair(Int left, Bool right),
}

Int classify(Value value) {
    switch value {
        .none => 0,
        .one(_ item) => item,
    }
}
