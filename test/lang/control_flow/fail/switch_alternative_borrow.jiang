// expected: switch_pattern_bindings_mismatch
#doc(module) 共享 body 不能混用按值及借用的替代绑定。
enum Choice: Copyable { one(Int), two(Int) }
Int score(Choice value) { switch value { .one(a), .two(ref _ a) => a } }
