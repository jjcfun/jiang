#doc(module)
    泛型实例的局部常量不能仅按声明身份共享具体求值结果。
#end
Int calculate<N: const Int>() {
    const Int result = N + 1;
    const doubled = result * 2;
    doubled / 2
}
Int main() {
    assert(calculate<2>() == 3);
    assert(calculate<7>() == 8);
    assert(calculate<2>() == 3);
    0
}
