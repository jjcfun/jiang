#doc(module)
    内层 comptime 共享外层求值中的局部状态。
#end
Int main() {
    Int answer = comptime {
        Int value! = 40;
        Int nested = comptime { value = value + 1; value + 1 };
        nested
    };
    assert(answer == 42);
    return 0;
}
