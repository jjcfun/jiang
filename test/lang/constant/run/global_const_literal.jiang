// run-pass
// expected-exit: 45

const Int answer = 42;
const Int arithmetic = 1 + 2 * 3;
const Int shifted = (arithmetic << 1) - 4;
const Bool checks = shifted == 10 && !false;

Int main() {
    if (!checks) {
        return 1;
    }
    answer + 3
}
