#doc(module)
    comptime return 只返回本次求值，不返回所在函数。
#end
Int main() {
    Bool answer = comptime { return true; };
    assert(answer);
    Int nested = comptime {
        Int part = comptime { return 2; };
        part + 40
    };
    assert(nested == 42);
    return 0;
}
