// run-pass
// expected-exit: 13

const Int block_value = {
    Int base = 3;
    Int multiplier = 4;
    base * multiplier
};

const Int branch_value = if (block_value == 12) {
    block_value + 1
} else {
    0
};

Int main() {
    branch_value
}
