#doc(module)
    类型参数参与局部常量求值时使用具体实例的布局，并保持实例之间的值隔离。
#end
Int width<T>() {
    const Int result = T$.size();
    result
}
Int main() {
    assert(width<Int8>() == 1);
    assert(width<Int64>() == 8);
    assert(width<Int8>() == 1);
    0
}
