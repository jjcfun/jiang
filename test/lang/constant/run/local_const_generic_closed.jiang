#doc(module)
    泛型函数中的闭合常量表达式不应因外围模板参数尚未绑定而被拒绝。
#end
Int calculate<N: const Int>() {
    const result = 42;
    result + N
}
Int main() {
    assert(calculate<0>() == 42);
    assert(calculate<1>() == 43);
    0
}
