#doc(module)
    显式编译期 block 的泛型结果按实例求值。
#end
Int calculate<N: const Int>() {
    Int value = comptime { N + 1 };
    value
}
Int main() {
    assert(calculate<2>() == 3);
    assert(calculate<7>() == 8);
    return 0;
}
