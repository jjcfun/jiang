#doc(module)
    局部 const 复用编译期值初始化，显式类型和推导绑定均保持词法作用域。
#end
Int main() {
    const Int first = 40;
    const second = 2;
    const total = first + second;
    {
        const first = 3;
        assert(first == 3);
    }
    total - 42
}
