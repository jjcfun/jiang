// run-pass
// expected-exit: 11

struct Point {
    Int x;
    Int y;

    public init(Int x, Int y) {
        self.x = x + 1;
        self.y = y + 2;
    }
}

const Point point = Point(3, 5);
const Int point_sum = point.x + point.y;

Int main() {
    point_sum
}
