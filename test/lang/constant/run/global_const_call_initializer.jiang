// run-pass
// expected-exit: 42

Int add(Int left, Int right) {
    left + right
}

Int select(Int value) {
    if (value > 0) {
        return value + 2;
    }
    0
}

const Int value = add(19, 20) + select(1);

Int main() {
    value
}
