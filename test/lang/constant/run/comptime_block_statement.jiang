#doc(module)
    独立语句位置的 comptime 支持 Void 返回，块内名字不影响同名外层变量。
#end
Int main() {
    Int value = 7;
    comptime {
        const Int value = 42;
        assert(value == 42);
        return;
    }
    assert(value == 7);
    return 0;
}
