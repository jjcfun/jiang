#doc(module)
    配额以执行步数计数，有限递归在步骤与深度限制内应正常完成。
#end

Int count(Int value) {
    if (value == 0) { return 0; }
    count(value - 1) + 1
}

const Int result = count(100);
Int main() { assert(result == 100); 0 }
