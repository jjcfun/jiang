#doc(module)
    局部聚合常量保持具体类型，后续常量初始化可以读取其字段。
#end
struct Pair: Copyable { public Int first; public Int second; }
Int main() {
    const pair = Pair(first = 40, second = 2);
    const total = pair.first + pair.second;
    assert(pair.first == 40);
    total - 42
}
