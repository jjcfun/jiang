#doc(module)
    字符串字面量的静态存储不属于编译期堆，不能释放。
#end
// expected: comptime_invalid_free

Int calculate() {
    unsafe {
        UInt8* text = "hello";
        text$.dealloc();
    }
    0
}
const Int invalid = calculate();
Int main() { invalid }
