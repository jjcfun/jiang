#doc(module)
    空聚合仍有初始化状态；没有执行构造的局部值不能在编译期被读取或移动。
#end

struct Empty: Movable {}
Void consume(Empty value) {}
Int read() {
    Empty value;
    consume(value$.move());
    0
}
const Int invalid = read();
Int main() { invalid }
