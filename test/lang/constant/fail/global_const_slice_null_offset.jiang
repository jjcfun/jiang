#doc(module) 空裸指针不能形成超出零长度范围的切片。
// expected: comptime_invalid_slice
Int invalid() {
    unsafe {
        Int* pointer = null;
        Int[]& view = pointer[1..1];
        view.length
    }
}
const Int result = invalid();
Int main() { result }
