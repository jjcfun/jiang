#doc(module)
    从裸分配构造 slice 时，结束边界不能超出元素存储。
#end
// expected: comptime_invalid_slice
Int invalid() {
    unsafe {
        Int*! p = Int$.alloc(2);
        Int[]& view = p[0..3];
        view.length
    }
}
const Int computed = invalid();
Int main() { computed }
