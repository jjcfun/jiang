#doc(module)
    尾后元素的裸指针可以形成，但不能解引用。
#end
// expected: comptime_invalid_projection
Int invalid() { unsafe { Int*! p = Int$.alloc(2); Int* end = p[2]$.ptr(); end$.get() } }
const Int computed = invalid();
Int main() { computed }
