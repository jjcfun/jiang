#doc(module)
    延迟泛型局部常量求值不允许初始化器读取函数的运行期参数。
#end
// expected: const_initializer_not_constant
Int calculate<N: const Int>(Int input) {
    const result = N + input;
    result
}
Int main() { calculate<2>(3) }
