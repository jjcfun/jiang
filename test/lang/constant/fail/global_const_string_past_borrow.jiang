#doc(module)
    即使底层静态分配仍有字节，也不能安全借用 slice 长度之外的元素。
#end
// expected: comptime_invalid_projection

Int calculate() {
    UInt8[]& text = "hi";
    UInt8& past = text[2]$.ref();
    Int(past$.get())
}
const Int invalid = calculate();
Int main() { invalid }
