#doc(module)
    闭包间接调用不绕过编译期外部函数执行限制。
#end
// expected-error: comptime_call_not_supported
extern[C] Int foreign(Int value);
Int calculate() {
    Int value = 7;
    Fn<Int> call = { [value] => unsafe { foreign(value) } };
    return call();
}
const Int result = calculate();
Int main() { result }
