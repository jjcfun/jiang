#doc(module)
    从具体元素取出的指针也不能在原数组移动和重新初始化后复活。
#end
// expected: comptime_dead_storage
Int invalid() {
    Int[2] array! = [1, 2];
    Int* pointer = unsafe { array[0]$.ptr() };
    Int[2] moved = array$.move();
    array = [3, 4];
    unsafe { pointer[0] }
}
const Int computed = invalid();
Int main() { computed }
