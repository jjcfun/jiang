#doc(module)
    擦除和恢复不能将内部元素指针变成可释放的分配根。
#end
// expected: comptime_invalid_free
Int calculate() {
    unsafe {
        Int*! values = Int$.alloc(2);
        values[1]$.ptr()$.as(Void*)$.as(Int*)$.dealloc();
    }
    0
}
const Int result = calculate();
Int main() { result }
