#doc(module)
    unsafe 裸元素指针也不能在编译期读取分配外的地址。
#end
// expected: comptime_invalid_projection
Int invalid() { unsafe { Int*! p = Int$.alloc(1); p[0] = 7; p[1] } }
const Int computed = invalid();
Int main() { computed }
