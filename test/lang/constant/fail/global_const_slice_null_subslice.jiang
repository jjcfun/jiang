#doc(module) 从空 null 切片产生子切片时仍需检查长度上界。
// expected: comptime_invalid_slice
Int invalid() {
    unsafe {
        Int* pointer = null;
        Int[]& view = pointer[0..0];
        view[0..1].length
    }
}
const Int result = invalid();
Int main() { result }
