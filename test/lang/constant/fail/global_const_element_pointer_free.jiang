#doc(module)
    对内部元素直接取址不能洗掉偏移使其成为可释放的分配根。
#end
// expected: comptime_invalid_free
Int invalid() { unsafe { Int*! p = Int$.alloc(2); p[1]$.ptr()$.dealloc(); } 0 }
const Int computed = invalid();
Int main() { computed }
