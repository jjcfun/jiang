#doc(module)
    元素取址不能跨过分配的尾后边界。
#end
// expected: comptime_invalid_projection
Int invalid() { unsafe { Int*! p = Int$.alloc(2); Int* bad = p[3]$.ptr(); } 0 }
const Int computed = invalid();
Int main() { computed }
