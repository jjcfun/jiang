#doc(module)
    静态裸指针可以读取末尾 NUL，但不能越过实际字节分配。
#end
// expected: comptime_invalid_projection

Int calculate() {
    UInt8* text = "hi";
    unsafe { Int(text[3]) }
}
const Int invalid = calculate();
Int main() { invalid }
