#doc(module) 零元素分配的存储身份在释放后失效，不能从旧视图再次借用。
// expected: comptime_dead_storage
Int invalid() {
    unsafe {
        Int*! pointer = Int$.alloc(0);
        Int[]& view = pointer[0..0];
        pointer$.dealloc();
        view[0..0].length
    }
}
const Int result = invalid();
Int main() { result }
