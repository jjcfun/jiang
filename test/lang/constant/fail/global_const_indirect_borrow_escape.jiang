#doc(module)
    仅通过函数值引用的被调用函数仍须先通过借用检查。
#end
// expected: borrowed_value_escape
@life()
Int& invalid() {
    Int local = 17;
    local$.ref()
}
Int calculate() {
    RawFn<Int&> callback = invalid;
    callback()$.get()
}
const Int result = calculate();
Int main() { result }
