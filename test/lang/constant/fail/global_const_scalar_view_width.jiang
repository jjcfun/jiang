#doc(module)
    标量位视图不允许越过原存储宽度读取。
#end
// expected: comptime_invalid_cast
UInt64 invalid() {
    Float32 value = 1.5;
    return unsafe { value$.ptr()$.as(UInt64*)$.get() };
}
const UInt64 result = invalid();
