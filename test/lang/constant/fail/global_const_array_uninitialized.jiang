#doc(module)
    动态分配仅提供存储，不隐式初始化元素。
#end
// expected: comptime_uninitialized_value
Int invalid() { unsafe { Int*! p = Int$.alloc(2); p[0] = 7; p[1] } }
const Int computed = invalid();
Int main() { computed }
