#doc(module)
    slice 描述符不能延长原动态元素存储的生命周期。
#end
// expected: comptime_dead_storage
Int invalid() {
    unsafe {
        Int*! p = Int$.alloc(1);
        p[0] = 7;
        Int[]& view = p[0..1];
        p$.dealloc();
        view[0]
    }
}
const Int computed = invalid();
Int main() { computed }
