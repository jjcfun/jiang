#doc(module)
    哨兵 slice 允许访问末尾哨兵槽，但不能读取该槽之后的字节。
#end
// expected: comptime_invalid_projection

Int calculate() {
    UInt8[:0]& text = "hi";
    Int(text[3])
}
const Int invalid = calculate();
Int main() { invalid }
