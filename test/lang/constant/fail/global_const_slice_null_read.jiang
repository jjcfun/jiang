#doc(module) 合法的空 null 切片不提供可访问的元素。
// expected: comptime_invalid_projection
Int invalid() {
    unsafe {
        Int* pointer = null;
        Int[]& view = pointer[0..0];
        view[0]
    }
}
const Int result = invalid();
Int main() { result }
