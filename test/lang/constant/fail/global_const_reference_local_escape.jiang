#doc(module)
    const 引用可返回不代表从它复制出的局部存储可以逃逸。
#end
// expected: borrowed_value_escape

const Int number = 17;
@life()
Int& escape() {
    Int local = number;
    return local$.ref();
}
Int main() { escape()$.get() }
