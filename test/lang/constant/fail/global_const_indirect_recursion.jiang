#doc(module)
    间接递归调用消耗同一执行器的调用深度额度。
#end
// expected: comptime_recursion_limit
@life()
Int recurse(Int value) {
    RawFn<Int, Int> next = recurse;
    next(value + 1)
}
const Int result = recurse(0);
Int main() { result }
