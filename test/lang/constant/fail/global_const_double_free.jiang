#doc(module)
    编译期显式 free 不能重复释放同一个逻辑分配，即使通过 unsafe 裸指针访问。
#end
Int invalid() {
    Int^ owned = new 3;
    unsafe {
        Int* pointer = owned$.ptr();
        pointer$.dealloc();
        pointer$.dealloc();
    }
    0
}
const Int computed = invalid();
Int main() { computed }
