#doc(module)
    按值捕获不使栈闭包可跨帧返回，求值前仍执行正常的闭包逃逸检查。
#end
// expected-error: stack_fn_escape
@life()
Fn<Int> escaped() {
    Int value = 7;
    return { [value] => value };
}
Int calculate() { Fn<Int> read = escaped(); read() }
const Int result = calculate();
Int main() { result }
