#doc(module)
    子切片的数据指针不是原分配根，不能用于 free。
#end
// expected: comptime_invalid_free
Int invalid() {
    unsafe {
        Int*! p = Int$.alloc(2);
        Int[]& tail = p[1..2];
        tail$.ptr()$.dealloc();
    }
    0
}
const Int computed = invalid();
Int main() { computed }
