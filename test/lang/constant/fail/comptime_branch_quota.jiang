// expected: comptime_branch_quota_exceeded

Int burn(Int value) {
    Int current! = value;
    while (current > 0) { current = current + 1; }
    current
}

const Int value = burn(100);

Int main() {
    value
}
