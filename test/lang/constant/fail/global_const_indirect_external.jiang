#doc(module)
    间接调用外部函数不能绕过编译期执行的宿主隔离边界。
#end
// expected: comptime_call_not_supported
extern [C] Int foreign(Int value);
Int calculate() {
    RawFn<unsafe Int, Int> callback = foreign;
    unsafe { callback(1) }
}
const Int result = calculate();
Int main() { result }
