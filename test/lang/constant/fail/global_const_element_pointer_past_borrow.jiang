#doc(module)
    尾后地址只能表示裸指针，不能借用为有效元素引用。
#end
// expected: comptime_invalid_projection
Int invalid() { unsafe { Int*! p = Int$.alloc(2); Int& bad = p[2]$.ref(); } 0 }
const Int computed = invalid();
Int main() { computed }
