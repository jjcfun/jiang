#doc(module)
    字符串的末尾 NUL 不属于 slice 的可索引长度。
#end
// expected: comptime_invalid_projection

Int calculate() {
    UInt8[]& text = "hi";
    Int(text[2])
}
const Int invalid = calculate();
Int main() { invalid }
