#doc(module)
    同位宽重解释不延长指向已退出函数帧的存储生命周期。
#end
// expected: comptime_invalid_pointer
Float32* expired() {
    Float32 value = 1.5;
    return unsafe { value$.ptr() };
}
UInt32 invalid() { return unsafe { expired()$.as(UInt32*)$.get() }; }
const UInt32 result = invalid();
