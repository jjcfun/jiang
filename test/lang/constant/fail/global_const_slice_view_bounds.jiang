#doc(module)
    子切片索引不能越过自身长度，即使底层数组还有存活且已初始化的元素。
#end
// expected: comptime_invalid_projection

Int calculate() {
    Int[3] array = [2, 4, 6];
    Int[]& middle = array[1..2];
    middle[1]
}
const Int invalid = calculate();
Int main() { invalid }
