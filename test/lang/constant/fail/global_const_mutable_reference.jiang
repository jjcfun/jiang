#doc(module)
    const 的静态物化不使其成为可变借用来源。
#end
// expected: immutable_borrow_source

struct Pair: Copyable { Int left!; Int right; }
const Pair pair = Pair(left = 5, right = 6);
Int main() {
    Pair&! view = pair$.mut_ref();
    view.left = 7;
    return 0;
}
