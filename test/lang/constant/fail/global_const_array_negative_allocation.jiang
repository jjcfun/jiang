#doc(module)
    编译期动态分配拒绝负数量，与运行期的分配检查保持边界一致。
#end
// expected: comptime_invalid_allocation
Int invalid() { unsafe { Int*! p = Int$.alloc(-1); p$.dealloc(); } 0 }
const Int computed = invalid();
Int main() { computed }
