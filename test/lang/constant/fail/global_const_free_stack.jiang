#doc(module)
    unsafe 不允许求值器把局部变量地址交给 free；只有显式分配的根可以被释放。
#end
Int invalid() {
    Int value = 3;
    unsafe { value$.ptr()$.dealloc(); }
    0
}
const Int computed = invalid();
Int main() { computed }
