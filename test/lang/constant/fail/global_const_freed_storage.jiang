#doc(module)
    owning 值析构结束分配的存储期，留下的裸指针不能继续读取旧值。
#end
Int invalid() {
    Int* pointer!;
    { Int^ owned = new 3; pointer = unsafe { owned$.ptr() }; }
    unsafe { pointer$.get() }
}
const Int computed = invalid();
Int main() { computed }
