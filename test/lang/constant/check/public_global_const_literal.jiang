// check-pass

public const Int page_size = 4096;
public const Int16 small_page_count = 12;
public const UInt64 large_mask = 255;
public const Bool enabled = true;
public const Char newline = '\n';
public const Float ratio = 1.5;
public const Double scale = 2.5e1;
public const Double separated_scale = 1_2.5e1;

public enum Mode {
    debug,
    release,
}

public struct Point {
    public Int x;
    public Int y;
}

public const Mode mode = Mode.release;
public const Point origin = Point(x: 0, y: 0);
public const (Int, Bool) pair = (1, true);
public const Int[3] numbers = [1, 2, 3];

Int main() {
    if (enabled) {
        return page_size;
    }
    0
}
