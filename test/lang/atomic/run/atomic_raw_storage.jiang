// run-pass
// expected-exit: 42

Int main() {
    Int storage! = 1;
    Int*! pointer = unsafe { storage$.mut_ptr() };
    Atomic<Int>& value = unsafe { pointer$.as(Atomic<Int>&) };
    value.set_with_order(42, .release);
    value.get_with_order(.acquire)
}
