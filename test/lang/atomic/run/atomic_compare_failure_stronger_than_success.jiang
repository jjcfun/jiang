// Compare-exchange failure ordering cannot be stronger than success ordering.
// run-pass
// expected-exit: trap

Int main() {
    Atomic<Int> value = Atomic<Int>(0);
    if (value.compare_and_set(0, 1, .release, .acquire)) { return 1; }
    0
}
