// Compare-exchange failure ordering cannot release writes.
// run-pass
// expected-exit: trap

Int main() {
    Atomic<Int> value = Atomic<Int>(0);
    if (value.compare_and_set(0, 1, .sequential, .release)) { return 1; }
    0
}
