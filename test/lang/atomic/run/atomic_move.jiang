// Atomic has one move-only identity and remains usable after an explicit move.
// run-pass
// expected-exit: 42

Int main() {
    Atomic<Int> source = Atomic<Int>(41);
    Atomic<Int> destination = source$.move();
    destination.set(destination.get() + 1);
    destination.get()
}
