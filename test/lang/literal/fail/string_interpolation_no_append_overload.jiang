// check-fail
// expected: call_arg_type_mismatch

import std;

struct Unsupported {
    Int value;
}

Int main() {
    Unsupported value = Unsupported(value: 1);
    std.String text = "\(value)";
    return 0;
}
