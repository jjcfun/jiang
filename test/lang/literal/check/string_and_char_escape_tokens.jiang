// check-pass
// 覆盖 lexer 对 string/char escape token 的接受；当前测试只固定 token 边界，不验证 escape 解码语义。

UInt8[]& escaped_string() {
    "line\nquote\"slash\\"
}

Char escaped_char() {
    '\n'
}

Int main() {
    0
}
