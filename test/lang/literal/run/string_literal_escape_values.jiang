// run-pass
// expected-exit: 0

Int main() {
    UInt8[]& text = "a\n\t\\\"";
    if (text.length != 5) {
        return 1;
    }
    if (text[0] != 97 || text[1] != 10 || text[2] != 9 || text[3] != 92 || text[4] != 34) {
        return 2;
    }
    UInt8[]& nul = "a\0b";
    if (nul.length != 3 || nul[1] != 0) {
        return 3;
    }
    return 0;
}
