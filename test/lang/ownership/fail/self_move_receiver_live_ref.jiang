// check-fail
// expected: use_after_move

struct Box: Movable {
    Int value;

    @self(move)
    Int consume() {
        self.value
    }
}

Int main() {
    Box box = Box(value: 1);
    Int& borrowed = box.value$.ref();
    Int value = box.consume();
    value + borrowed$.get()
}
