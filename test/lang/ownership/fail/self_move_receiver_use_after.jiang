// check-fail
// expected: use_after_move

struct Box: Movable {
    Int value;

    Int consume(Self self) {
        self.value
    }
}

Int use_after_self_move() {
    Box box = Box(value: 1);
    Int value = box.consume();
    Box moved = box;
    value + moved.value
}
