// check-fail
// expected: use_after_move

struct Box: Movable {
    Int value;

    @self(move)
    Int consume() {
        self.value
    }
}

Int use_after_self_move() {
    Box box = Box(value: 1);
    Int value = box.consume();
    Box moved = box$.move();
    value + moved.value
}
