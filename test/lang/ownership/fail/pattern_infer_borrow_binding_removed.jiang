// check-fail
// expected: expected_token

Int read_ref(Int& value) {
    value$.get()
}

Int unwrap(Int? value) {
    if value is .some(_& item) {
        return read_ref(item);
    }
    return 0;
}
