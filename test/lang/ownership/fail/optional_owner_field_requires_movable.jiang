// check-fail
// expected: use_after_move
// 含 Int^? 字段的 nominal 默认可移动，但移动后源值不可再用。

struct Holder {
    Int^? value;
    Int marker;
}

Int move_holder(Holder value) {
    Holder moved = value;
    value.marker + moved.marker
}
