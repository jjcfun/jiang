// check-fail
// expected: use_after_move

import std;

Int use_after_into_array() {
    std.Vector<UInt8>! vec = std.Vector<UInt8>();
    vec.append(1);
    UInt8[]^ values = vec.into_array();
    vec.append(2);
    return values.length;
}
