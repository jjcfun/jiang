// check-fail
// expected: binary_operand_type_mismatch
// T^ is non-null. Use T^? when absence is part of the value.

Int main() {
    Int^ owner = new 7;
    if (owner == null) {
        return 1;
    }
    return owner$.get() - 7;
}
