// check-fail
// expected: use_after_move
// 自定义 deinit 类型可以默认移动，但移动后源值不可再用。

struct NeedsDrop {
    Int value;

    deinit(self) {
    }
}

Int move_resource(NeedsDrop value) {
    NeedsDrop moved = value;
    value.value + moved.value
}
