// check-fail
// expected: explicit_move_required
// Int^? 传参也不能隐式复制，必须显式 $.move()。

Int^? consume_optional_owner(Int^? owner) {
    owner$.move()
}

Int^? pass_optional_owner(Int^? owner) {
    consume_optional_owner(owner)
}

