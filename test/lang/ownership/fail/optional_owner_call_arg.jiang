// check-fail
// expected: use_after_move
// Int^? 传参会隐式移动，随后不能再次使用源值。

Int^? consume_optional_owner(Int^? owner) {
    owner
}

Int^? pass_optional_owner(Int^? owner) {
    Int^? moved = consume_optional_owner(owner);
    owner
}
