// check-fail
// expected: unique_self_move_receiver

struct Box: Movable {
    Int value;

    @unique(self)
    Int consume(Self self) {
        self.value
    }
}

Int main() {
    Box box = Box(value: 1);
    box.consume()
}
