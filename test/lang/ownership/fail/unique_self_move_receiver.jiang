// check-fail
// expected: unique_self_move_receiver

struct Box: Movable {
    Int value;

    @self(move)
    @unique(self)
    Int consume() {
        self.value
    }
}

Int main() {
    Box box = Box(value: 1);
    box.consume()
}
