// check-fail
// expected: use_after_move
// 未声明 Copyable 的 nominal 默认移动。

struct Value {
    Int value;
}

Int main() {
    Value first = Value(value: 1);
    Value second = first;
    first.value + second.value
}
