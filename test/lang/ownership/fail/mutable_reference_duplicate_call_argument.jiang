// check-fail
// expected: mutable_borrow_conflict

struct Box {
    Int value!;
}

Void take_two(Box&! left, Box&! right) {
}

Int main() {
    Box box! = Box(value: 1);
    take_two(box$.mut_ref(), box$.mut_ref());
    return 0;
}
