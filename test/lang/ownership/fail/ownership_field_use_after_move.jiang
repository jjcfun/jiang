// check-fail
// expected: use_after_move
// 已移动字段不能再次读取。

struct Pair: Movable {
    Int^ left;
    Int^ right;
}

Int invalid(Pair pair) {
    Int^ left = pair.left;
    assert(left$.get() == 1);
    pair.left$.get()
}
