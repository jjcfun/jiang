// expected-error: use_after_move

import std;

Int main() {
    std.StringBuilder! builder = std.StringBuilder();
    builder.write("hello");
    UInt8[]^ bytes = builder.into_slice();
    builder.write("world");
    return bytes.length;
}
