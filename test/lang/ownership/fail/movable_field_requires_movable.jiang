// check-fail
// expected: use_after_move
// 含 Movable 字段的 nominal 默认可移动，但移动后源值不可再用。

struct Resource: Movable {
    Int value;
}

struct Holder {
    Resource resource;
}

Int move_holder(Holder value) {
    Holder moved = value;
    value.resource.value + moved.resource.value
}
