// check-fail
// expected: movable_field_requires_movable
// 含 Movable 字段的 nominal type 必须显式声明 Movable。

struct Resource: Movable {
    Int value;
}

struct Holder {
    Resource resource;
}

Int main() {
    0
}
