// check-fail
// expected: unique_requirement_not_declared

struct Box {
    Int! value;

    () clear(unique self) {
        self.value = 0;
    }
}

() clear_box(Box& box) {
    box.clear();
}

Int main() {
    Box! box = Box(value: 1);
    clear_box(box$.ref());
    box.value
}
