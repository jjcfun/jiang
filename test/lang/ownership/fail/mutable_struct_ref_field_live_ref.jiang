// check-fail
// expected: mutable_borrow_conflict

struct Box {
    Int value!;

    Void reset(Self&! self) {
        self.value = 0;
    }
}

@region(a)
struct IntRef {
    @life(a)
    Int& value;
}

Int main() {
    Box box! = Box(value: 1);
    IntRef wrapper = IntRef(value: box.value$.ref());
    box.reset();
    wrapper.value$.get()
}
