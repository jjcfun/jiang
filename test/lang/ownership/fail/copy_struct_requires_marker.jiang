// check-fail
// expected: explicit_move_required
// Movable nominal type 禁止隐式复制，即使字段本身都可以隐式复制。

struct Plain: Movable {
    Int value;
}

Int main() {
    Plain first = Plain(value: 1);
    Plain second = first;
    second.value
}
