// check-fail
// expected: explicit_move_required
// aggregate 不会因为字段结构可复制而自动 Copy；struct/record/union 必须显式声明 : Copy。

struct Plain {
    Int value;
}

Int main() {
    Plain first = Plain {value: 1};
    Plain second = first;
    second.value
}
