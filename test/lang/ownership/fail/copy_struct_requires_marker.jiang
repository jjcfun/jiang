// check-fail
// expected: use_after_move
// Movable nominal type 会被隐式移动，即使字段本身都可以复制。

struct Plain: Movable {
    Int value;
}

Int main() {
    Plain first = Plain(value: 1);
    Plain second = first;
    first.value + second.value
}
