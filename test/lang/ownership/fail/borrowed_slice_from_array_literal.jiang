// check-fail
// expected: borrowed_value_escape
// 数组字面量的临时 storage 在初始化表达式结束后死亡，借用 slice 不能继续使用。

enum Item {
    empty,
    value(Int),
}

Int main() {
    Item[]& values = [.value(1), .empty()];
    return 0;
}
