// check-fail
// expected: partial_move_from_deinit_type
// 自定义 deinit 必须观察完整 self，不能从其父值移出字段。

struct Resource: Movable {
    Int^ value;

    deinit(self) {}
}

Int^ invalid(Resource resource) {
    resource.value
}
