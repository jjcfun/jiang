// check-fail
// expected: mutable_borrow_conflict

struct Box {
    Int value!;
}

() bump(Box&! box) {
    box.value = box.value + 1;
}

Int main() {
    Box box! = Box(value: 1);
    Box& shared = box$.ref();
    bump(box$.mut_ref());
    return shared.value;
}
