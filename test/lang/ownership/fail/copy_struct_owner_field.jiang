// check-fail
// expected: use_after_move
// 含 owning pointer 的 nominal 可以默认移动，但移动后源值不可再用。

struct OwnerBox {
    Int^ value;
    Int marker;
}

Int move_owner_box(OwnerBox box) {
    OwnerBox moved = box;
    box.marker + moved.marker
}
