// check-fail
// expected: partial_move_from_deinit_type
// 嵌套路径上的任一父 aggregate 声明 deinit 时，都禁止部分移动。

struct Inner: Movable {
    Int^ value;
}

struct Outer: Movable {
    Inner inner;

    deinit(self) {}
}

Int^ invalid(Outer outer) {
    outer.inner.value
}
