// check-fail
// expected: use_after_move

struct Box {
    Int value!;
}

Int main() {
    Box box! = Box(value: 1);
    Box&! first = box$.mut_ref();
    Box&! second = first;
    second.value = 2;
    return first.value;
}
