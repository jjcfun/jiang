// check-fail
// expected: unique_borrow_conflict

struct Box {
    Int! value;
}

@unique(box)
() clear_box(Box& box) {
    box.value = 0;
}

Int main() {
    Box! box = Box(value: 1);
    Int& borrowed = box.value$.ref();
    clear_box(box$.ref());
    borrowed$.get()
}
