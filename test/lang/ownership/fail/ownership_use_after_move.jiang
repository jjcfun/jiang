// check-fail
// expected: use_after_move
// $.move() 后源 place 失效，后续再次读取应由 borrow check 报错。

Int^ use_after_move(Int^ owner) {
    Int^ first = owner$.move();
    Int^ second = owner$.move();
    first$.move()
}
