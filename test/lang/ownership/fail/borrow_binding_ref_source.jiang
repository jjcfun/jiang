// check-fail
// expected: borrow_binding_ref_source
// `_&` 会主动 borrow source；source 已经是引用时拒绝，避免幂等引用语义。

Int main() {
    Int value = 1;
    Int& existing = value$.ref();
    _& borrowed = existing;
    borrowed$.get()
}
