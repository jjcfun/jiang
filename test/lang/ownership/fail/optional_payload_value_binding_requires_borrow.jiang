// check-fail
// expected: explicit_move_required
// Movable optional payload 不能通过 `some name` 隐式复制；需要使用 `some ref T name` 借用绑定。

Int use_owner(Int^ value) {
    value$.free();
    0
}

Int unwrap(Int^? owner) {
    if owner is .some(value) {
        use_owner(value)
    } else {
        0
    }
}
