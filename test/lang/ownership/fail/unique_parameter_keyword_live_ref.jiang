// check-fail
// expected: unique_borrow_conflict

struct Box {
    Int! value;
}

() clear_box(unique Box& box) {
    box.value = 0;
}

Int main() {
    Box! box = Box(value: 1);
    Int& borrowed = box.value$.ref();
    clear_box(box$.ref());
    borrowed$.get()
}
