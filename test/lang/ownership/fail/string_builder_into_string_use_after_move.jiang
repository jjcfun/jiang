// expected-error: use_after_move

import std;

Int main() {
    std.StringBuilder! builder = std.StringBuilder();
    builder.write("hello");
    String value = builder.into_string();
    builder.write("world");
    return value.byte_length;
}
