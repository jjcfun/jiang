// check-fail
// expected: non_movable_value_transfer
// 部分移动不会绕过字段类型自身的 !Movable 约束。

struct Pinned: !Movable {
    Int value;
}

struct Holder: !Movable {
    Pinned pinned;
}

Pinned invalid(Holder holder) {
    holder.pinned
}
