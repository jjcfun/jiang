// check-fail
// expected: use_after_move
// Copyable 默认复制，但显式 $.move() 仍会消费源值。

struct Point: Copyable {
    Int x;
    Int y;
}

Int main() {
    Point first = Point(x: 1, y: 2);
    Point second = first$.move();
    first.x + second.x
}
