// check-fail
// expected: mutable_borrow_conflict

struct Box {
    Int value!;

    Void clear(Self&! self) {
        self.value = 0;
    }
}

Int main() {
    Box box! = Box(value: 1);
    Int& borrowed = box.value$.ref();
    box.clear();
    borrowed$.get()
}
