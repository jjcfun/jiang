// check-fail
// expected: use_after_move
// 自定义 deinit 不要求显式 Movable，但仍遵守移动后的失效规则。

struct Resource {
    Int value;

    deinit(self) {}
}

Int move_resource(Resource value) {
    Resource moved = value;
    value.value + moved.value
}
