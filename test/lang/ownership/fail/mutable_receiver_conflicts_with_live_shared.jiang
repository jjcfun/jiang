// check-fail
// expected: mutable_borrow_conflict

struct Box {
    Int value!;

    Void clear(Self&! self) {
        self.value = 0;
    }
}

Int main() {
    Box box! = Box(value: 1);
    Box& shared = box$.ref();
    box.clear();
    return shared.value;
}
