// check-fail
// expected: use_after_move
// Movable 只允许改变 storage address，不会让 nominal 获得 Copyable。

struct Plain: Movable {
    Int value;
}

Int main() {
    Plain first = Plain(value: 1);
    Plain second = first;
    first.value + second.value
}
