// expected: use_after_move

import std;

Int main() {
    std.Vector<Int> values! = std.Vector<Int>();
    values.append(1);
    std.Vector<Int> filtered! = values.filter({ _ => true });
    return values.length() + filtered.length();
}
