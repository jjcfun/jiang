// check-fail
// expected: borrowed_value_escape
// 引用字段缺少 `field > self` 约束时，不能把外部 borrow 存进返回值。

struct Holder {
    Int& value;
}

@life(input > return)
Holder make_holder(Int& input) {
    Holder {
        value: input,
    }
}
