// check-fail
// expected: unique_borrow_conflict

struct Box {
    Int! value;

    @self(unique)
    () clear() {
        self.value = 0;
    }
}

Int main() {
    Box! box = Box(value: 1);
    Int& borrowed = box.value$.ref();
    box.clear();
    borrowed$.get()
}
