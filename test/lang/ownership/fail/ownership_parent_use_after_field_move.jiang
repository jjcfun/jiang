// check-fail
// expected: use_after_move
// 存在已移动字段时，不能把整个 aggregate 当成完整值移动。

struct Pair: Movable {
    Int^ left;
    Int^ right;
}

Pair invalid(Pair pair) {
    Int^ left = pair.left;
    assert(left$.get() == 1);
    pair
}
