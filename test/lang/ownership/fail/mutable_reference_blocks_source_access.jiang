// check-fail
// expected: mutable_borrow_conflict

struct Box {
    Int value!;
}

Int main() {
    Box box! = Box(value: 1);
    Box&! borrowed = box$.mut_ref();
    box.value = 2;
    return borrowed.value;
}
