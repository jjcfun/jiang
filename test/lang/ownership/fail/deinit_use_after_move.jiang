// check-fail
// expected: use_after_move
// 带 deinit 的 owning local move 后仍然不能再次读取或再次 move。

struct Resource: Movable {
    Int id;

    deinit() {}
}

Int main() {
    Resource! value;
    Resource! first = value$.move();
    Resource! second = value$.move();
    0
}
