// check-fail
// expected: use_after_move
// 传参会隐式转移 owning pointer，随后源值不可再用。

Int^ consume(Int^ owner) {
    owner$.move()
}

Int^ implicit_call_arg_move(Int^ owner) {
    Int^ moved = consume(owner);
    owner$.move()
}
