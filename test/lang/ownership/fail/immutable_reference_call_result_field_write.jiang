// check-fail
// expected: immutable_assignment_target

struct Box {
    Int value!;
}

@life(return: box)
Box& identity(Box& box) {
    return box;
}

Int main() {
    Box box! = Box(value: 1);
    identity(box$.ref()).value = 2;
    return box.value;
}
