// run-pass
// expected-exit: 7
// 空 deinit 语义上仍然是 custom drop，但代码生成可以跳过空调用。

struct Resource: Movable {
    Int value;

    deinit() {}
}

Int main() {
    Resource! resource = Resource(value: 7);
    Int result = resource.value;
    resource$.drop();
    return result;
}
