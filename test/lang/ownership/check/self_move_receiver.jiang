// check-pass
// @self(move) 的 instance method 会消耗 receiver。

struct Box: Movable {
    Int value;

    @self(move)
    Int consume() {
        self.value
    }
}

Int main() {
    Box box = Box(value: 7);
    box.consume()
}
