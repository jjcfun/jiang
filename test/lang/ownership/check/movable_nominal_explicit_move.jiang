// check-pass
// 非 Copyable nominal 默认移动，也可以用 $.move() 显式表达所有权转移。

struct Box: Movable {
    Int value;
}

Box pass_box(Box box) {
    Box next = box$.move();
    next$.move()
}

Int main() {
    Box box = Box(value: 1);
    Box moved = pass_box(box$.move());
    moved.value
}
