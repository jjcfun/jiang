// check-pass
// Movable nominal type 只能通过 $.move() 显式转移所有权。

struct Box: Movable {
    Int value;
}

Box pass_box(Box box) {
    Box next = box$.move();
    next$.move()
}

Int main() {
    Box box = Box {value: 1};
    Box moved = pass_box(box$.move());
    moved.value
}
