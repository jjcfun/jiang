// check-pass
// member parameter `self` is shorthand for the default reference receiver.

struct Box: Movable {
    Int value;

    Int get(self) {
        self.value
    }
}

Int main() {
    Box box = Box(value: 9);
    Int value = box.get();
    Box moved = box;
    value + moved.value
}
