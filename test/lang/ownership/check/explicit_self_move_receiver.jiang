// check-pass
// `Self self` is an explicit by-value receiver and supports dot-call.

struct Box: Movable {
    Int value;

    Int consume(Self self) {
        self.value
    }
}

Int main() {
    Box box = Box(value: 11);
    box.consume()
}
