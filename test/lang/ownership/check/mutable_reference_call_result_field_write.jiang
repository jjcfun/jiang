// check-pass
// 可变引用调用结果携带写能力，字段投影不要求先绑定临时 T&!。

struct Box {
    Int value!;
}

@life(return: box)
Box&! identity(Box&! box) {
    return box;
}

Int main() {
    Box box! = Box(value: 1);
    identity(box$.mut_ref()).value = 2;
    return box.value - 2;
}
