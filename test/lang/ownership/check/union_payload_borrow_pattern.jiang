// check-pass
// Union payload pattern 和 optional pattern 共用 `_&` 借用绑定规则。

union PairOrNone {
    () none;
    (Int, Int) pair;
}

Int read_ref(Int& value) {
    value$.get()
}

Int first(PairOrNone input) {
    switch input {
        .pair(_& left, _) => read_ref(left),
        .none => 0,
        else => 0,
    }
}
