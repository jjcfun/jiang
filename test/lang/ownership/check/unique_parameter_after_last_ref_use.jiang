// check-pass
// unique 参数对普通函数参数使用同一套调用点约束。

struct Box {
    Int value!;
}

() clear_box(unique Box& box) {
    box.value = 0;
}

Int main() {
    Box box! = Box(value: 1);
    Int& borrowed = box.value$.ref();
    Int before = borrowed$.get();
    clear_box(box$.ref());
    before + box.value
}
