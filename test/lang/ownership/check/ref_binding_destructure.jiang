// check-pass
// Destructuring supports the same `ref` binding mode as pattern payloads.

Int read_ref(Int& value) {
    value$.get()
}

Int main() {
    Int first_source = 1;
    (ref Int first, _ second) = (first_source, 2);
    read_ref(first) + second
}
