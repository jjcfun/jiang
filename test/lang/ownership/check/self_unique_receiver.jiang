// check-pass
// 可变 receiver 以引用 ABI 调用，但要求 receiver 没有未来仍会使用的派生 borrow。

struct Box {
    Int value!;

    () clear(Self&! self) {
        self.value = 0;
    }

    Int read_unique(Self&! self) {
        self.value
    }
}

Int main() {
    Box box! = Box(value: 7);
    box.clear();
    box.read_unique()
}
