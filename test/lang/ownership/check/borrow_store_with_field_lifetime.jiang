// check-pass
// 引用字段声明 `field > self` 后，可以把参数引用保存到返回的 owner 中。

struct Holder {
    @life(value > self)
    Int& value;
}

@life(input > return)
Holder make_holder(Int& input) {
    Holder {
        value: input,
    }
}
