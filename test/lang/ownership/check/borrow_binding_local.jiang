// check-pass
// `ref` 解构 pattern 对 RHS place 做隐式 borrow，并推断 pointee 类型。

Int read_ref(Int& value) {
    value$.get()
}

Int main() {
    Int value = 1;
    (ref borrowed) = value;
    read_ref(borrowed)
}
