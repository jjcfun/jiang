// check-pass
// by-value parameter 是函数入口即 live 的 owning local，return 前应补 drop/deinit。

struct Resource: Movable {
    Int id;

    deinit(self) {}
}

Int consume(Resource value) {
    0
}

Int main() {
    Resource value = Resource(id: 1);
    consume(value)
}
