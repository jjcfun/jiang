// check-pass
// T? 的 copy/move 性质跟随 T；Int^? 必须通过 $.move() 显式转移整个 optional。

Int^? pass_optional_owner(Int^? owner) {
    Int^? next = owner$.move();
    next$.move()
}

Int^? lift_owner_to_optional(Int^ owner) {
    owner$.move()
}

