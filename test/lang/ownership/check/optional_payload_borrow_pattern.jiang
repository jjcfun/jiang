// check-pass
// `some _& name` 借用 optional payload，不隐式 move payload。

Int read_ref(Int& value) {
    value$.get()
}

Int unwrap_ref(Int? value) {
    if value is some _& item {
        read_ref(item)
    } else {
        0
    }
}
