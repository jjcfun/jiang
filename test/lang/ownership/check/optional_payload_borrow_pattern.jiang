// check-pass
// `.some(ref T name)` 借用 optional payload，不隐式 move payload。

Int read_ref(Int& value) {
    value$.get()
}

Int unwrap_ref(Int? value) {
    if value is .some(ref Int item) {
        read_ref(item)
    } else {
        0
    }
}
