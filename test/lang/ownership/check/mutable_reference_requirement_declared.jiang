// check-pass
// 可变引用需要沿参数封装边界传播。

struct Box {
    Int value!;

    () clear(Self&! self) {
        self.value = 0;
    }
}

() clear_box(Box&! box) {
    box.clear();
}

Int main() {
    Box box! = Box(value: 1);
    clear_box(box$.mut_ref());
    box.value
}
