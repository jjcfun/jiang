// check-pass
// `Self& self` is an explicit reference receiver and supports dot-call.

struct Box: Movable {
    Int value;

    Int get(Self& self) {
        self.value
    }
}

Int main() {
    Box box = Box(value: 5);
    Int value = box.get();
    Box moved = box;
    value + moved.value
}
