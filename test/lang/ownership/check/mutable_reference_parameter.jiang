// check-pass
// `T&!` 参数在签名上直接声明唯一可变访问。

struct Box {
    Int value!;
}

Void clear_box(Box&! box) {
    box.value = 0;
}

Int main() {
    Box box! = Box(value: 1);
    Int& borrowed = box.value$.ref();
    Int before = borrowed$.get();
    clear_box(box$.mut_ref());
    before + box.value
}
