// check-pass
// 显式 return 离开当前 scope 前，也必须先清理已经 live 的 block local。

struct Resource: Movable {
    Int id;

    deinit() {}
}

Int run(Bool cond) {
    {
        Resource! value;
        if cond {
            return 1;
        }
    }
    0
}

Int main() {
    run(true)
}
