// check-pass
// move 后源 local 不再析构，目标 local 在离开 scope 时负责 drop/deinit。

struct Resource: Movable {
    Int id;

    deinit(self) {}
}

Int main() {
    Resource! source;
    Resource! target = source$.move();
    0
}
