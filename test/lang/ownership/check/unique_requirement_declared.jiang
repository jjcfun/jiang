// check-pass
// unique 需要沿引用参数封装边界传播。

struct Box {
    Int! value;

    () clear(unique self) {
        self.value = 0;
    }
}

() clear_box(unique Box& box) {
    box.clear();
}

Int main() {
    Box! box = Box(value: 1);
    clear_box(box$.ref());
    box.value
}
