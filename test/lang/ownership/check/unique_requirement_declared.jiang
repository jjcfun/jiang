// check-pass
// @unique 需要沿引用参数封装边界传播。

struct Box {
    Int! value;

    @unique(self)
    () clear(self) {
        self.value = 0;
    }
}

@unique(box)
() clear_box(Box& box) {
    box.clear();
}

Int main() {
    Box! box = Box(value: 1);
    clear_box(box$.ref());
    box.value
}
