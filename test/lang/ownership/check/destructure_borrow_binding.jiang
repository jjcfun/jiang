// check-pass
// Destructuring 只拆结构；每个 binding 复用普通变量定义规则。

Int read_ref(Int& value) {
    value$.get()
}

Int main() {
    Int value = 1;
    (ref _ borrowed, Int copied) = (value, 2);
    read_ref(borrowed) + copied
}
