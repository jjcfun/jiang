// check-pass
// `_&` / `_&!` remain local inferred borrow-binding forms.

Int read_ref(Int& value) {
    value$.get()
}

Int main() {
    Int source = 1;
    _& borrowed = source;
    _&! mutable_ref = source;
    read_ref(borrowed) + read_ref(mutable_ref)
}
