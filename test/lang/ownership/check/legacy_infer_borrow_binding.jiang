// check-pass
// 0.3.2 keeps the old inferred borrow-binding spelling for bootstrap compatibility.

Int read_ref(Int& value) {
    value$.get()
}

Int main() {
    Int source = 1;
    _& borrowed = source;
    _&! mutable_ref = source;
    read_ref(borrowed) + read_ref(mutable_ref)
}
