#doc(module) 同文件文档读取仍是 object 的输入，不能因无需跨文件失效而忽略。
struct Box {
    #doc A
    Int field;
}
Int width() { return reflect.type_of<Box>().members().get(0).documentation()$.some().length; }
const Int length = width();
Int main() { return length; }
