#doc(module)
    未调用函数的异步模板仍依赖缓存模块；该依赖不能冒充运行期发射根。
#end
alias api = import "./api.jiang";
alias leaf = import "./async_leaf.jiang";
Int unused() { coroutine.sync(main_domain) { leaf.compute() } }
Int main() { api.answer() }
