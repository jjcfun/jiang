#doc(module)
    命名 namespace alias 的条件选择，验证跨进程恢复不会沿用旧目标。
#end
alias settings = import "./settings.jiang";
alias left = import "./left.jiang";
alias right = import "./right.jiang";
alias selected = if (settings.enabled) { left } else { right };
public Int answer() { selected.answer() }
