#doc(module)
    缓存中首次请求普通函数时恢复嵌套 lambda 及跨词法层的捕获，不依赖协程语法。
#end

Int apply(Fn<Int> action) { action() }

public Int answer() {
    Int value = 40;
    apply { [value] => apply { [value] => value + 2 } }
}
