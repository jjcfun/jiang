#doc(module) 目录编译先求值此包声明，root 再通过普通导入读取同一个常量。
#package {
    name = "native_package";
    version = "0.5.5";
    type = .bin;
    root = "src/main.jiang";
    dependencies {
        util = "../native_dependency";
        common = "../native_common";
    }
}
