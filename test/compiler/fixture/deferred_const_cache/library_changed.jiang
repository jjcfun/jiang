#doc(module)
    修改 initializer 后必须重新求值各实例，不能复用旧长度或旧调用实参。
#end
Int identity<N: const Int>() { N }
public Int calculate<N: const Int>() {
    const Int count = N + 2;
    const Int duplicate = N + 2;
    const Int bytes = UInt8[count]$.size();
    identity<count>() + bytes + duplicate - count
}
