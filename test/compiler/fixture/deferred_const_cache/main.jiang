#doc(module)
    跨模块的局部 const 泛型实例同时参与编译期求值与运行期调用。
#end
alias library = import "./library.jiang";
const Int first = library.calculate<2>();
const Int second = library.calculate<4>();
Int main() { first + 2 * second + 3 * library.calculate<6>() }
