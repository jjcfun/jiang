#doc(module)
    仅修改私有 callee 的实现，仍须失效依赖它的编译期结果。
#end
Int identity<N: const Int>() { N + 1 }
public Int calculate<N: const Int>() {
    const Int count = N + 1;
    const Int duplicate = N + 1;
    const Int bytes = UInt8[count]$.size();
    identity<count>() + bytes + duplicate - count
}
