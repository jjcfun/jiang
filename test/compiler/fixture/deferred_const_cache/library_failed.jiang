#doc(module)
    部分实例求值失败不能被旧的成功缓存吞掉；修复后仍须可重建。
#end
Int identity<N: const Int>() { N }
public Int calculate<N: const Int>() {
    const Int count = { assert(N != 4); N + 1 };
    identity<count>() + UInt8[count]$.size()
}
