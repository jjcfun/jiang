#doc(module) 反射 payload 类型的输入，涵盖整数 enum、空分支、符号泛型和元组 payload。
public enum Plain { zero, one, }
public enum Message<T: Copyable> { none, single(T), pair(T, Bool), }
public enum Scalar { none, value(Int64), }
