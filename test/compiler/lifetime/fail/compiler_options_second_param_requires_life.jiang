// compiler-integration
// check-fail
// expected: implicit_lifetime_contract_requires_annotation
// compiler 自身的 CompileOptions 也必须被识别为携带 slice borrow 的聚合。

import options = "../../../../src/compiler/driver/options.jiang";

options.CompileOptions forward(Int& allowed, options.CompileOptions value) {
    value
}
