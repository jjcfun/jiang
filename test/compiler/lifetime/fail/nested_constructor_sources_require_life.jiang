// compiler-integration
// check-fail
// expected: implicit_lifetime_contract_requires_annotation
// 嵌套 constructor 不能丢失第二个 slice 参数的 lifetime 来源。

import options = "../../../../src/compiler/driver/options.jiang";

@region(a)
struct OptionsResult {
    @life(a)
    options.CompileOptions options;
    Bool ok;
}

OptionsResult make_options(UInt8[]& input, UInt8[]& output) {
    OptionsResult(options: options.CompileOptions(.executable, input, output), ok: true)
}
