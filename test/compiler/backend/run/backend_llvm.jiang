import context = "../../../../src/context.jiang";
import llvm_lower = "../../../../src/backend/llvm/lower.jiang";
import jil = "../../../../src/jil.jiang";
import qid = "../../../../src/id.jiang";
import rdef = "../../../../src/resolve/def.jiang";
import types = "../../../../src/sema/type.jiang";
import list = "../../../../src/support/list.jiang";
import token = "../../../../src/syntax/token.jiang";

public unsafe Int run() {
    check_llvm_declares_function_through_ffi();
    return 0;
}

unsafe Void check_llvm_declares_function_through_ffi() {
    context.CompilerContext ctx! = context.CompilerContext();
    ensure_backend_smoke_defs(ctx$.ref());
    jil.Store store! = jil.Store(true);
    store.program.add_function(simple_int_function(ctx$.ref(), store$.ref()));
    store.program.add_function(copy_local_function(ctx$.ref(), store$.ref()));
    store.program.add_function(binary_function(ctx$.ref(), store$.ref()));
    store.program.add_function(param_function(ctx$.ref(), store$.ref()));
    store.program.add_function(branch_function(ctx$.ref(), store$.ref()));
    store.program.add_function(callee_function(ctx$.ref(), store$.ref()));
    store.program.add_function(caller_function(ctx$.ref(), store$.ref()));
    store.program.add_function(tuple_function(ctx$.ref(), store$.ref()));
    store.program.add_function(tuple_projection_function(ctx$.ref(), store$.ref()));
    store.program.add_function(optional_some_function(ctx$.ref(), store$.ref()));
    store.program.add_function(optional_is_some_function(ctx$.ref(), store$.ref()));
    store.program.add_function(slice_len_function(ctx$.ref(), store$.ref()));
    store.program.add_function(optional_discriminant_function(ctx$.ref(), store$.ref()));
    store.program.add_function(error_union_value_function(ctx$.ref(), store$.ref()));

    assert(llvm_lower.lower_module(ctx$.ref(), store$.ref()));
}

unsafe Void ensure_backend_smoke_defs(context.CompilerContext& ctx) {
    while (ctx.store.defs.len() <= 120) {
        qid.SymbolId symbol = ctx.store.symbols.intern("backend_smoke_function");
        ctx.store.defs.create(
            .function_def,
            .value_name,
            .public_visibility,
            rdef.Name (symbol: symbol, span: token.Span()),
            null,
            null,
            ctx.store.resolve.builtin_namespace,
            null
        );
    }
}

@life(return: (store, store, store, store))
unsafe jil.Function simple_int_function(context.CompilerContext& ctx, jil.Store& store) {
    jil.LocalDecl*! locals = store.storage.alloc_local_array(1);
    locals[0] = jil.LocalDecl (
        id: jil.LocalId.from_index(0),
        kind: .return_place,
        type_id: ctx.store.types.int_type(),
        span: token.Span(),
        name: null,
        def_id: null,
        mutable_local: false,
        address_taken: false
    );

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: jil.Place (
                base: jil.local_place_base(jil.LocalId.from_index(0)),
                local_id: jil.LocalId.from_index(0),
                projection: store.storage.empty_projection()
            ),
            value: .use_value(jil.UseRvalue (
                value: .const_operand(jil.Const (
                    type_id: ctx.store.types.int_type(),
                    span: token.Span(),
                    value: .int_value(jil.IntConst (bits: 42, signed_value: true))
                ))
            ))
        ))
    ));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(jil.Block (
        id: jil.BlockId.from_index(0),
        span: token.Span(),
        statements: store.storage.statement_array(statements$.ref()),
        terminator: jil.Terminator (
            span: token.Span(),
            kind: .return_term(jil.ReturnTerminator (implicit_unit: false))
        )
    ));

    jil.Body body = jil.Body (
        id: jil.BodyId.from_index(0),
        span: token.Span(),
        locals: locals[0..1],
        blocks: store.storage.block_array(blocks$.ref()),
        entry: jil.BlockId.from_index(0),
        return_local: jil.LocalId.from_index(0)
    );

    return jil.Function (
        id: jil.FunctionId.from_index(0),
        origin: .source(qid.DefId.from_index(100)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: body
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function copy_local_function(context.CompilerContext& ctx, jil.Store& store) {
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = jil.LocalDecl (
        id: jil.LocalId.from_index(0),
        kind: .return_place,
        type_id: ctx.store.types.int_type(),
        span: token.Span(),
        name: null,
        def_id: null,
        mutable_local: false,
        address_taken: false
    );
    locals[1] = jil.LocalDecl (
        id: jil.LocalId.from_index(1),
        kind: .user_local,
        type_id: ctx.store.types.int_type(),
        span: token.Span(),
        name: null,
        def_id: null,
        mutable_local: false,
        address_taken: false
    );

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_const_int(ctx, store, 1, 7));
    statements.add(assign_copy(store, 0, 1));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block(store, statements$.ref()));

    return jil.Function (
        id: jil.FunctionId.from_index(1),
        origin: .source(qid.DefId.from_index(101)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(1),
            span: token.Span(),
            locals: locals[0..2],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function binary_function(context.CompilerContext& ctx, jil.Store& store) {
    jil.LocalDecl*! locals = store.storage.alloc_local_array(1);
    locals[0] = jil.LocalDecl (
        id: jil.LocalId.from_index(0),
        kind: .return_place,
        type_id: ctx.store.types.int_type(),
        span: token.Span(),
        name: null,
        def_id: null,
        mutable_local: false,
        address_taken: false
    );

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_add(ctx, store, 0, 10, 32));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block(store, statements$.ref()));

    return jil.Function (
        id: jil.FunctionId.from_index(2),
        origin: .source(qid.DefId.from_index(102)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(2),
            span: token.Span(),
            locals: locals[0..1],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function param_function(context.CompilerContext& ctx, jil.Store& store) {
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = local_decl(ctx, 0, .return_place);
    locals[1] = local_decl(ctx, 1, .param);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_copy(store, 0, 1));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block(store, statements$.ref()));

    return jil.Function (
        id: jil.FunctionId.from_index(3),
        origin: .source(qid.DefId.from_index(103)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(3),
            span: token.Span(),
            locals: locals[0..2],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function branch_function(context.CompilerContext& ctx, jil.Store& store) {
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = local_decl(ctx, 0, .return_place);
    locals[1] = bool_local_decl(ctx, 1, .param);

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(branch_block(ctx, store));

    list.ArrayList<jil.Statement> then_statements! = list.ArrayList<jil.Statement>();
    then_statements.add(assign_const_int(ctx, store, 0, 1));
    blocks.add(return_block_at(store, then_statements$.ref(), 1));

    list.ArrayList<jil.Statement> else_statements! = list.ArrayList<jil.Statement>();
    else_statements.add(assign_const_int(ctx, store, 0, 2));
    blocks.add(return_block_at(store, else_statements$.ref(), 2));

    return jil.Function (
        id: jil.FunctionId.from_index(4),
        origin: .source(qid.DefId.from_index(104)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(4),
            span: token.Span(),
            locals: locals[0..2],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function callee_function(context.CompilerContext& ctx, jil.Store& store) {
    jil.LocalDecl*! locals = store.storage.alloc_local_array(1);
    locals[0] = local_decl(ctx, 0, .return_place);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_const_int(ctx, store, 0, 5));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));

    return jil.Function (
        id: jil.FunctionId.from_index(5),
        origin: .source(qid.DefId.from_index(105)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(5),
            span: token.Span(),
            locals: locals[0..1],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function caller_function(context.CompilerContext& ctx, jil.Store& store) {
    jil.LocalDecl*! locals = store.storage.alloc_local_array(1);
    locals[0] = local_decl(ctx, 0, .return_place);

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(call_block(ctx, store));
    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    blocks.add(return_block_at(store, statements$.ref(), 1));

    return jil.Function (
        id: jil.FunctionId.from_index(6),
        origin: .source(qid.DefId.from_index(106)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(6),
            span: token.Span(),
            locals: locals[0..1],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function tuple_function(context.CompilerContext& ctx, jil.Store& store) {
    qid.TypeId tuple_type = int_pair_type(ctx);
    jil.LocalDecl*! locals = store.storage.alloc_local_array(1);
    locals[0] = typed_local_decl(ctx, 0, .return_place, tuple_type);

    list.ArrayList<jil.Operand> fields! = list.ArrayList<jil.Operand>();
    fields.add(const_int_operand(ctx, 11));
    fields.add(const_int_operand(ctx, 22));

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, 0),
            value: .aggregate_value(jil.AggregateRvalue (
                kind: .tuple,
                def_id: null,
                type_id: tuple_type,
                tag: null,
                payload_field_index: -1,
                fields: store.storage.operand_array(fields$.ref())
            ))
        ))
    ));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));

    return jil.Function (
        id: jil.FunctionId.from_index(7),
        origin: .source(qid.DefId.from_index(107)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(7),
            span: token.Span(),
            locals: locals[0..1],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function tuple_projection_function(context.CompilerContext& ctx, jil.Store& store) {
    qid.TypeId tuple_type = int_pair_type(ctx);
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = local_decl(ctx, 0, .return_place);
    locals[1] = typed_local_decl(ctx, 1, .user_local, tuple_type);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_tuple(ctx, store, 1, tuple_type));
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, 0),
            value: .use_value(jil.UseRvalue (
                value: .copy_operand(jil.CopyOperand (
                    place: constant_index_place(store, 1, 0)
                ))
            ))
        ))
    ));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));

    return jil.Function (
        id: jil.FunctionId.from_index(8),
        origin: .source(qid.DefId.from_index(108)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(8),
            span: token.Span(),
            locals: locals[0..2],
            blocks: store.storage.block_array(blocks$.ref()),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function optional_some_function(context.CompilerContext& ctx, jil.Store& store) {
    qid.TypeId optional_type = ctx.store.types.optional_type(ctx.store.types.int_type(), token.Span());
    jil.LocalDecl*! locals = store.storage.alloc_local_array(1);
    locals[0] = typed_local_decl(ctx, 0, .return_place, optional_type);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, 0),
            value: .optional_value(jil.OptionalRvalue (
                op: .make_some,
                value: const_int_operand(ctx, 9)
            ))
        ))
    ));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));

    return jil_function(store, 9, 109, locals, 1, blocks$.ref());
}

@life(return: (store, store, store, store))
unsafe jil.Function optional_is_some_function(context.CompilerContext& ctx, jil.Store& store) {
    qid.TypeId optional_type = ctx.store.types.optional_type(ctx.store.types.int_type(), token.Span());
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = typed_local_decl(ctx, 0, .return_place, ctx.store.types.bool_type());
    locals[1] = typed_local_decl(ctx, 1, .user_local, optional_type);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_optional_some(ctx, store, 1));
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, 0),
            value: .optional_value(jil.OptionalRvalue (
                op: .is_some,
                value: .copy_operand(jil.CopyOperand (place: local_place(store, 1)))
            ))
        ))
    ));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));

    return jil_function(store, 10, 110, locals, 2, blocks$.ref());
}

@life(return: (store, store, store, store))
unsafe jil.Function slice_len_function(context.CompilerContext& ctx, jil.Store& store) {
    qid.TypeId slice_type = ctx.store.types.slice_type(ctx.store.types.int_type(), token.Span());
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = local_decl(ctx, 0, .return_place);
    locals[1] = typed_local_decl(ctx, 1, .user_local, slice_type);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_slice(ctx, store, 1, slice_type));
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, 0),
            value: .len_value(jil.LenRvalue (place: local_place(store, 1)))
        ))
    ));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));

    return jil_function(store, 11, 111, locals, 2, blocks$.ref());
}

@life(return: (store, store, store, store))
unsafe jil.Function optional_discriminant_function(context.CompilerContext& ctx, jil.Store& store) {
    qid.TypeId optional_type = ctx.store.types.optional_type(ctx.store.types.int_type(), token.Span());
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = typed_local_decl(ctx, 0, .return_place, ctx.store.types.bool_type());
    locals[1] = typed_local_decl(ctx, 1, .user_local, optional_type);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_optional_some(ctx, store, 1));
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, 0),
            value: .discriminant_value(jil.DiscriminantRvalue (place: local_place(store, 1)))
        ))
    ));

    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));

    return jil_function(store, 12, 112, locals, 2, blocks$.ref());
}

@life(return: (store, store, store, store))
unsafe jil.Function error_union_value_function(context.CompilerContext& ctx, jil.Store& store) {
    qid.TypeId error_union_type = ctx.store.types.error_union_type(
        ctx.store.types.int_type(),
        ctx.store.types.bool_type(),
        token.Span()
    );
    jil.LocalDecl*! locals = store.storage.alloc_local_array(2);
    locals[0] = typed_local_decl(ctx, 0, .return_place, ctx.store.types.int_type());
    locals[1] = typed_local_decl(ctx, 1, .user_local, error_union_type);

    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    statements.add(assign_error_union_value(ctx, store, 1));
    statements.add(jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, 0),
            value: .error_union_value(jil.ErrorUnionRvalue (
                op: .unwrap_value,
                value: .copy_operand(jil.CopyOperand (place: local_place(store, 1)))
            ))
        ))
    ));
    list.ArrayList<jil.Block> blocks! = list.ArrayList<jil.Block>();
    blocks.add(return_block_at(store, statements$.ref(), 0));
    return jil_function(store, 13, 113, locals, 2, blocks$.ref());
}

@life(return: store)
unsafe jil.Statement assign_error_union_value(
    context.CompilerContext& ctx,
    jil.Store& store,
    Int local_index
) {
    return jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, local_index),
            value: .error_union_value(jil.ErrorUnionRvalue (
                op: .make_value,
                value: .const_operand(jil.Const (
                    type_id: ctx.store.types.int_type(),
                    span: token.Span(),
                    value: .int_value(jil.IntConst (bits: 42, signed_value: true))
                ))
            ))
        ))
    );
}

@life(return: (store, store, store, store))
unsafe jil.Function jil_function(
    jil.Store& store,
    Int function_index,
    Int def_index,
    jil.LocalDecl*! locals,
    Int local_count,
    list.ArrayList<jil.Block>& blocks
) {
    return jil.Function (
        id: jil.FunctionId.from_index(function_index),
        origin: .source(qid.DefId.from_index(def_index)),
        type_args: store.storage.empty_type_args(),
        const_args: store.storage.empty_const_args(),
        signature_id: null,
        effects: types.EffectSet(),
        is_coroutine: false,
        coroutine_frame: null,
        is_coroutine_resume: false,
        is_coroutine_completion: false,
        is_language_entry: false,
        is_runtime_entry: false,
        is_external: false,
        inline_always: false,
        naked: false,
        span: token.Span(),
        body: jil.Body (
            id: jil.BodyId.from_index(function_index),
            span: token.Span(),
            locals: locals[0..local_count],
            blocks: store.storage.block_array(blocks),
            entry: jil.BlockId.from_index(0),
            return_local: jil.LocalId.from_index(0)
        )
    );
}

unsafe jil.LocalDecl local_decl(context.CompilerContext& ctx, Int index, jil.LocalKind kind) {
    return typed_local_decl(ctx, index, kind, ctx.store.types.int_type());
}

unsafe jil.LocalDecl typed_local_decl(
    context.CompilerContext& ctx,
    Int index,
    jil.LocalKind kind,
    qid.TypeId type_id
) {
    return jil.LocalDecl (
        id: jil.LocalId.from_index(index),
        kind: kind,
        type_id: type_id,
        span: token.Span(),
        name: null,
        def_id: null,
        mutable_local: false,
        address_taken: false
    );
}

unsafe qid.TypeId int_pair_type(context.CompilerContext& ctx) {
    list.ArrayList<qid.TypeId> items! = list.ArrayList<qid.TypeId>(2);
    items.add(ctx.store.types.int_type());
    items.add(ctx.store.types.int_type());
    return ctx.store.types.tuple_type(items$.move(), token.Span());
}

unsafe jil.LocalDecl bool_local_decl(context.CompilerContext& ctx, Int index, jil.LocalKind kind) {
    return jil.LocalDecl (
        id: jil.LocalId.from_index(index),
        kind: kind,
        type_id: ctx.store.types.bool_type(),
        span: token.Span(),
        name: null,
        def_id: null,
        mutable_local: false,
        address_taken: false
    );
}

@life(return: store)
unsafe jil.Statement assign_const_int(
    context.CompilerContext& ctx,
    jil.Store& store,
    Int local_index,
    UInt64 value
) {
    return jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, local_index),
            value: .use_value(jil.UseRvalue (
                value: .const_operand(jil.Const (
                    type_id: ctx.store.types.int_type(),
                    span: token.Span(),
                    value: .int_value(jil.IntConst (bits: value, signed_value: true))
                ))
            ))
        ))
    );
}

unsafe jil.Statement assign_copy(jil.Store& store, Int destination, Int source) {
    return jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, destination),
            value: .use_value(jil.UseRvalue (
                value: .copy_operand(jil.CopyOperand (place: local_place(store, source)))
            ))
        ))
    );
}

@life(return: store)
unsafe jil.Statement assign_add(
    context.CompilerContext& ctx,
    jil.Store& store,
    Int destination,
    UInt64 lhs,
    UInt64 rhs
) {
    return jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, destination),
            value: .binary_value(jil.BinaryRvalue (
                op: .add,
                lhs: const_int_operand(ctx, lhs),
                rhs: const_int_operand(ctx, rhs)
            ))
        ))
    );
}

@life()
unsafe jil.Operand const_int_operand(context.CompilerContext& ctx, UInt64 value) {
    return .const_operand(jil.Const (
        type_id: ctx.store.types.int_type(),
        span: token.Span(),
        value: .int_value(jil.IntConst (bits: value, signed_value: true))
    ));
}

@life(return: store)
unsafe jil.Statement assign_tuple(
    context.CompilerContext& ctx,
    jil.Store& store,
    Int local_index,
    qid.TypeId tuple_type
) {
    list.ArrayList<jil.Operand> fields! = list.ArrayList<jil.Operand>();
    fields.add(const_int_operand(ctx, 11));
    fields.add(const_int_operand(ctx, 22));
    return jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, local_index),
            value: .aggregate_value(jil.AggregateRvalue (
                kind: .tuple,
                def_id: null,
                type_id: tuple_type,
                tag: null,
                payload_field_index: -1,
                fields: store.storage.operand_array(fields$.ref())
            ))
        ))
    );
}

@life(return: store)
unsafe jil.Statement assign_optional_some(
    context.CompilerContext& ctx,
    jil.Store& store,
    Int local_index
) {
    return jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, local_index),
            value: .optional_value(jil.OptionalRvalue (
                op: .make_some,
                value: const_int_operand(ctx, 33)
            ))
        ))
    );
}

@life(return: store)
unsafe jil.Statement assign_slice(
    context.CompilerContext& ctx,
    jil.Store& store,
    Int local_index,
    qid.TypeId slice_type
) {
    list.ArrayList<jil.Operand> fields! = list.ArrayList<jil.Operand>();
    qid.TypeId pointer_type = ctx.store.types.raw_pointer_type(ctx.store.types.int_type(), token.Span());
    jil.Operand pointer_operand = .const_operand(jil.Const (
        type_id: pointer_type,
        span: token.Span(),
        value: .null_value(jil.NullConst (typed_null: true))
    ));
    fields.add(pointer_operand$.move());
    fields.add(const_int_operand(ctx, 4));
    return jil.Statement (
        span: token.Span(),
        kind: .assign_stmt(jil.AssignStatement (
            destination: local_place(store, local_index),
            value: .aggregate_value(jil.AggregateRvalue (
                kind: .struct_value,
                def_id: null,
                type_id: slice_type,
                tag: null,
                payload_field_index: -1,
                fields: store.storage.operand_array(fields$.ref())
            ))
        ))
    );
}

unsafe jil.Place local_place(jil.Store& store, Int index) {
    return jil.Place (
        base: jil.local_place_base(jil.LocalId.from_index(index)),
        local_id: jil.LocalId.from_index(index),
        projection: store.storage.empty_projection()
    );
}

unsafe jil.Place constant_index_place(jil.Store& store, Int local_index, Int offset) {
    list.ArrayList<jil.PlaceElem> projection! = list.ArrayList<jil.PlaceElem>();
    jil.PlaceElem elem = .constant_index_projection(jil.ConstantIndexProjection (offset: offset));
    projection.add(elem$.move());
    return jil.Place (
        base: jil.local_place_base(jil.LocalId.from_index(local_index)),
        local_id: jil.LocalId.from_index(local_index),
        projection: store.storage.projection_array(projection$.ref())
    );
}

@life(return: store)
unsafe jil.Block return_block(jil.Store& store, list.ArrayList<jil.Statement>& statements) {
    return return_block_at(store, statements, 0);
}

@life(return: store)
unsafe jil.Block return_block_at(
    jil.Store& store,
    list.ArrayList<jil.Statement>& statements,
    Int id
) {
    return jil.Block (
        id: jil.BlockId.from_index(id),
        span: token.Span(),
        statements: store.storage.statement_array(statements),
        terminator: jil.Terminator (
            span: token.Span(),
            kind: .return_term(jil.ReturnTerminator (implicit_unit: false))
        )
    );
}

@life(return: store)
unsafe jil.Block branch_block(context.CompilerContext& ctx, jil.Store& store) {
    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    return jil.Block (
        id: jil.BlockId.from_index(0),
        span: token.Span(),
        statements: store.storage.statement_array(statements$.ref()),
        terminator: jil.Terminator (
            span: token.Span(),
            kind: .branch_term(jil.BranchTerminator (
                condition: .copy_operand(jil.CopyOperand (place: local_place(store, 1))),
                then_target: jil.BlockId.from_index(1),
                else_target: jil.BlockId.from_index(2)
            ))
        )
    );
}

@life(return: store)
unsafe jil.Block call_block(context.CompilerContext& ctx, jil.Store& store) {
    list.ArrayList<jil.Statement> statements! = list.ArrayList<jil.Statement>();
    list.ArrayList<jil.Operand> args! = list.ArrayList<jil.Operand>();
    list.ArrayList<qid.TypeId> param_types! = list.ArrayList<qid.TypeId>();
    qid.TypeId function_type = ctx.store.types.function_type(
        param_types$.move(),
        ctx.store.types.int_type(),
        token.Span()
    );
    return jil.Block (
        id: jil.BlockId.from_index(0),
        span: token.Span(),
        statements: store.storage.statement_array(statements$.ref()),
        terminator: jil.Terminator (
            span: token.Span(),
            kind: .call_term(jil.CallTerminator (
                callee: .direct_function(jil.FunctionRef (
                    target: .source(qid.DefId.from_index(105)),
                    type_args: store.storage.empty_type_args(),
                    const_args: store.storage.empty_const_args(),
                    coroutine_resume: false,
            coroutine_completion: false,
                type_id: function_type,
                    span: token.Span()
                )),
                args: store.storage.operand_array(args$.ref()),
                current_effects: types.EffectSet(),
                is_suspend_point: false,
                destination: local_place(store, 0),
                target: jil.BlockId.from_index(1),
                initialized_place: null,
                exclusive_places: store.storage.empty_places(),
                cleanup: null
            ))
        )
    );
}
