import Store = "enum_ns_defs.jiang";

_ kw = Store.TokenKind.kw;
_ punct = Store.TokenKind.punct;

assert(kw == Store.TokenKind.kw);
assert(punct == Store.TokenKind.punct);
assert(punct == .punct);
assert(kw != punct);

print("Namespace enum test passed!\n");
