Int twice(Int value) {
    return value + value;
}

Int counter = 0;
Int acc = twice(3);

while (counter < 4) {
    acc = acc + counter;
    counter = counter + 1;
}

if (acc == 10) {
    acc = acc + 1;
} else {
    acc = 0;
}
