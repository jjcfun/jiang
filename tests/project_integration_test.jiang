import std;
import "project/math.jiang";
import "project/model.jiang";

Int total = sum_pair(10);
std.debug.assert(total == 21);
Int seen = inspect(7);
std.debug.assert(seen == 15);
std.debug.assert(std.fs.is_absolute("/tmp/file"));
std.debug.assert(std.fs.exists("tests/project/model.jiang"));

print("project_integration_test passed!\n");
