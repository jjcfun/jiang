import std;
import "project/math.jiang";
import "project/model.jiang";

Int total = sum_pair(10);
std.assert.require(total == 21);
Int seen = inspect(7);
std.assert.require(seen == 15);
std.assert.require(std.fs.is_absolute("/tmp/file"));

{
    _ file = std.fs.File.open(3);
    std.assert.require(file.is_valid());
    file.close();
}

print("project_integration_test passed!\n");
