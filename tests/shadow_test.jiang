() test_shadowing() {
    Int print = 123;
    // Now 'print' is an Int, not a function
    // assert(print == 123); // This should work because 'assert' is not shadowed
    // print("This should error in semantic check if I uncomment it");
}

test_shadowing();
