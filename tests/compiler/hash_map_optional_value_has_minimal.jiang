import "../../compiler/support/map.jiang";

Int main() {
    map.HashMap<Int, Int?> table = map.HashMap<Int, Int?>();
    table.set(1, null);

    if (table.has(1)) {
        return 42;
    }
    return 1;
}
