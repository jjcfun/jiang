import "../../compiler/support/arena.jiang";
import "../../compiler/support/list.jiang";

Int main() {
    arena.Arena* store = new arena.Arena();
    list.ArenaList<Int!> items = list.ArenaList<Int!>(store);
    items.add(1);
    items.add(2);
    items.add(3);
    items[1] = 20;
    items.reserve(10);
    items.clear();
    items.add(40);
    items.add(2);
    Int result = items[0] + items[1] + items.len() + items.cap();
    store$.free();
    return result;
}
