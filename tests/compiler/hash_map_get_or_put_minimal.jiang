import "../../compiler/support/map.jiang";

Int main() {
    map.HashMap<Int, Int!> table = map.HashMap<Int, Int!>();

    Int!* first = table.get_or_put(3, 7);
    first = first + 5;

    Int!* second = table.get_or_put(3, 100);
    second = second + 1;

    Int?! value = table.get(3);
    if (value != null) {
        return value + table.len() + table.cap();
    }
    return 1;
}
