import "../../compiler/support/map.jiang";

Int main() {
    map.HashMap<Int, Int!> table = map.HashMap<Int, Int!>();
    table.set(1, 10);

    Int!*? maybe = table.get_ptr(1);
    assert(maybe != null);

    Int!* slot = maybe$.some();
    slot = 40;

    Int?! value = table.get(1);
    assert(value != null);
    assert(value$.some() + table.len() + table.cap() == 45);
    return 0;
}
