struct Table: SubscriptGet {
    UInt8 value;

    init() {
        self.value = 42;
    }

    public UInt8 subscript_get(Int index) {
        return self.value;
    }
}

Int main() {
    Table table = Table();
    assert(table[7]$.as(Int) == 42);
    return 0;
}
