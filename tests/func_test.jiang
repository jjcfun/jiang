// 测试自定义函数
Int add(Int a, Int b) {
    return a + b;
}

() test_void() {
    Int! x = 1;
    x = x + 1;
    return;
}

Int result = add(5, 10);
assert(result == 15);

test_void();

print("func_test passed!\n");
