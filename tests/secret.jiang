public struct Vault {
    Int id; // Private by default
    public Int balance;
    
    public init(Int id, Int balance) {
        self.id = id;
        self.balance = balance;
    }
    
    public () show() {
        print("Vault id: %d, balance: %d\n", self.id, self.balance);
    }
}
