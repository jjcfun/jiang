Double add_fraction(Double x) {
    return x + 0.5;
}

Float lift_float(Float x) {
    return x + 1.5;
}

Double result = add_fraction(1.0);
Float lifted = lift_float(2.0);

assert(result == 1.5);
assert(lifted == 3.5);
print("jir_float_test passed!\n");
