import "./multi_file_enum_helper.jiang";

Int code(multi_file_enum_helper.Mode mode) {
    if (mode == multi_file_enum_helper.Mode.read) {
        return 1;
    }
    return 0;
}

Int main() {
    return code(.read);
}
