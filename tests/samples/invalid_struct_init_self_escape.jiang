struct User {
    Int id;

    init(Int id) {
        takes_user(self);
        self.id = id;
        return;
    }
}

() takes_user(User* user) {
    return;
}

Int main() {
    User user = User(42);
    return user.id;
}
