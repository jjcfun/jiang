@where(T: Hashable, T: Equatable)
Int score<T>(T value, T other) {
    Int! total = Int(value.hash());
    if (value.equal(other$.ref())) {
        total = total + 10;
    }
    return total;
}

Int main() {
    return score(7, 7);
}
