struct Profile {
    Int age;
}

struct User {
    Profile? profile;
}

Int main() {
    User? user = User { profile: Profile { age: 42 } };
    Int? age = user?.profile?.age;
    if (age is some value) {
        return value;
    }
    return 0;
}
