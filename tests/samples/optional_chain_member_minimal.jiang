struct Profile {
    Int age;
}

struct User {
    Profile? profile;
}

Int main() {
    User? user = User {
        profile: Profile { age: 42 }
    };
    Int? age = user?.profile?.age;
    if (age != null) {
        return age;
    }
    return 0;
}
