struct Pair {
    Int left;
    Int right;
}

Int mutate() {
    Pair pair = Pair { left: 1, right: 2 };
    pair.left = pair.left + pair.right;
    return pair.left;
}

Int main() {
    return mutate();
}
