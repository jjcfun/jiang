trait Iterator {
    type Item;
    Item next();
}

struct Counter: Iterator {
    type Iterator.Item = Int;

    UInt8 next() {
        return 42;
    }
}

Int main() {
    Counter counter = Counter {};
    return counter.next()$.as(Int);
}
