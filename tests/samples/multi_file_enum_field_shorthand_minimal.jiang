import "./multi_file_enum_helper.jiang";

struct Config {
    multi_file_enum_helper.Mode mode;
}

Int main() {
    Config config = Config { mode: .read };
    if (config.mode == multi_file_enum_helper.Mode.read) {
        return 1;
    }
    return 0;
}
