struct Profile {
    Int age;
}

struct User {
    Profile? profile;
}

Int main() {
    User[1] users = [
        User { profile: Profile { age: 42 } }
    ];
    Int? age = users[0].profile?.age;
    if (age is some value) {
        return value;
    }
    return 0;
}
