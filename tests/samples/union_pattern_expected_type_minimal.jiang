union Pattern {
    Int literal;
}

union Expr {
    Pattern literal;
}

Int read(Expr expr) {
    if (expr is .literal(pattern)) {
        if (pattern is .literal(value)) {
            return value;
        }
    }
    return 0;
}

Int main() {
    return read(.literal(.literal(42)));
}
