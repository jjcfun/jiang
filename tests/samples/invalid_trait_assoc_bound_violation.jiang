trait HasItem {
    associated Item: Hashable;
    Item item();
}

struct Box: HasItem {
    associated Item = Float;

    Float item() {
        return 42;
    }
}

Int main() {
    Box box = Box {};
    return box.item()$.as(Int);
}
