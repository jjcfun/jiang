record Vec3 {
    Int x, y = 2, z;
}

struct Pair {
    Int left, right;
}

Int main() {
    Vec3 vec = { x: 1, z: 3 };
    Pair pair = Pair { left: 10, right: 20 };
    Int a = 4, b = 5, c = 6;
    return vec.x + vec.y + vec.z + pair.left + pair.right + a + b + c;
}
