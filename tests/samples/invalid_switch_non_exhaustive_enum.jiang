enum Priority {
    low,
    medium,
    high,
}

Int choose(Priority priority) {
    switch (priority) {
        Priority.low: return 1;
        Priority.medium: return 2;
    }
}

Int main() {
    return choose(Priority.high);
}
