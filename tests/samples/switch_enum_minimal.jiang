enum Priority {
    low,
    medium,
    high,
}

Int choose(Priority priority) {
    switch (priority) {
        .low: return 1;
        .medium: return 42;
        else: return 3;
    }
}

Int main() {
    return choose(Priority.medium);
}
