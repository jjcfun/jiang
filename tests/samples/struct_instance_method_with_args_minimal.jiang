struct User {
    Int id;

    init(Int id) {
        self.id = id;
        return;
    }

    Int add(Int x) {
        return self.id + x;
    }
}

Int main() {
    User user = User(40);
    return user.add(2);
}
