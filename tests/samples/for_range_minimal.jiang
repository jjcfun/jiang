Int loop_for(Int n) {
    Int sum = 0;
    for Int i in 0..n {
        if (i == 2) {
            continue;
        }
        if (i == 5) {
            break;
        }
        sum = sum + i;
    }
    return sum;
}

Int main() {
    return loop_for(10);
}
