union Payload {
    Int number;
    Bool flag;
}

Int main() {
    Payload value = if (true) {
        .number(42)
    } else {
        .flag(false)
    };

    if (value is .number(n)) {
        return n;
    }
    return 1;
}
