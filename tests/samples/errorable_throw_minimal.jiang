enum Err {
    bad = 1,
}

Int@Err fail() {
    throw Err.bad;
}

Int main() {
    switch (fail()) {
    error e: {
        if (e == Err.bad) {
            return 1;
        }
        return 0;
    }
    else:
        return 0;
    }
}
