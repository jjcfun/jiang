enum Kind {
    c,
    e,
}

union(Kind) MyUnion {
    c((Int, Int));
    e;
}

Int choose(MyUnion value) {
    if (value is .c(_! left, _ right)) {
        left = left + right;
        return left;
    }
    return 0;
}

Int main() {
    return choose(.c((40, 2)));
}
