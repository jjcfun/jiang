enum Err {
    bad = 1,
}

Int@Err parse(Bool fail) {
    if (fail) {
        throw Err.bad;
    }
    return 40;
}

Int main() {
    Int a = parse(false) catch 1;
    Int b = parse(true) catch 1;
    return a + b + 1;
}
