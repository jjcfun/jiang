enum Err {
    bad,
}

Int@Err fail() {
    throw Err.bad;
}

Int main() {
    try {
        fail();
    } catch (Err first) {
        return 1;
    } catch (Err second) {
        return 2;
    }
    return 0;
}
