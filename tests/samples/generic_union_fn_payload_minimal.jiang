enum Err {
    bad,
}

union Result<T, E> {
    T value;
    E error;
}

Int plus_one(Int value) {
    return value + 1;
}

Int main() {
    Result<Fn<Int, Int>, Err> callback = .value(plus_one);
    Fn<Int, Int> fn = callback.value;
    return fn(41);
}
