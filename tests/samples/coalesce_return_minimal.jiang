Int! global = 0;

() store(Int? value) {
    Int number = value ?? return;
    global = number;
}

Int main() {
    store(null);
    if (global != 0) {
        return 1;
    }
    store(42);
    return global;
}
