union Expr {
    Int hir_literal;
    Int hir_name;
}

Int read(Expr expr) {
    if (expr is .hir_literal(value)) {
        return value;
    }
    if (expr is .hir_name(value)) {
        return value + 1;
    }
    return 0;
}

Int main() {
    return read(.hir_literal(42));
}
