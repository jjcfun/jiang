struct User {
    Int id;

    init(Int id) {
        self.id = id;
        return;
    }

    init(Int value) {
        self.id = value;
        return;
    }
}

Int main() {
    User user = User(42);
    return user.id;
}
