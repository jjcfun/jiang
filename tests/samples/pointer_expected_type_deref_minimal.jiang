Int^ make_value(Int value) {
    return new Int(value);
}

Int main() {
    Int^ ptr = make_value(10);
    _ inferred = make_value(20);
    Int value = make_value(30);
    Int^! assigned = make_value(40);
    assigned = make_value(3);
    return ptr + inferred + value + assigned;
}
