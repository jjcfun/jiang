enum Kind {
    a,
    b,
}

union(Kind) MyUnion {
    Int a;
    Int b;
}

Int main() {
    MyUnion value = MyUnion.a(1);
    if (value != MyUnion.a(_ payload)) {
        return payload;
    }
    return 0;
}
