concept HasValue {
    Int value();
}

struct Box {
    Int inner;
}

@where(T: HasValue)
Int read_value<T>(T item) {
    return 0;
}

Int main() {
    Box box = Box { inner: 42 };
    return read_value(box);
}
