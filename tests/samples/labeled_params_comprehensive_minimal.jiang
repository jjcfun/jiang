Int score(Int base, first: Int left, second: Int, bonus: Int extra = 3, tail: Int end = 4) {
    return base + left * 100 + second * 10 + extra + end;
}

Int main() {
    return score(1, second: 2, first: 3, tail: 5);
}
