import "../../compiler/support/list.jiang";

Int main() {
    ArrayList<Int>* list = new ArrayList<Int> {};
    list.add(7);
    list.add(35);
    Int result = list.get(0) + list.get(1) + list.len() + list.cap();
    list$.free();
    return result;
}
