import "../../compiler/support/list.jiang";

Int main() {
    list.ArrayList<Int>* items = new list.ArrayList<Int> {};
    items.add(7);
    items.add(35);
    Int result = items.get(0) + items.get(1) + items.len() + items.cap();
    items$.free();
    return result;
}
