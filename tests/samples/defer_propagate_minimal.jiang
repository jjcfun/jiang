Int! value = 0;

() push(Int digit) {
    value = value * 10 + digit;
}

enum Err {
    bad = 7,
}

()@Err inner() {
    defer push(1);
    throw Err.bad;
}

Int@Err outer() {
    defer push(2);
    inner();
    return 0;
}

Int main() {
    try outer() catch 0;
    return value;
}
