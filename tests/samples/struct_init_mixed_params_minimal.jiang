struct User {
    Int id;
    Int age = 18;
    Int! score = 0;

    init(Int base, Int id, Int points) {
        self.id = base + id;
        self.score = points;
        return;
    }
}

Int main() {
    User user = User(1, 41, 5);
    return user.id + user.age + user.score;
}
