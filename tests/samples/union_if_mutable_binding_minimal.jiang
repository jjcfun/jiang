enum Kind {
    a,
}

union(Kind) MyUnion {
    Int a;
}

Int main() {
    MyUnion value = .a(41);
    if (value == .a(_! payload)) {
        payload = payload + 1;
        return payload;
    }
    return 0;
}
