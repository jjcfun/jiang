enum Err {
    bad = 1,
}

Int@Err ok() {
    return 42;
}

Int main() {
    switch (ok()) {
    value x =>
        return x;
    error e => {
        if (e == Err.bad) {
            return 1;
        }
        return 0;
    }
    }
}
