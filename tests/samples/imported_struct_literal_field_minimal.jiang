import helper = "./imported_struct_literal_field_helper.jiang";

Int main() {
    helper.AstRange range = helper.AstRange(0, 0);
    helper.RootData root = helper.RootData {items: range};
    assert(root.items.empty());
    return 0;
}
