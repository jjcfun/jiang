struct User {
    Int id;

    init named(Int id) {
        self.id = id;
    }

    init named(Int value) {
        self.id = value;
    }
}

Int main() {
    User user = User.named(42);
    return user.id;
}
