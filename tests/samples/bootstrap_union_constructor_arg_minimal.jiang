union Value {
    Int item;
}

Int read(Value value) {
    switch (value) {
        .item(_ payload) => return payload;
        else => return 0;
    }
}

Int main() {
    return read(
        .item(42)
    );
}
