trait HasValue {
    Int value();
}

struct User {
    Int id;
}

extend User: HasValue {
    Int value() {
        return self.id;
    }
}

@where(T: HasValue)
Int read<T>(T value) {
    return value.value();
}

Int main() {
    User user = User(id: 42 );
    return read<User>(user);
}
