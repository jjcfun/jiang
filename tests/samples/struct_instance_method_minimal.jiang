struct User {
    Int id;

    init(Int id) {
        self.id = id;
        return;
    }

    Int value() {
        return self.id;
    }
}

Int main() {
    User user = User(42);
    return user.value();
}
