enum Kind {
    a,
    b,
}

union(Kind) MyUnion {
    Int a;
    Int b;
}

Int choose(MyUnion value) {
    switch (value) {
        MyUnion.a: return 1;
        MyUnion.b: return 42;
        else: return 0;
    }
}

Int main() {
    MyUnion value = MyUnion.b(123);
    return choose(value);
}
