import modes = "./multi_file_enum_helper.jiang";

struct Config {
    modes.Mode mode;
}

Int main() {
    Config config = Config(mode: .read );
    if (config.mode == modes.Mode.read) {
        return 1;
    }
    return 0;
}
