trait HasAnswer {
    Int answer();
}

enum Mode: HasAnswer {
    read,
    write,

    Int answer() {
        return self.value + 41;
    }
}

Int main() {
    Mode mode = Mode.write;
    return mode.answer();
}
