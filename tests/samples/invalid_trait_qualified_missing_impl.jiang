trait AddInt {
    Int apply(Int delta);
}

struct Counter {
    Int base;

    Int apply(Int delta) {
        return self.base + delta;
    }
}

Int main() {
    Counter counter = Counter { base: 1 };
    return counter.[AddInt].apply(2);
}
