struct Constants {
    static UInt64 mask32 = 4294967295;

    UInt64 value() {
        return Constants.mask32;
    }
}

Int main() {
    Constants constants = Constants {};
    return Int(constants.value() & UInt64(255));
}
