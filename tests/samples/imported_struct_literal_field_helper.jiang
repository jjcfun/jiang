public struct AstRange {
    public Int start;
    public Int len;

    public init(Int start, Int len) {
        self.start = start;
        self.len = len;
    }

    public Bool empty() {
        return self.len == 0;
    }
}

public struct RootData {
    public AstRange items;
}
