trait HasAnswer {
    Int answer();
}

enum Kind {
    a,
    b,
}

union(Kind) MyUnion: HasAnswer {
    Int a;
    Int b;

    Int answer() {
        return self.a + 40;
    }
}

Int main() {
    MyUnion value = .a(2);
    return value.answer();
}
