enum Err {
    bad = 7,
}

Int@Err parse(Bool fail) {
    if (fail) {
        throw Err.bad;
    }
    return 9;
}

Int main() {
    Int! result = 0;
    parse(true) catch (e) {
        if (e == Err.bad) {
            result = 42;
        }
    };
    return result;
}
