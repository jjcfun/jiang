import helper = "imported_typeid_field_helper.jiang";

Int read_id(helper.TypeId type_id) {
    Int! raw_type_id = type_id.id;
    return raw_type_id;
}

Int main() {
    return read_id(helper.TypeId(42));
}
