Int main() {
    UInt8 newline = '\n';
    UInt8 carriage = '\r';
    UInt8 tab = '\t';
    UInt8 zero = '\0';

    if (newline == 10 && carriage == 13 && tab == 9 && zero == 0) {
        return 42;
    }
    return 0;
}
