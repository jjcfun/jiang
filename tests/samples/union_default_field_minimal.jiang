union Payload {
    Int number;
    Bool flag;
}

struct Holder {
    Payload! payload;

    init() {
        self.payload = .number(42);
    }
}

Int main() {
    Holder holder = Holder();
    return holder.payload.number;
}
