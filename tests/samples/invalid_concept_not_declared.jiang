trait HasValue {
    Int value();
}

struct Box {
    Int inner;

    Int value() {
        return self.inner;
    }
}

@where(T: HasValue)
Int read_value<T>(T item) {
    return item.value();
}

Int main() {
    Box box = Box { inner: 42 };
    return read_value(box);
}
