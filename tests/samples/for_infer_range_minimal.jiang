Int loop_for(Int n) {
    Int sum = 0;
    for i in 0..n {
        if (i == 1) {
            continue;
        }
        if (i == 4) {
            break;
        }
        sum = sum + i;
    }
    return sum;
}

Int main() {
    return loop_for(10);
}
