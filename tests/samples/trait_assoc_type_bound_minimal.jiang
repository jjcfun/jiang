trait HasItem {
    type Item: Hashable & Equatable;
    Item item();
}

struct Box: HasItem {
    type Item = UInt8;

    UInt8 item() {
        return 42;
    }
}

Int main() {
    Box box = Box {};
    return box.item()$.as(Int);
}
