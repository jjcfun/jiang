struct Table {

    UInt8 value;

    init() {
        self.value = 20;
    }

    public UInt8 subscript_get(Int index) {
        return self.value;
    }
}

Int main() {
    Table table = Table();
    table[7] = 42$.as(UInt8);
    return 0;
}
