struct User {
    Int id;

    init(Int id) {
        self.id = id;
        return;
    }

    static Int value() {
        return 42;
    }
}

Int main() {
    User user = User(0);
    return user.value();
}
