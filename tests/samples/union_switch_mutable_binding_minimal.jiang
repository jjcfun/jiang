enum Kind {
    a,
}

union(Kind) MyUnion {
    Int a;
}

Int main() {
    MyUnion value = .a(41);
    switch (value) {
        .a(_! payload): {
            payload = payload + 1;
            return payload;
        }
        else: return 0;
    }
}
