enum ErrA {
    a,
}

enum ErrB {
    b,
}

Int@ErrA fail_a() {
    throw ErrA.a;
}

Int@ErrB fail_b() {
    throw ErrB.b;
}

Int main() {
    try {
        fail_a();
        fail_b();
    } catch (ErrA e) {
        return 1;
    }
    return 0;
}
