struct Math {
    static Int scale(Int value) {
        return value * 2;
    }

    static Int scale(Bool flag) {
        if (flag) {
            return 1;
        }
        return 2;
    }
}

Int main() {
    _ fn = Math.scale;
    return 0;
}
