struct Store {
    Int value;
}

struct Holder {
    Store store;
    Store* store_ptr;

    init() {
        self.store = Store { value: 1 };
        self.store_ptr = self.store$.ptr();
    }
}

Int main() {
    Holder holder = Holder();
    return holder.store_ptr == null;
}
