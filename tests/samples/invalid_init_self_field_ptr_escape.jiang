struct Store {
    Int value;
}

struct Holder {
    Store store;
    Store& store_ref;

    init() {
        self.store = Store { value: 1 };
        self.store_ref = self.store$.ref();
    }
}

Int main() {
    Holder holder = Holder();
    return holder.store_ref.value;
}
