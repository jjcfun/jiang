enum Err {
    bad = 7,
}

Int@Err parse(Bool fail) {
    if (fail) {
        throw Err.bad;
    }
    return 9;
}

Int main() {
    return parse(true) catch (e) {
        Int base = 35;
        if (e == Err.bad) base + 7 else 0
    };
}
