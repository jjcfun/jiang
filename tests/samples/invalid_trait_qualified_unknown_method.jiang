trait AddInt {
    Int apply(Int delta);
}

struct Counter: AddInt {
    Int base;

    Int apply(Int delta) {
        return self.base + delta;
    }
}

Int main() {
    Counter counter = Counter { base: 1 };
    return counter.[AddInt].missing(2);
}
