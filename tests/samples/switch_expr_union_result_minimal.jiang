union Payload {
    Int number;
    Bool flag;
}

Int main() {
    Payload value = switch (1) {
        0 => .flag(false);
        else => .number(42);
    };

    if (value is .number(n)) {
        return n;
    }
    return 1;
}
