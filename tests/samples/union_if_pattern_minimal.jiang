enum Kind {
    a,
    b,
}

union(Kind) MyUnion {
    Int a;
    Int b;
}

Int choose(MyUnion value) {
    if (value == MyUnion.a(_ payload)) {
        return payload;
    }
    return 0;
}

Int main() {
    return choose(MyUnion.a(42));
}
