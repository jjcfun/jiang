enum Kind {
    a,
}

union(Kind) MyUnion {
    Int a;
}

Int main() {
    MyUnion value = MyUnion.a(1);
    switch (value) {
        MyUnion.a(_ left, _ right): return 1;
        else: return 0;
    }
}
