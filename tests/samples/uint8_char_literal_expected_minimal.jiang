Int main() {
    UInt8 byte = 'a';
    UInt8? optional = 'b';
    if (byte == 'a') {
        if (optional is some value) {
            if (value == 'b') {
                return Int(byte) - 55;
            }
        }
    }
    return 0;
}
