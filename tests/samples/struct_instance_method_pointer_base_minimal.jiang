struct User {
    Int id;

    init(Int id) {
        self.id = id;
        return;
    }

    Int value() {
        return self.id;
    }
}

Int main() {
    User^ owner = new User(42);
    User& user = owner$.ref();
    Int result = user.value();
    owner$.free();
    return result;
}
