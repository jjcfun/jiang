enum Err {
    bad = 9,
}

()@Err ping(Bool fail) {
    if (fail) {
        throw Err.bad;
    }
    return;
}

Int main() {
    switch (ping(true)) {
    error e: {
        if (e == Err.bad) {
            return 9;
        }
        return 0;
    }
    else:
        return 0;
    }
}
