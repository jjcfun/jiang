enum Err {
    bad = 9,
}

()@Err ping(Bool fail) {
    if (fail) {
        throw Err.bad;
    }
    return;
}

Int main() {
    try ping(true) catch (e) {
        if (e == Err.bad) {
            return 9;
        }
    };
    return 0;
}
