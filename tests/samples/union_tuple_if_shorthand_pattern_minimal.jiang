enum Kind {
    c,
    e,
}

union(Kind) MyUnion {
    (Int, Int) c;
    () e;
}

Int choose(MyUnion value) {
    if (value == .c(_ left, _ right)) {
        return left + right;
    }
    return 0;
}

Int main() {
    return choose(.c((40, 2)));
}
