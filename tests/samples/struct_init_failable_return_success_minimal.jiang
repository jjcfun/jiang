struct User {
    Int id;

    init?(Int id) {
        self.id = id;
        return;
    }
}

Int main() {
    User? maybe = User(43);
    if (maybe == null) {
        return 1;
    }
    return maybe$.some().id;
}
