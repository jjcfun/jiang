struct Table {

    UInt8 value;

    init() {
        self.value = 42;
    }

    public UInt8 subscript_get(Int index) {
        return self.value;
    }
}

Int main() {
    Table table = Table();
    return table[7]$.as(Int);
}
