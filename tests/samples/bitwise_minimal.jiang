Int main() {
    Int a = 15 >> 1;
    Int b = a & 7;
    Int c = b ^ 5;
    Int d = c | 40;

    UInt8 zero = 0;
    UInt8 all = ~zero;
    UInt8 full = 255;
    Int keep = all == full ? 0 : 1;

    return d + keep;
}
