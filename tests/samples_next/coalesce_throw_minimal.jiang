enum Err {
    bad = 1,
}

()@Err check() {
    Int? value = 42;
    Int number = value ?? throw Err.bad;
}

Int main() {
    try check() catch (e) {
        if e == Err.bad {
            return 1;
        }
    };
    return 42;
}
