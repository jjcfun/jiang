// Test tuple return and binding assignment
(Int, Int) foo(Int a, Int b) {
    return (a * a, b * 2);
}

(_ x, _! y) = foo(10, 200);
assert(x == 100);
assert(y == 400);

y = y + 100;
assert(y == 500);

print("binding_test passed!\n");
