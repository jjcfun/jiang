// 这是一个完整的江语言测试用例
Int a = 123;
Int* p = new 100;
// $p.move(a); // 暂时注释掉，因为 C 里没这方法

Int[10] array;
Int!? nullable_mutable_int = 456;

Int result = a + 10 * 2;
// foo.bar.baz(a, result); // 暂时注释掉，避免 C 编译报错


