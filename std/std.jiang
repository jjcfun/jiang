// Internal std incubator entry point.
//
// 入口只重新导出 std 子模块命名空间，避免把 fs/io/process 的声明平铺到
// 顶层；调用方应使用 `import std;`，再访问 std.fs、std.io、std.process。

public import fs = "fs.jiang";
public import io = "io.jiang";
public import process = "process.jiang";
public import vector = "vector.jiang";
