public struct File {
    public Int fd;

    public Bool is_valid() {
        return fd >= 0;
    }

    public () close() {
        print("closing fd=%lld\n", fd);
    }

    public File open(Int fd) {
        return File { fd: fd };
    }
}
