// 进程级不可恢复错误入口。
//
// panic 向标准错误输出消息与换行后立即 abort；不执行 unwind，也不保证运行析构。

import abort = "../src/system/abort.jiang";

// 向 stderr 写入 message 和换行后 abort；不 unwind，也不保证 drop。
public Void panic(UInt8[]& message) {
    abort.panic(message);
}
