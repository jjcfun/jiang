public struct Allocator {
    UInt8* state;
}

public Allocator default_allocator() {
    return Allocator { state: null };
}

public () memmove(UInt8* dst, UInt8* src, Int bytes) {
    __intrinsic_memmove(dst, src, bytes);
}

public () free(UInt8* ptr) {
    __intrinsic_free(ptr);
}
