#doc(module) 将示例语言映射为 methods，并编译调用生成方法的程序。
#package {
    name = "parser_composition_example";
    type = .bin;
    root = "main.jiang";
    dependencies { methods = "../provider"; }
    lang methods { package = methods; }
}
