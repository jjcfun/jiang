// Cross-stage IDs shared by query API, name, sema, IR, and incremental layers.
//
// Rule: if an ID can cross a phase boundary, define it here. Phase-private
// array indexes stay in that phase's internal files.

import "../support/traits.jiang";

public struct QuerySessionId: Copy {
    public Int index;
}

public struct QueryId: Copy {
    public Int index;
}

public struct SymbolId: traits.Indexable, Hashable, Equatable, Copy {
    // SymbolId is always a valid 0-based interned symbol index.
    public Int index;

    public init(Int index) {
        self.index = index;
    }

    public Int to_index() {
        return self.index;
    }

    public UInt64 hash() {
        return UInt64(self.index);
    }

    public static Bool equal(SymbolId& lhs, SymbolId& rhs) {
        return lhs.index == rhs.index;
    }

    public static SymbolId from_index(Int index) {
        return SymbolId(index);
    }
}

public struct PackageId: traits.Indexable, Hashable, Equatable, Copy {
    public Int index;

    public init(Int index) {
        self.index = index;
    }

    public Int to_index() {
        return self.index;
    }

    public static PackageId from_index(Int index) {
        return PackageId(index);
    }

    public UInt64 hash() {
        return UInt64(self.index + 1);
    }

    public static Bool equal(PackageId& lhs, PackageId& rhs) {
        return lhs.index == rhs.index;
    }
}

public struct ModuleId: traits.Indexable, Hashable, Equatable, Copy {
    public Int index;

    public init(Int index) {
        self.index = index;
    }

    public Int to_index() {
        return self.index;
    }

    public static ModuleId from_index(Int index) {
        return ModuleId(index);
    }

    public UInt64 hash() {
        return UInt64(self.index + 1);
    }

    public static Bool equal(ModuleId& lhs, ModuleId& rhs) {
        return lhs.index == rhs.index;
    }
}

public struct NamespaceId: traits.Indexable, Copy {
    public Int index;

    public init(Int index) {
        self.index = index;
    }

    public Int to_index() {
        return self.index;
    }

    public static NamespaceId from_index(Int index) {
        return NamespaceId(index);
    }
}

public struct DefId: traits.Indexable, Hashable, Equatable, Copy {
    public Int index;

    public init(Int index) {
        self.index = index;
    }

    public Int to_index() {
        return self.index;
    }

    public static DefId from_index(Int index) {
        return DefId(index);
    }

    public UInt64 hash() {
        return UInt64(self.index + 1);
    }

    public static Bool equal(DefId& lhs, DefId& rhs) {
        return lhs.index == rhs.index;
    }
}

public struct TypeId: traits.Indexable, Equatable, Copy {
    public Int index;

    public init(Int index) {
        self.index = index;
    }

    public Int to_index() {
        return self.index;
    }

    public static TypeId from_index(Int index) {
        return TypeId(index);
    }

    public static Bool equal(TypeId& lhs, TypeId& rhs) {
        return lhs.index == rhs.index;
    }
}

public struct TypeParamId: Copy {
    public Int index;
}

public struct TraitId: Copy {
    public Int index;
}

public struct ImplId: Copy {
    public Int index;
}

public struct ExprId: Copy {
    public Int index;
}

public struct TypeRefId: Copy {
    public Int index;
}

public struct SignatureId: Copy {
    public Int index;
}
