// src/system 私有层 中操作系统 entropy 的统一入口。
//
// 不支持或调用失败时返回 false；这里不提供伪随机 fallback。

import provider = "./os/provider.jiang";

public Bool fill_entropy(UInt8[]&! bytes) {
    return provider.fill_entropy(bytes);
}
