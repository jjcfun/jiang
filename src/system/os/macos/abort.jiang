// macOS hosted abort/exit provider.

import c = "./c.jiang";

public () exit_process(Int code) {
    c.exit(Int32(code));
}

public () abort_process() {
    c.abort();
}

public () trap() {
    c.abort();
}
