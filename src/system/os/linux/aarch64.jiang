// Linux aarch64 no-libc syscall ABI.
//
// aarch64 syscall 使用 x8 传 syscall number，x0..x5 传参数，通过 svc #0
// 进入 kernel。实际 inline asm / compiler intrinsic 接入等待自定义 DSL
// 和 inline asm proposal 稳定后再推进。

public Bool available() {
    return false;
}
