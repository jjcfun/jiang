// Linux x86_64 no-libc syscall ABI.
//
// x86_64 syscall 使用 rax 传 syscall number，rdi/rsi/rdx/r10/r8/r9 传参数，
// 通过 syscall 指令进入 kernel。实际 inline asm / compiler intrinsic 接入
// 等待自定义 DSL 和 inline asm proposal 稳定后再推进。

public Bool available() {
    return false;
}
