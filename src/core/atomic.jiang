// 公开原子 API。操作由 compiler builtin lowering 实现。

public import * = "atomic_value.jiang";
public import * = "../builtin/atomic.jiang";

// AtomicValue 限定后端支持的原子 storage；无 body conformance 表示同步操作封装了该 storage。
@where(T: AtomicValue, T: Copyable)
public extend <T> Atomic<T>: Sendable;
