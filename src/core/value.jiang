// 编译器 core value 声明。
//
// 实现仍可以由编译器 lowered；名称和签名放在 core 源码里，避免 builtin API
// 继续合成这些声明。

// core 文件仍按普通模块作用域解析。builtin 类型名来自 primitives.jiang 中的
// 源码声明，而不是 core 加载阶段的隐式 prelude。
import * = "primitives.jiang";

extern {
    public () assert(Bool condition);
}
