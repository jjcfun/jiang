// Compiler core value declarations.
//
// Implementations may still be compiler-lowered, but names and signatures live
// in core source so the builtin API no longer has to synthesize them.

import * = "types.jiang";

extern {
    public () assert(Bool condition);
}
