// Compiler core module source skeleton.
//
// This file is intentionally not loaded by the current bootstrap pipeline yet.
// The next step is to teach module loading to import core before user roots and
// bind selected definitions here to compiler-known core traits.

public trait Contiguous {
    associated Element;

    Element[*] many_pointer();

    Int length();
}
