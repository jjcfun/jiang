// src/compiler/system 私有层在 macOS 上查询当前进程可用 CPU 数。

import c = "./libc.jiang";

const Int32 sysconf_processors_online = 58;

public UInt32 available_count() {
    Int count = unsafe { c.sysconf(sysconf_processors_online) };
    if (count <= 0) { return UInt32(1); }
    return UInt32(count);
}
