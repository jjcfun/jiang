#doc(module)
    Linux AArch64 no-libc 的裸入口，将初始栈交给共享的 Jiang 启动流程。
#end

@link_symbol("_start")
@naked
Void linux_start() {
    #asm {
        code "mov x0, sp; bl __jiang_linux_start_from_stack";
        clobber "x0";
        memory;
        volatile;
        noreturn;
    }
}
