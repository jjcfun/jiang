#doc(module)
    Linux x86_64 no-libc 的裸入口，将初始栈交给共享的 Jiang 启动流程。
#end

@link_symbol("_start")
@naked
Void linux_start() {
    #asm {
        code "movq %rsp, %rdi; callq __jiang_linux_start_from_stack";
        clobber "rdi";
        memory;
        volatile;
        noreturn;
    }
}
