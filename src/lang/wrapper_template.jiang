import provider;
import std;

public alias Lang = provider.Lang;

@life()
@link_symbol("jiang_lang_provider_create")
public std.jiang.syntax.Provider.Any^ jiang_lang_provider_create() {
    return std.jiang.syntax.Provider$.new(provider.Lang());
}
