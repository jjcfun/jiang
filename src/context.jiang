// Shared compiler context for one compilation session.

import query = "./query/api.jiang";

public struct CompilerContext {
    public query.QuerySystem^ query;

    public init() {
        self.query = new query.QuerySystem();
    }
}
