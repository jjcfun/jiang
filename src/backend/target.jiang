// Target triple and backend-independent codegen options.

public struct TargetOptions {
    public UInt8[]& triple;

    public init(self) {
        self.triple = "";
    }

    public init(self, UInt8[]& triple) {
        self.triple = triple;
    }
}
