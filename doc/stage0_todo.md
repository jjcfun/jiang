# Stage0 TODO

## 待完成

### 1. 产出目标文件与可执行文件

- 增加 `--emit-obj`，直接输出目标文件
- 增加最小链接流程，能够从 `main` 产出可执行文件
- 明确 `jiangc` 的输出模式：
  - `--emit-llvm`
  - `--emit-obj`
  - 默认生成可执行文件

### 2. 驱动与工具链整合

- 接入宿主平台链接器调用
- 处理标准库、运行时对象与入口符号
- 明确输出文件名与构建目录规则

### 3. 验证与文档

- 增加 `--emit-obj` 与可执行文件的回归样例
- 增加最小运行样例与 smoke test
- 更新 README 与 `jiang.md` 的编译说明
