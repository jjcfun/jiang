enum Kind {
    a,
    b,
}

union(Kind) MyUnion {
    Int a;
    Int b;
}

struct Box {
    MyUnion value;
}

Int unwrap(Box box) {
    if (box.value == .a(_ payload)) {
        return payload;
    }
    return 0;
}

Int main() {
    Box box = Box { value: .a(42) };
    return unwrap(box);
}
