enum Kind {
    a,
    b,
}

union(Kind) MyUnion {
    Int a;
    Int b;
}

Int choose(MyUnion value) {
    switch (value) {
        MyUnion.a: return 1;
    }
}

Int main() {
    return choose(MyUnion.b(42));
}
