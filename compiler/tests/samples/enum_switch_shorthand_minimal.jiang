enum Priority {
    low = 1,
    medium = 42,
    high = 3,
}

Int choose(Priority value) {
    switch (value) {
        .low: return 1;
        .medium: return 42;
        .high: return 3;
        else: return 0;
    }
}

Int main() {
    return choose(.medium);
}
