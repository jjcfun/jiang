import "./multi_file_enum_helper.jiang";

struct Config {
    Mode mode;
}

Int main() {
    Config config = Config { mode: .read };
    if (config.mode == Mode.read) {
        return 1;
    }
    return 0;
}
