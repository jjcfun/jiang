enum HttpStatus {
    ok = 42,
    bad = 1,
}

Int code(HttpStatus status) {
    return status.value;
}

HttpStatus make_ok() {
    return .ok;
}

Int main() {
    HttpStatus status = .ok;
    return code(status) + code(make_ok()) - 42;
}
