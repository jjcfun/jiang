struct Profile {
    Int age;
}

struct User {
    Profile? profile;
}

Int main() {
    User[1] users = {
        User {
            profile: Profile { age: 42 }
        }
    };
    Int? age = users[0].profile?.age;
    if (age != null) {
        return age;
    }
    return 0;
}
