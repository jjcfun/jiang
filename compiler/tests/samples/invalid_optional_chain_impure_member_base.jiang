struct Profile {
    Int age;
}

struct User {
    Profile? profile;
}

User load() {
    return User {
        profile: Profile { age: 42 }
    };
}

Int main() {
    Int? age = load().profile?.age;
    if (age != null) {
        return age;
    }
    return 0;
}
