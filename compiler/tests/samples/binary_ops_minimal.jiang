Int compare(Int a, Int b) {
    if (a == b) {
        return 1;
    }
    if (a != b) {
        return 2;
    }
    return 0;
}

Int math(Int a, Int b) {
    return a * b - a / b;
}

Int main() {
    return compare(math(6, 3), 16);
}
