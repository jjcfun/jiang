enum Priority {
    low = 1,
    medium = 42,
}

struct Config {
    Priority priority;
}

Int code(Config config) {
    return config.priority.value;
}

Int main() {
    Config config = Config { priority: .medium };
    return code(config);
}
