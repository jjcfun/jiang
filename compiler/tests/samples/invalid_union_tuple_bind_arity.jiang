enum Kind {
    c,
}

union(Kind) MyUnion {
    (Int, Int) c;
}

Int main() {
    MyUnion value = MyUnion.c((1, 2));
    switch (value) {
        MyUnion.c(_ a, _ b, _ c): return 1;
        else: return 0;
    }
}
