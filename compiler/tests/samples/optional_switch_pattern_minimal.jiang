Int unwrap(Int? value) {
    switch (value) {
        Option.some(_ x): {
            return x;
        }
        else: {
            return 0;
        }
    }
}

Int main() {
    return unwrap(42);
}
