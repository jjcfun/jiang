enum Priority {
    low,
    medium,
}

Int choose(Priority priority) {
    switch (priority) {
        Priority.low: return 1;
        Priority.low: return 2;
        else: return 3;
    }
}

Int main() {
    return choose(Priority.medium);
}
